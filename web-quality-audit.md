# تدقيق جودة الويب الحي — Billy Store

**تاريخ التدقيق:** 2026-07-25
**فرع التدقيق:** `audit/web-quality` (من `main`)
**نطاق التدقيق:** استكشاف وتوثيق فقط — **صفر تعديل على أي كود مصدري**. لا يتضمن هذا التقرير خطة إصلاح؛ القرار على خطة الإصلاح يصير لاحقًا مع المستخدم.
**منهجية التحقق:** تدقيق حي فعلي عبر Chrome DevTools MCP (`lighthouse_audit` لأربعة أبعاد — Accessibility/Best Practices/SEO/Agentic Browsing — و`performance_start_trace`/`performance_analyze_insight` لـPerformance/Core Web Vitals، `list_network_requests`/`get_network_request` لأحجام الشبكة الفعلية، `evaluate_script` للتحقق المباشر من الـDOM). **بناء إنتاجي فعلي** (`npm run build && npm start`)، وليس `next dev`، لتفادي أرقام أداء مضلَّلة بسبب HMR. كل رقم بهذا التقرير مقاس مباشرة، وليس تقديرًا من قراءة الكود.

---

## ⚠️ حالة المعالجة (تحديث لاحق — 2026-07-25، فرع `fix/web-quality-quick-wins`)

**10 من أصل 12 بند انحلّت بكود فعلي** على فرع `fix/web-quality-quick-wins` (من `main`)، كل بند بـcommit منفصل مع تحقق فعلي (Lighthouse جزئي/كامل، `evaluate_script`، `curl`، `list_network_requests` قبل/بعد). **بندان مؤجَّلان عمدًا لجلسة معمارية منفصلة**: بند 1 (إعادة هيكلة `/products` لـISR) وبند 12 (pagination لجدول الأدمن) — كلاهما تغيير أكبر يتطلب تصميم مستقل، خارج نطاق "quick wins".

### مقارنة Lighthouse قبل/بعد (Accessibility — الفئة الوحيدة اللي تغيّرت رقميًا عبر كل الصفحات)

| الصفحة | قبل | بعد |
|---|---|---|
| `/` (الرئيسية) | 98 | **100** |
| `/products` | 97 | **100** |
| `/products/[id]` | 96 | **100** |
| `/admin/dashboard` | 81 | **100** |

**SEO — ملاحظة مهمة على `/admin/dashboard` تحديدًا**: سكور SEO لهذي الصفحة انخفض من 100 (قبل) إلى **66 (بعد)** — هذا **مقصود وصحيح**، مش تراجع. السبب: `robots.ts` الجديد (بند 10) يمنع فهرسة `/admin` صراحة (`disallow: '/admin'`)، فـ`is-crawlable` بيفشل الآن لأن الصفحة فعليًا محجوبة عن الفهرسة — وهذا بالضبط المطلوب للوحة تحكم إدارية خاصة (كانت قابلة للفهرسة بالخطأ سابقًا، وهذا كان العيب الحقيقي، مش العكس). باقي الصفحات (`/`, `/products`, `/products/[id]`) بقيت SEO=100 كما هي.

Best Practices بقي 81 على الأربع صفحات بدون تغيير (نفس السبب الوحيد الموثَّق أصلًا: `is-on-https` يفشل حتمًا على `localhost` المحلي، قيد بيئة اختبار لا علاقة له بالكود — الإنتاج على Vercel يفرض HTTPS).

### حالة كل بند

- **بند 1** (`/products` بدون ISR، LCP 3.82s) — 📄 **مؤجَّل لجلسة معمارية منفصلة**. يتطلب تحويل الصفحة من client component لـServer Component + ISR، تغيير بنيوي أكبر من نطاق "quick wins".
- **بند 2** (لا meta tags ديناميكية) — ✅ **مُصلَح**. `generateMetadata` بـ`products/[id]/page.tsx` (title/description/canonical من بيانات المنتج الفعلية). تحقّق: `evaluate_script` على منتج حقيقي يعكس القيم الصحيحة، مش الثابتة القديمة.
- **بند 3** (لا Structured Data) — ✅ **مُصلَح**. `<script type="application/ld+json">` بـProduct schema (اسم، وصف، صورة، سعر، `availability: InStock`). تحقّق: `JSON.parse` لمحتوى السكربت الفعلي يطابق بيانات المنتج.
- **بند 4** (أزرار أيقونة-فقط بدون اسم accessible، 27 عنصر) — ✅ **مُصلَح**. `aria-label` صريح لكل من Pencil/Pause-Play/X بقواعد الخصم المستهدف. تحقّق: `evaluate_script` يؤكد 3 أسماء مختلفة بدل "button" فارغ؛ Lighthouse `button-name` صار يمر.
- **بند 5** (شعار/صور أدمن بحجم 24-50x أكبر من اللازم) — ✅ **مُصلَح**. `next/image` للشعار (Navbar/Footer) وصور جدول الإحصائيات بالأدمن. تحقّق رقمي فعلي: الشعار 479KB→~1KB (~493x)، صورة أدمن واحدة 206KB→~1.6KB (~130x)، وعدد طلبات الصور بتبويب الإحصائيات انخفض من 315 (eager) لـ30 فقط عند الفتح (بفضل `loading="lazy"`).
- **بند 6** (تباين شارة الخصم/العدّاد) — ✅ **مُصلَح** (بآلية مختلفة عن الطلب الحرفي الأصلي، بقرار المستخدم بعد اكتشاف السبب الجذري). السبب الحقيقي كان `opacity-60` على كارت القاعدة غير المفعّلة، مش لون النص. رُفع لـ`opacity-90` سطر واحد. تحقّق: Lighthouse `color-contrast` صار يمر (كان فاشلًا بـ18 عنصر)، وتأكيد بصري إن الفرق بين قاعدة نشطة/معطّلة لسا واضح.
- **بند 7** (Checkboxes بدون اسم accessible، 397 عنصر) — ✅ **مُصلَح**. `aria-label` ديناميكي لكل صف (`تحديد {اسم المنتج}`) وثابت للهيدر. تحقّق: `evaluate_script` على 396 صف + الهيدر يؤكد تسميات صحيحة ومختلفة.
- **بند 8** (heading-order مكسور، 4 مواقع) — ✅ **مُصلَح**. تصحيحات تسلسل عبر الصفحات الأربع (rename بدون تغيير className + إضافة `h2.sr-only` واحد بـ`/products`). تحقّق: Lighthouse `heading-order` صار يمر على كل الصفحات الأربع + لقطات شاشة تؤكد صفر تغيير بصري.
- **بند 9** (لا `<main>` landmark) — ✅ **مُصلَح**. `<main>` واحد بكل صفحة (لف أو rename حسب البنية). تحقّق: `document.querySelectorAll('main').length === 1` على الأربع صفحات + لقطات شاشة.
- **بند 10** (لا sitemap.xml/robots.txt) — ✅ **مُصلَح**. `src/app/sitemap.ts` (مع `revalidate = 3600` لتفادي استعلام Turso بكل زحف) و`src/app/robots.ts`. تحقّق: `curl` يرجع 200 لكليهما، وعدّ فعلي لعناصر `<loc>` بالـXML الناتج = 400 بالضبط (4 صفحات ثابتة + 396 منتج حقيقي — طابق `SELECT COUNT(*)` قبل الاعتماد على الاستعلام).
- **بند 11** (لا Open Graph/canonical) — ✅ **مُصلَح** (نفس commit بند 2/3). `openGraph` + `alternates.canonical` + `metadataBase` جديد بـ`layout.tsx`. تحقّق: `meta[property=og:*]` و`link[rel=canonical]` تعكس بيانات المنتج والدومين الفعلي (`https://billy-store.vercel.app`، مؤكَّد من المستخدم — لا دومين موثَّق آخر بالمشروع).
- **بند 12** (DOM ضخم بلوحة الأدمن، بدون pagination) — 📄 **مؤجَّل لجلسة معمارية منفصلة**. يتطلب تصميم pagination/virtualization لجدول ~400 منتج، خارج نطاق "quick wins".

### ملاحظة بيئية إضافية اكتُشفت أثناء التنفيذ (غير موثَّقة بالتدقيق الأصلي)

`src/app/sitemap.ts` و`robots.txt` يُبنيان كصفحات **ثابتة (Static)** وقت `npm run build` (ليس عند كل طلب runtime رغم `revalidate = 3600` — القيمة دي تتحكم بإعادة التوليد اللاحقة، مش أول بناء). يعني: **بيئة بناء الإنتاج (Vercel) لازم يكون فيها `TURSO_URL`/`TURSO_AUTH_TOKEN` الحقيقيين وقت الـ`build step` نفسه**، مش بس وقت التشغيل — وهذا فعليًا الحال أصلًا بـVercel (متغيرات البيئة متاحة لمرحلتي البناء والتشغيل معًا هناك)، لكن يستحق الانتباه له لو جرَّب أي حد بناء محليًا بدون تصدير المتغيرات على مستوى الـshell قبل `npm run build` تحديدًا (وليس `npm start` فقط) — بناء محلي بدونها بيولّد sitemap بـ8 منتجات تجريبية فقط من قاعدة SQLite الاحتياطية بدل الـ396 الحقيقية، بصمت وبدون أي خطأ ظاهر.

---

## ⚠️ ملاحظة بيئة مكتشفة أثناء التدقيق (مهمة، خارج نطاق النتائج نفسها)

أثناء تحضير التدقيق، اكتُشف أن `.env.local` يحتوي على `TURSO_URL=` و`TURSO_AUTH_TOKEN=` **فارغين** (سطران موجودان لكن بدون قيمة)، بينما `.env` يحتوي على القيم الحقيقية. Next.js يعطي أولوية لـ`.env.local` حتى لو كانت القيمة فارغة (لا يملأ الفراغ من `.env`) — أي تشغيل محلي عادي لـ`npm run dev`/`npm start` **يستخدم فعليًا قاعدة SQLite المحلية الاحتياطية (`data/billy.db`، 8 منتجات تجريبية بدون أي صورة)، وليس Turso الحقيقية**، دون أي تحذير ظاهر.

تحقّقنا من هذا مباشرة: `npm start` العادي أرجع `/api/products/2011` → `404` (منتج غير موجود بالقاعدة المحلية)، بينما حقن `TURSO_URL`/`TURSO_AUTH_TOKEN` الحقيقيين على مستوى الـshell فقط (بدون لمس أي ملف) جعل نفس المسار يرجع بيانات المنتج الحقيقي فورًا.

**لهذا التدقيق تحديدًا**: بناءً على تفضيل صريح من المستخدم، أُعيد تشغيل `npm start` مع `TURSO_URL`/`TURSO_AUTH_TOKEN` الحقيقيين مُمرَّرين كمتغيرات بيئة على مستوى العملية (shell) فقط — **لم يُعدَّل `.env.local` ولا أي ملف آخر**. كل الأرقام بهذا التقرير مقاسة ضد بيانات الإنتاج الحقيقية على Turso.

**لا تأكيد قاطع بدون تحقق منفصل**: يُحتمل أن جولات تدقيق سابقة اعتمدت على `npm run dev`/`npm start` عاديين محليًا (مثل `discount-audit.md` و`security-audit.md`) كانت تعمل بالفعل ضد قاعدة SQLite الاحتياطية المحلية بدل Turso الحقيقية — هذا **يستحق تأكيدًا منفصلًا لاحقًا**، لا نجزم به هنا. `.env.local` نفسه **لم يُصلَح بهذه الجلسة** — إصلاحه قرار منفصل خارج نطاق هذا التدقيق.

---

## ملخص السكورات (Lighthouse، mobile emulation)

| الصفحة | Performance* | Accessibility | Best Practices | SEO | Agentic Browsing |
|---|---|---|---|---|---|
| `/` (الرئيسية) | انظر Core Web Vitals أدناه | **98** | 81** | **100** | 100 |
| `/products` | انظر Core Web Vitals أدناه | **97** | 81** | **100** | 100 |
| `/products/2011` (Alone in The Dark) | انظر Core Web Vitals أدناه | **96** | 81** | **100** | 100 |
| `/admin/dashboard` | انظر Core Web Vitals أدناه | **81** | 81** | 100 | **50** |

\* أداة `lighthouse_audit` بـChrome DevTools MCP لا تشمل فئة Performance ضمن السكور الإجمالي (تتطلب `performance_start_trace` منفصلة لـLCP/CLS/TBT الفعلية — مفصّلة بالجدول التالي). لا يوجد سكور Lighthouse Performance رقمي إجمالي (0-100) بهذا التدقيق؛ الاعتماد على Core Web Vitals الخام المقاسة مباشرة، وهي أدق أصلًا من السكور المجمّع.
\*\* **81 على الأربع صفحات بالضبط لنفس السبب الوحيد**: فحص `is-on-https` (Lighthouse يفحص `http://localhost:3000` محليًا، فيفشل حتمًا). هذا **قيد بيئة الاختبار المحلي، وليس عيبًا حقيقيًا** — الموقع بالإنتاج على Vercel يعمل عبر HTTPS إجباريًا (مؤكَّد بـ`security-audit.md`، Gotcha #16، هيدر HSTS موجود). **لا توجد أي مخالفة Best Practices حقيقية أخرى على أي من الصفحات الأربع** (تحقّق مباشر: صفر audits فاشلة غير `is-on-https` بكل تقارير الـJSON الأربعة).

## Core Web Vitals (مقاسة مباشرة عبر Performance Trace، بناء إنتاجي، بدون throttling)

| الصفحة | LCP | CLS | TTFB | تفصيل LCP |
|---|---|---|---|---|
| `/` (الرئيسية) | **818ms** ✅ | **0.01** ✅ | 48ms | Render delay 770ms (94%) — الباقي تحميل سريع |
| `/products` | **3,821ms** 🔴 | 0.02 ✅ | 47ms | **Load delay 2,509ms (65.7%)** + Load duration 1,207ms (31.6%) — TTFB وRender delay مجتمعين أقل من 2% |
| `/products/2011` | **543ms** ✅ | **0.00** ✅ | 35ms | Load delay 27ms فقط — صورة الـLCP مكتشَفة فورًا من الـHTML |
| `/admin/dashboard` | **358ms** ✅ | 0.06 (قريب من الحد) | 36ms | Render delay 321ms |

**تصنيف Lighthouse القياسي لـLCP:** جيد <2.5s، يحتاج تحسين 2.5-4s، سيء >4s. **`/products` عند 3.82 ثانية يقع بالكامل في نطاق "يحتاج تحسين" — الأبطأ بمقدار 4.7x من الرئيسية و7x من تفاصيل المنتج.**

---

## 1) Performance

### 🔴 حرج — `/products` بدون ISR (Gotcha #18) يُنتج LCP أبطأ بـ4.7-7x من باقي الصفحات — مؤكَّد رقميًا الآن

**الفرضية بـGotcha #18 (فرق نظري بين revalidate=60/30 و"بدون ISR إطلاقًا") تحققت بشكل صارخ، أوضح من المتوقع.**

القياس المباشر (`performance_start_trace`، جدول Core Web Vitals أعلاه): LCP على `/products` = **3,821ms**، مقابل 818ms على الرئيسية و543ms على تفاصيل المنتج. تفصيل `LCPBreakdown` (عبر `performance_analyze_insight`) يحدد السبب بدقة:
- **Time to First Byte: 47ms فقط (1.2% من الوقت الكلي)** — الخادم سريع، المشكلة ليست بالباك-إند.
- **Resource load delay: 2,509ms (65.7% من الوقت الكلي)** — الوقت بين بداية الطلب واكتشاف صورة الـLCP.
- **Resource load duration: 1,207ms (31.6%)**.

السبب الجذري بالكود: [src/app/products/page.tsx](src/app/products/page.tsx) هي `'use client'` بالكامل ([السطر 1](src/app/products/page.tsx#L1)) بدون `revalidate` export، تجلب بياناتها عبر `fetch('/api/discounts')` داخل `useEffect` ([السطر 33](src/app/products/page.tsx#L33)) بعد الـhydration الكاملة — أي أن المتصفح يجب أن: يُنزّل الـHTML الفارغ (بدون منتجات) ← يُنزّل حزمة JS ← يُنفّذها ويُهيدرت ← يُطلق `useEffect` ← يطلب `/api/products`/`/api/discounts` عبر الشبكة ← يستقبل الاستجابة ← *عندها فقط* يبدأ رندر شبكة المنتجات (التي تحوي صورة الـLCP). هذا بالضبط ما يفسّر الـ2.5 ثانية "load delay" — الصورة غير مكتشَفة من الـHTML الأولي إطلاقًا، بعكس الرئيسية (`revalidate = 60`) وتفاصيل المنتج (`revalidate = 30`) حيث المنتجات (وصورها) موجودة في الـHTML المُقدَّم من الخادم مباشرة (Server Component + ISR)، فتُكتشف صورة الـLCP فورًا (`Load delay: 27ms` فقط بصفحة تفاصيل المنتج).

**الأولوية: 🔴 حرجة.** هذا يضع `/products` — الصفحة الأهم تجاريًا (تصفح كامل الكتالوج) — في نطاق "يحتاج تحسين" لـCore Web Vitals، بينما باقي الموقع "جيد". تحويل الصفحة لـServer Component (Next.js App Router) مع نفس نمط ISR المستخدم بالفعل بصفحتي الرئيسية/تفاصيل المنتج هو الحل المباشر — خارج نطاق هذا التدقيق (استكشاف وتوثيق فقط).

### 🟡 متوسط — شعار الموقع (`<img>` خام) محمَّل بحجم 50x أكبر من اللازم (Gotcha #4/#6.5) — مقاس رقميًا الآن

القياس المباشر (`get_network_request` + `evaluate_script` على صفحة تفاصيل المنتج): [Navbar.tsx:76](src/components/layout/Navbar.tsx#L76) و[Footer.tsx:11](src/components/layout/Footer.tsx#L11) (`<img src="/logo.jpg">`) يحمّلان ملفًا بحجم **2000×2000px طبيعي (490,776 بايت ≈ 479 KB) JPEG خام** بينما حجم العرض الفعلي على الشاشة **40×40px فقط** (`h-10 w-10`) — أي **2500 ضعف عدد البكسلات** اللازمة، بدون أي تصغير أو تحويل صيغة حديثة (WebP/AVIF) أو `srcset`.

للمقارنة المباشرة على نفس الصفحة بالضبط: صورة المنتج (Cloudinary عبر `next/image`) بحجم عرض 640px تُحمَّل كـ**20,610 بايت (~20KB) بصيغة AVIF** مع cache-control سنة كاملة. الفرق: **~24x أكبر حجمًا لصورة تُعرض في مساحة 256 ضعف أصغر (40×40 مقابل ~640px).**

**أثر أسوأ بكثير على لوحة الأدمن**: تبويب "الإحصائيات" ([dashboard/page.tsx:911](src/app/admin/dashboard/page.tsx#L911)) يستخدم `<img src={p.image}>` خام **لكل منتج في الجدول** (396 منتج فعليًا بقاعدة الإنتاج وقت التدقيق) بدون `next/image` وبدون `loading="lazy"`. قياس مباشر (`list_network_requests`): **315 طلب صورة منفصل أُطلق فعليًا عند فتح التبويب** (بدون أي تحكم بالتحميل الكسول). عينة مقاسة: صورة واحدة بحجم **210,788 بايت (~206KB)**، أبعادها الطبيعية **1024×1024px**، تُعرض بمساحة **32×32px فقط** — نفس مشكلة الشعار لكن مُكرَّرة 396 مرة على صفحة واحدة. حتى مع ضغط Cloudinary التلقائي (`f_auto,q_auto` بالرابط نفسه)، غياب تحديد حجم العرض المطلوب (`w=`/`h=` بالرابط أو عبر `next/image`) يعني تحميل الدقة الكاملة دائمًا.

**الأثر على CLS تحديدًا**: رغم الحجم الزائد، القياس الفعلي لـCLS على كل الصفحات الأربع كان منخفضًا (0.00-0.06، كلها تحت حد 0.1 "جيد") — أي أن الأثر العملي المُقاس هو **حجم بيانات منقولة ووقت تحميل شبكة زائد، وليس قفزات تخطيط ملحوظة** (على الأرجح لأن حاويات الصور مقيَّدة بأبعاد CSS ثابتة `h-10 w-10`/`w-8 h-8` بغض النظر عن وقت تحميل الصورة). **الأولوية: 🟡 متوسطة** — لا يكسر Core Web Vitals حاليًا، لكنه هدر شبكة/بيانات حقيقي ومقاس، وأثره يتضخم مع نمو عدد المنتجات (كل منتج جديد بلوحة الأدمن = +200KB تقريبًا لتبويب الإحصائيات).

### 🟡 متوسط — DOM ضخم بلوحة الأدمن بدون pagination/virtualization — اكتشاف جديد

قياس مباشر (`performance_analyze_insight` → `DOMSize`) على `/admin/dashboard`: **8,661 عنصر DOM إجمالي**، أكبر أب واحد (`TBODY` — جدول إدارة المنتجات) له **396 ابنًا مباشرًا** (كل المنتجات مُرندرة دفعة واحدة بدون pagination). أحداث إعادة تخطيط (layout) ضخمة مقاسة فعليًا: **358ms و346ms لكل منها، تؤثر على 9,137-9,561 عقدة** في كل مرة. هذا يفسر جزئيًا لماذا CLS على هذه الصفحة (0.06) أعلى من باقي الصفحات رغم بقائه تحت حد "جيد". **الأولوية: 🟡 متوسطة** — لا يكسر أي معيار حاليًا بحجم الكتالوج الحالي (~400 منتج)، لكنه سينمو خطيًا مع كل منتج جديد، وقد يتحول لمشكلة INP حقيقية عند التفاعل (فرز، فلترة) على كتالوج أكبر.

---

## 2) Accessibility

design-audit.md (سكيل `ui-ux-pro-max`، تدقيق يدوي سابق) غطّى تباين النص العام (`text-muted`)، أحجام أزرار اللمس، ربط labels بـinputs، وتسلسل عناوين عام. **ما يلي فجوات جديدة اكتشفها Lighthouse تلقائيًا (axe-core) ولم يغطها ذلك التدقيق اليدوي** — تم التحقق من كل واحدة بقراءة الكود مباشرة لتأكيد الموقع بالضبط.

### 🔴 حرج — أزرار أيقونة-فقط بدون اسم accessible بلوحة الخصومات المستهدفة — انحدار من تنفيذ design-audit.md نفسه

design-audit.md (بند 5.3) وثّق استبدال إيموجي ✏️/⏸▶/✕ بأيقونات `lucide-react` (Pencil/Pause-Play/X) كـ"مُنفّذ بالكامل" من منظور بصري/إيموجي. لكن Lighthouse (`button-name`، ومكرر بـ`agent-accessibility-tree`) يكشف أن الاستبدال أزال أي اسم accessible كان متاحًا (الإيموجي نفسه، رغم عيوبه، كان نصًا يُقرأ لبعض قارئات الشاشة) دون إضافة `aria-label` بديل. **27 عنصر فاشل بالضبط** (9 قواعد خصم مستهدفة × 3 أزرار).

**الموقع بالضبط**: [dashboard/page.tsx:548-550](src/app/admin/dashboard/page.tsx#L548) (`<Pencil size={16}>` بدون aria-label)، [:551-554](src/app/admin/dashboard/page.tsx#L551) (`<Pause>`/`<Play>`)، [:555-558](src/app/admin/dashboard/page.tsx#L555) (`<X>`). الثلاثة أزرار HTML فارغة تمامًا من نص عدا الـSVG.

**الدليل الرقمي**: `button-name` score **0**, 27 عنصر فاشل. **الأولوية: 🔴 حرجة** — قارئ شاشة يقرأ هذه الأزرار الثلاثة بدون أي تمييز بينها ("button", "button", "button") — وظيفة إدارية أساسية (تعديل/تفعيل/حذف قاعدة خصم) غير قابلة للاستخدام عبر screen reader.

**✅ تحقّق نطاق إضافي (بعد سؤال مباشر من المستخدم): هل هذا النمط موجود بمكان آخر بالمشروع؟**

فُحص كل استخدام لأيقونات `lucide-react` عبر المشروع بالكامل (الملفات الستة التي تستوردها فعليًا: `dashboard/page.tsx`، `ProductCard.tsx`، `products/page.tsx`، `products/[id]/page.tsx`، `page.tsx`، `admin/login/page.tsx` — تحقّق `Grep` مباشر، لا ملف سابع يستورد `lucide-react`)، بالإضافة لكل مكوّن بـ`src/components/` يحتوي `<button>` (`ProductActions.tsx`, `FaqAccordion.tsx`, `ConfirmDialog.tsx`, `ScrollToTop.tsx`, `AnnouncementBar.tsx`, `Navbar.tsx`). **النتيجة: النمط محصور فعليًا بهذا المكان الوحيد (الثلاثة أزرار بسطور 548-558).**

- **أزرار جدول المنتجات (تعديل/حذف) التي سأل عنها المستخدم تحديدًا** — [dashboard/page.tsx:762-769](src/app/admin/dashboard/page.tsx#L762): **غير متأثرة إطلاقًا** — نصّ ظاهر بالكامل ("تعديل"/"حذف")، بدون أيقونة إطلاقًا. نفس الشيء لبقية أزرار الخصم العام والإعلان ([:469-482](src/app/admin/dashboard/page.tsx#L469)، [:588-601](src/app/admin/dashboard/page.tsx#L588)): كل زر فيه أيقونة (`Pause`/`Play`/`Pencil`/`Trash2`) **مرفَق بنص ظاهر مجاور** ("إيقاف مؤقت"، "تعديل الخصم"، "حذف"...) — النص وحده كافٍ لاسم accessible صحيح رغم غياب `aria-label`، فلا مشكلة.
- أزرار الحفظ/الإلغاء بكل المودالات الخمسة (السطور 1039-1338) — نفس النمط: أيقونة (`Save`/`TrendingUp`/`TrendingDown`) + نص ظاهر دائمًا.
- زر إظهار/إخفاء كلمة المرور بصفحة تسجيل الدخول ([admin/login/page.tsx:83-86](src/app/admin/login/page.tsx#L83)) — أيقونة-فقط (`Eye`/`EyeOff`) لكن **معه `aria-label` ديناميكي فعلي بالفعل** (إصلاح design-audit.md بند 4.4، لا يزال قائمًا).
- زر الهامبرغر وأزرار التواصل الاجتماعي بـ`Navbar.tsx` ([:88-102](src/components/layout/Navbar.tsx#L88)) — أيقونة-فقط لكن **كلها معها `aria-label`** بالفعل (إصلاح design-audit.md بند 6.1/6.2).
- `ScrollToTop.tsx` (سهم للأعلى، SVG خام مش lucide) — أيقونة-فقط لكن **معه `aria-label="العودة لأعلى"`**.
- أزرار "×" النصية (وليست أيقونة SVG) بـ[dashboard/page.tsx:623](src/app/admin/dashboard/page.tsx#L623)، [:864](src/app/admin/dashboard/page.tsx#L864)، و[AnnouncementBar.tsx:20-25](src/components/ui/AnnouncementBar.tsx#L20) — هذه **ليست من نمط استبدال إيموجي بـlucide-react** (الرمز `×` هنا نص Unicode حرفي داخل الزر، وليس أيقونة SVG)، وتُعتبر تقنيًا نصًا ظاهرًا لقارئ الشاشة (لم تظهر بقائمة الـ27 عنصر الفاشلة بـ`button-name`) — **لا تدخل ضمن نطاق هذا الفحص**، رغم أن النطق الفعلي لقارئ الشاشة لها ("علامة ضرب") غير واضح دلاليًا كإشارة "إغلاق"؛ هذه ملاحظة UX منفصلة أقل أولوية، خارج نطاق سؤال المستخدم المحدد هنا.

**الخلاصة الأصلية (وقت هذا الفحص): لا يوجد توسّع بالنطاق — المشكلة محصورة بالضبط بثلاثي أزرار قاعدة الخصم المستهدف (27 عنصر، سطور 548-558)، ولم تتكرر بأي مكان آخر بالمشروع.** ⚠️ **تصحيح لاحق**: هذا الفحص بحث تحديدًا عن أيقونات `lucide-react` (Pencil/Trash2/X/...)، ففاته نمط مختلف تمامًا (مفاتيح تبديل بدون أي أيقونة إطلاقًا) — موثّق بالبند الجديد أدناه، اكتُشف بجلسة إصلاح لاحقة (`fix/web-quality-quick-wins`) أثناء اختبار تفاعلي حي، مش بهذا التدقيق الأصلي.

### 🟡 متوسط — اكتشاف جديد خارج نطاق هالدفعة (2026-07-25، أثناء QA حي لفرع `fix/web-quality-quick-wins`) — 4 مفاتيح تبديل بدون اسم accessible

أثناء اختبار تفاعلي حي بعد إصلاحات هالدفعة (فتح مودال "تعديل الخصم المستهدف" فعليًا)، ظهرت ملاحظة DevTools "No label associated with a form field" لم تكن موجودة عند تحميل الصفحة العادي. تحقّق مباشر (`lighthouse_audit` بوضع `snapshot` — يفحص حالة الـDOM الحالية بدون إعادة تحميل، بينما المودال مفتوح) حدد السبب بدقة: `button-name` فشل لعنصر واحد ظاهر بالحالة، لكن **نفس النمط بالضبط مكرر حرفيًا بـ4 مواقع** (تحقّق `Grep` مباشر بكامل الملف):

- [dashboard/page.tsx:832-837](src/app/admin/dashboard/page.tsx#L832) — مفتاح تفعيل الشريط الإعلاني (`annForm.active`).
- [dashboard/page.tsx:1220-1225](src/app/admin/dashboard/page.tsx#L1220) — مفتاح تفعيل الخصم العام (`discountForm.active`).
- [dashboard/page.tsx:1165-1170](src/app/admin/dashboard/page.tsx#L1165) — مفتاح تفعيل قاعدة الخصم المستهدف (`targetedForm.active`) — **هذا بالضبط اللي أطلق تحذير DevTools وقت الاختبار**.
- [dashboard/page.tsx:1321-1326](src/app/admin/dashboard/page.tsx#L1321) — مفتاح "منتج مميز" (`form.featured`) بمودال إضافة/تعديل منتج.

**البنية المشتركة بكل الأربعة**: `<button type="button" onClick={...} className="min-w-11 min-h-11 flex-shrink-0 inline-flex items-center justify-center">` يحتوي فقط عنصرَي `<span>` زخرفيَّين بحتين (مسار المفتاح + الدائرة المتحركة، بدون أي نص) — **بدون** `aria-label` أو `aria-labelledby` أو `role="switch"`/`aria-checked`. النص الوصفي الفعلي ("مفعّل"/"موقوف"، "منتج مميز...") موجود بـ`<span>` **شقيق منفصل خارج الزر تمامًا** (مثال: [:1171](src/app/admin/dashboard/page.tsx#L1171))، فلا يُحتسب كاسم accessible للزر عبر أي آلية HTML قياسية.

**لماذا فات كل التدقيقات السابقة**: هذي الأربعة مفاتيح هي بالضبط "مفاتيح التبديل الأربعة" الموثّقة أصلًا بـ`design-audit.md` (بند 5.2) — لكن ذلك الإصلاح عالج **حجم منطقة اللمس فقط** (توسيع لـ44×44px، وهو سبب وجود الـ`<button>` الحاوي أصلًا)، بدون التطرق لاسم accessible. وفحص النطاق اللي عملته بهالجلسة (أعلى هالبند) بحث عن أيقونات `lucide-react` تحديدًا، وهذي المفاتيح لا تستخدم أي أيقونة (spans مصممة بـCSS فقط) فما ظهرت بنتيجة البحث. وبتدقيق `web-quality-audit.md` الأصلي، فحص Lighthouse كان بوضع `navigation` (تحميل الصفحة الافتراضي)، وهذي المفاتيح جوا مودالات مغلقة افتراضيًا — بالضبط نفس فجوة "المودالات المغلقة" الموثّقة أصلًا بـ`design-audit.md` ("ملاحظة تحقق منهجية"). ثلاث طبقات فحص مختلفة، كل وحدة عندها نقطة عمياء مختلفة أدّت لنفس الفجوة تفلت من الكل.

**الأولوية: 🟡 متوسطة** (نفس تصنيف بند 7 الأصلي — Checkboxes بدون اسم accessible) — قارئ شاشة يسمع "button" فارغ بدل "مفعّل"/"موقوف"، أربع وظائف إدارية (تفعيل إعلان، تفعيل خصم عام، تفعيل قاعدة مستهدفة، تمييز منتج) غير مفهومة الحالة عبر screen reader. **لا حرجة** لأنها: (أ) إدارية فقط (لا تصل لزوار الموقع)، (ب) النص الوصفي موجود بصريًا ملاصق مباشرة للمفتاح (مش غايب كليًا كما كانت أزرار Pencil/Pause/X الأصلية)، فبعض قارئات الشاشة الحديثة بوضع "استكشاف" العام قد تلتقطه كسياق مجاور حتى لو التقنية غير معتمدة رسميًا.

**لا إصلاح الآن** — هذا اكتشاف موثَّق فقط، خارج نطاق الثمانية بنود المُنفَّذة بفرع `fix/web-quality-quick-wins`. يشبه بند 7 الأصلي (labels بدون ربط)، ويستاهل جلسة إصلاح منفصلة صغيرة (نفس نمط الحل المتوقع: `aria-label` ديناميكي حسب الحالة، أو تحويله لـ`role="switch"` مع `aria-checked` كحل أدق دلاليًا لعنصر toggle حقيقي).

**✅ مُصلَح (2026-07-26، فرع `fix/toggle-accessible-names` من `main`)** — أُضيف `aria-label` ديناميكي لكل من الأربعة (`تفعيل الإعلان`/`إيقاف الإعلان`، `تفعيل الخصم`/`إيقاف الخصم`، `تفعيل القاعدة`/`إيقاف القاعدة`، `تمييز المنتج`/`إلغاء تمييز المنتج`)، بنفس منهجية التحقق المستخدمة لاكتشاف المشكلة أصلًا: فتح كل مودال حي فعليًا وتشغيل `lighthouse_audit` بوضع `snapshot` — **Accessibility 100 وصفر فشل `button-name`/`label` على الأربعة**، وتأكيد مباشر لقيمة `aria-label` بكل حالة. Commit واحد: `fix: add accessible names to admin toggle switches`.

### 🟡 متوسط — تباين لوني حقيقي فاشل على شارات الخصم وعدّاد المنتجات بلوحة الخصومات المستهدفة

design-audit.md عالج `text-slate-500/600` → `text-muted` بحساب تباين عام (6.28:1+) على خلفيات `dark`/`dark-card`/`dark-border`. لكن Lighthouse يكشف فشلًا فعليًا **في سياق محدد لم يُختبر هناك**: بطاقة قاعدة الخصم المستهدف (خلفية مختلفة قليلًا عن الحالات المختبَرة سابقًا). **18 عنصر فاشل بالضبط** (9 قواعد × عنصرين):
- `text-red-400` لنسبة الخصم (مثال: "-50%") — [dashboard/page.tsx:520](src/app/admin/dashboard/page.tsx#L520): تباين مقاس فعليًا **3.23:1** (يحتاج 4.5:1).
- `text-muted` لعدد المنتجات (مثال: "63 منتج") — [dashboard/page.tsx:523](src/app/admin/dashboard/page.tsx#L523): تباين مقاس فعليًا **3.01:1**.

**الأولوية: 🟡 متوسطة** — نص إعلامي (ليس تفاعلي)، لكن فشل WCAG AA حقيقي ومقاس، وليس تقديرًا.

### 🟡 متوسط — Checkboxes بدون اسم accessible بجدول إدارة المنتجات

design-audit.md (بند 5.6) عالج فقط حجم الـcheckboxes (`w-4 h-4` → `w-5 h-5`)، دون التطرق لوجود/غياب label. Lighthouse (`label`) يكشف: checkbox "تحديد الكل" بالهيدر ([dashboard/page.tsx:706](src/app/admin/dashboard/page.tsx#L706)) وكل checkbox صف منتج ([dashboard/page.tsx:722](src/app/admin/dashboard/page.tsx#L722)) بدون `aria-label` أو أي نص مرتبط. **397 عنصر فاشل بالضبط** (396 منتج + 1 هيدر — يطابق تمامًا عدد صفوف الجدول الفعلي بقاعدة الإنتاج). **الأولوية: 🟡 متوسطة** — وظيفة "تحديد للحذف/التعديل الجماعي" غير مفهومة لقارئ الشاشة (لا يعرف أي منتج يحدده).

### 🟡 متوسط — تسلسل عناوين مكسور (heading-order) في 4 مواقع مختلفة — يصحّح ملاحظة إيجابية سابقة

design-audit.md ذكر (بند 3.4) أن صفحة تفاصيل المنتج "فيها تسلسل عناوين (h1→h3) سليم" كـ**ملاحظة إيجابية**. **Lighthouse يصحّح هذا**: القفز من h1 مباشرة إلى h3 (بدون h2) هو بحد ذاته مخالفة `heading-order` (المستوى يجب أن يزيد بخطوة واحدة فقط في كل مرة) — [products/[id]/page.tsx](src/app/products/[id]/page.tsx) (h3 "الوصف"، بعد h1 اسم المنتج مباشرة، بدون h2 بينهما).

نفس النمط مكرر بثلاث مواقع أخرى (اكتشاف جديد بالكامل):
- الرئيسية: h4 داخل بطاقة "موثوقية عالية" ([page.tsx:137](src/app/page.tsx#L137)) يأتي مباشرة بعد h2 (يتخطى h3)، وh4 بالفوتر ([Footer.tsx:21](src/components/layout/Footer.tsx#L21)، [:35](src/components/layout/Footer.tsx#L35)) بنفس المشكلة.
- `/products`: h3 اسم كل منتج (`ProductCard.tsx:137`) يأتي مباشرة بعد h1 عنوان الصفحة (بدون h2 بينهما) — نفس مكوّن `ProductCard` يعمل بشكل صحيح على الرئيسية لأنه هناك يأتي بعد h2 "العروض المميزة" ([page.tsx:153](src/app/page.tsx#L153)).
- لوحة الأدمن: h1 "Billy Store" ([dashboard/page.tsx:416](src/app/admin/dashboard/page.tsx#L416)) يليه مباشرة h3 "الخصم العالمي على المنتجات" ([:455](src/app/admin/dashboard/page.tsx#L455))، بدون h2.

**الأولوية: 🟡 متوسطة** — لا يمنع الاستخدام، لكنه يكسر التنقل الهرمي لقارئات الشاشة (اختصار "انتقل للعنوان التالي بنفس المستوى" يصبح غير موثوق).

### 🟡 متوسط — لا يوجد `<main>` landmark على أي صفحة من الصفحات الأربع

تحقّق مباشر عبر `evaluate_script` (`document.querySelectorAll('main').length` و`[role="main"]`) على كل صفحة: **صفر عناصر `<main>` وصفر `role="main"` على الأربع صفحات كلها** (وليس فقط ما أظهره Lighthouse — لاحظنا أن `landmark-one-main` بحد ذاته أعطى نتيجة غير متسقة بين الصفحة الرئيسية (نجح رغم غياب `<main>` فعليًا، تحقّقنا بإعادة تحميل والفحص مرتين) و`/products`/`/products/2011`/`/admin/dashboard` (فشل بشكل متسق) — لذلك اعتمدنا الفحص المباشر بالـDOM كمصدر الحقيقة، وليس سكور Lighthouse وحده). لا يوجد `<main>` بأي مكان في `src/app/` (بحث `Grep` شامل). **الأولوية: 🟡 متوسطة** — landmark أساسي لتنقل قارئ الشاشة ("انتقل للمحتوى الرئيسي") غائب بالكامل عبر الموقع.

---

## 3) SEO

**هذا أول فحص SEO من نوعه للمشروع.** النتائج الأربعة (100/100 لكل صفحة) مضلِّلة — سكور Lighthouse SEO يفحص أساسيات فنية (viewport، lang، status code) ولا يفحص جودة/وجود structured data أو sitemap بعمق كافٍ. الفحص اليدوي المباشر يكشف فجوات كبيرة:

### 🔴 حرج — لا يوجد meta tags ديناميكية لأي صفحة منتج — كل صفحة تحمل نفس العنوان/الوصف

تحقّق مباشر (`evaluate_script` على `/products/2011` — "Alone in The Dark"):
```
title: "Billy Store - متجر ألعاب بلايستيشن"
description: "تسوق أفضل ألعاب PlayStation 4 و5 واشتراكات PS Plus بأسعار لا تُنافس..."
```
**نفس العنوان/الوصف الثابتين تمامًا الموجودين بـ[layout.tsx:9-10](src/app/layout.tsx#L9) للموقع كله.** لا يوجد `generateMetadata` بـ[products/[id]/page.tsx](src/app/products/[id]/page.tsx) (تحقّق `Grep` مباشر: صفر نتائج). يعني: **كل صفحة منتج (مئات الصفحات) تظهر بنفس العنوان بالضبط على نتائج بحث Google** — فرصة ضخمة ضائعة (اسم اللعبة، السعر، لا يظهر بعنوان/وصف نتيجة البحث أو تبويب المتصفح إطلاقًا).

**الأولوية: 🔴 حرجة** — أساسي لأي متجر إلكتروني؛ بدونه محركات البحث لا تستطيع تمييز صفحات المنتجات عن بعضها.

### 🔴 حرج — لا يوجد Structured Data (JSON-LD) لأي صفحة منتج

تحقّق مباشر (`evaluate_script`: `document.querySelectorAll('script[type="application/ld+json"]')` على `/products/2011`) = **مصفوفة فارغة تمامًا**. لا `Product` schema (السعر، التوفر، الصورة، التقييم) بأي مكان بـ`src/app/products/`. هذا يعني الموقع غير مؤهَّل لـ"Rich Results" بگوگل (نجمة السعر، التوفر بنتائج البحث) رغم أن كل البيانات اللازمة (سعر، اسم، صورة، فئة) متوفرة أصلًا بقاعدة البيانات. **الأولوية: 🔴 حرجة** لمتجر إلكتروني تحديدًا — Rich Results تؤثر مباشرة على معدل النقر من نتائج البحث.

### 🟡 متوسط — لا `sitemap.xml` ولا `robots.txt`

تحقّق مباشر: `curl http://localhost:3000/robots.txt` → **404**. `curl http://localhost:3000/sitemap.xml` → **404**. بحث `Glob` شامل بالمشروع (`**/sitemap*`, `**/robots*`) = صفر نتائج — لا ملف ثابت ولا `app/sitemap.ts`/`app/robots.ts` ديناميكي (طريقة Next.js 15 القياسية لتوليدهما). **الأولوية: 🟡 متوسطة** — بدون sitemap، اكتشاف Google لصفحات المنتجات الجديدة يعتمد بالكامل على الزحف العضوي من الروابط الداخلية، أبطأ بكثير من تقديم sitemap مباشرة عبر Search Console.

### 🟡 متوسط — لا Open Graph tags ولا canonical URL

نفس الفحص المباشر على `/products/2011`: `og:title` = `null`، `link[rel="canonical"]` = `null`. لا معاينة مخصصة عند مشاركة رابط منتج على واتساب/تويتر (القناة التسويقية الأساسية للمتجر حسب CLAUDE.md — روابط واتساب مذكورة بـ8+ ملفات). **الأولوية: 🟡 متوسطة** — يؤثر مباشرة على معدل النقر عند مشاركة روابط منتجات عبر واتساب، القناة الرئيسية لهذا المتجر تحديدًا.

---

## 4) Best Practices

**لا مخالفات حقيقية مكتشَفة.** السكور 81 على الصفحات الأربع بالكامل بسبب فحص `is-on-https` الفاشل حتمًا على `http://localhost` (بيئة اختبار محلية) — مؤكَّد بـ`security-audit.md` أن الإنتاج يفرض HTTPS عبر HSTS. تحقّق مباشر إضافي بقراءة تقارير JSON: صفر audits فاشلة أخرى (لا console errors غير متوقعة من كود التطبيق نفسه، لا مكتبات ضعيفة معروفة مكتشَفة عبر هذا الفحص المحدد — علمًا أن `npm audit` مغطّى بتفصيل أدق بـ`security-audit.md` Gotcha #20، خارج نطاق هذا التدقيق). **ملاحظة بيئية غير متعلقة بالتطبيق**: طلب شبكة من إضافة متصفح خارجية (Kaspersky، `gc.kis.v2.scr.kaspersky-labs.com`) ظهر بقائمة الشبكة أثناء الفحص — قطعة أثرية بيئية (إضافة أمان مثبتة بالمتصفح المستخدم للفحص)، لا علاقة له بكود المشروع، ولم يُحتسب ضمن أي نتيجة.

---

## 📊 أثر جانبي معروف — عداد المشاهدات

اختير المنتج **2011 ("Alone in The Dark")** لفحص صفحة تفاصيل المنتج (بصورة حقيقية، بدون خصم فعّال — كل الخصومات كانت `active: false` وقت التدقيق، بقرار صريح من المستخدم بعدم تفعيل خصم مؤقتًا لهذا الغرض).

- **قيمة العداد قبل بدء أي فحص**: `0` (تحقّق مباشر عبر استعلام `product_views` على Turso).
- **قيمة العداد بعد انتهاء كل فحوصات Lighthouse/الأداء**: `4` (تحقّق مباشر، نفس الاستعلام).
- **الفرق**: **+4**، ناتج عن 4 عمليات تصفح فعلية حقيقية لصفحة `/products/2011` أثناء التدقيق (فحص Lighthouse الأول [navigation mode يُعيد تحميل الصفحة]، `performance_start_trace` مع `reload: true`، وزيارتان يدويتان إضافيتان للتحقق من الـmeta tags و`heading-order`).

لا توجد آلية لحذف/تصفير هذا العداد عبر أي API موجود بالمشروع (`/api/views` هو POST فقط لزيادة العداد — لا `DELETE`/`RESET`). **هذا التوثيق هو أقصى ما يمكن عمله** — مقبول لأنه بيانات إحصائية بسيطة غير حساسة (عدد مشاهدات صفحة، وليس بيانات مستخدم أو سعر/خصم قابل للتأثير على معاملة حقيقية). هذا يتماشى أيضًا مع Gotcha #13 الموثَّق مسبقًا (عداد المشاهدات لا يستثني تصفح الأدمن، بعكس عداد الزيارات) — تصفح الأدمن/الاختبار هنا زاد نفس العداد الذي يظهر بإحصائيات لوحة التحكم.

### تحديث — جلسة الإصلاح (فرع `fix/web-quality-quick-wins`، 2026-07-25)

تحقّق التنفيذ (بند 2+3+11: `generateMetadata`/JSON-LD/OG tags) استخدم صفحات منتجات حقيقية إضافية عبر تصفح فعلي بالمتصفح، فزاد عداد كل واحد منها. **توثيق كامل بنفس الشفافية المطبَّقة على 2011 أعلاه:**

| المنتج | الاستخدام | قبل هذي الجلسة | بعد هذي الجلسة (تحقّق مباشر Turso) | الفرق |
|---|---|---|---|---|
| **2011** ("Alone in The Dark") | إعادة فحص `heading-order`/`landmark-one-main` بعد الإصلاح (بند 4/5) — لم يُستخدم لاختبار metadata هذي المرة | `4` (القيمة الختامية من التدقيق الأصلي أعلاه) | `6` | **+2** |
| **2012** ("Until Dawn Remake") | تحقّق بصري لـ`<main>` landmark (بند 5) — لم يُستخدم لاختبار metadata | لم يُفحص قبل هذي الجلسة (أول لمسة له بأي جلسة) | `1` | +1 (منسوب بالكامل لهذي الجلسة، انظر التحفظ أدناه) |
| **2013** ("Farming Simulator 25") | **✅ هذا هو المنتج المستخدم فعليًا لاختبار `generateMetadata`/JSON-LD (بند 2/3/11)** — تحقّق `evaluate_script` أكّد `title`/`description`/`canonical`/`og:*`/JSON-LD كلها تعكس بياناته الحقيقية | لم يُفحص قبل هذي الجلسة (أول لمسة له بأي جلسة) | `1` | +1 (منسوب بالكامل لهذي الجلسة، انظر التحفظ أدناه) |
| **2014** ("SOUTH PARK: SNOW DAY!") | تأكيد إضافي عرَضي أثناء جولة Lighthouse النهائية الشاملة (بند 10 بالخطة) أن العنوان الديناميكي لسا شغّال بعد كل التعديلات — ليس اختبار metadata مخصص | لم يُفحص قبل هذي الجلسة (أول لمسة له بأي جلسة) | `2` | +2 (منسوب بالكامل لهذي الجلسة، انظر التحفظ أدناه) |

**آلية الزيادة المؤكَّدة**: كل استدعاء `navigate_page` لصفحة منتج = مشاهدة واحدة (+1)، وكل استدعاء `lighthouse_audit` (وضع `navigation`، يُعيد تحميل الصفحة داخليًا) = مشاهدة إضافية (+1) — هذا يفسّر بدقة كل الأرقام أعلاه (2011: navigate+lighthouse=+2 هذي الجلسة؛ 2014: navigate+lighthouse=+2؛ 2012 وَ2013: navigate فقط بدون lighthouse=+1 لكل واحد).

**تحفّظ صريح على 2012/2013/2014 (فجوة منهجية مقابل 2011)**: على عكس منتج 2011 بالتدقيق الأصلي (حيث اسْتُعلِمت قيمة العداد **قبل** أي تصفح فعليًا)، هذي الثلاثة لم تُفحص قيمتها قبل أول تصفح لها بأي جلسة سابقة — فالعمود "قبل هذي الجلسة" هنا **مُستنتَج منطقيًا** (بما إنها أول مرة تُلمس بأي جلسة تدقيق/إصلاح، ولا آلية أخرى بالكود ترفع هذا العداد غير تصفح حقيقي لصفحة المنتج) وليس **مُقاسًا مباشرة** بنفس صرامة منهجية 2011. لا نجزم إنها كانت `0` قطعًا قبل هذي الجلسة (ممكن نظريًا زيارة عميل حقيقي عابرة لنفس المنتج بنفس الفترة، رغم بُعد الاحتمال بحجم حركة المتجر الحالية) — هذا التحفّظ نفسه هو التوثيق الشفاف المطلوب، بدل الافتراض الصامت.

---

## ملخص الأولويات

| # | الفئة | الوصف | الأولوية | الصفحة/الموقع | الحالة |
|---|---|---|---|---|---|
| 1 | Performance | `/products` بدون ISR → LCP 3.82s (4.7-7x أبطأ من باقي الموقع) | 🔴 حرجة | `products/page.tsx` | 📄 مؤجَّل لجلسة معمارية منفصلة |
| 2 | SEO | لا meta tags ديناميكية لأي صفحة منتج | 🔴 حرجة | `products/[id]/page.tsx` | ✅ مُصلَح |
| 3 | SEO | لا Structured Data (Product JSON-LD) | 🔴 حرجة | `products/[id]/page.tsx` | ✅ مُصلَح |
| 4 | Accessibility | أزرار أيقونة-فقط بدون اسم accessible (27 عنصر) | 🔴 حرجة | `dashboard/page.tsx:548-558` | ✅ مُصلَح |
| 5 | Performance | شعار/صور أدمن بحجم 24-50x أكبر من اللازم (`<img>` خام) | 🟡 متوسطة | `Navbar.tsx:76`, `Footer.tsx:11`, `dashboard/page.tsx:911` | ✅ مُصلَح |
| 6 | Accessibility | تباين لوني فاشل على شارات الخصم (18 عنصر) | 🟡 متوسطة | `dashboard/page.tsx:520,523` | ✅ مُصلَح |
| 7 | Accessibility | Checkboxes بدون اسم accessible (397 عنصر) | 🟡 متوسطة | `dashboard/page.tsx:706,722` | ✅ مُصلَح |
| 8 | Accessibility | heading-order مكسور بـ4 مواقع | 🟡 متوسطة | الرئيسية، `/products`، تفاصيل المنتج، الأدمن | ✅ مُصلَح |
| 9 | Accessibility | لا `<main>` landmark بأي صفحة | 🟡 متوسطة | كل الصفحات | ✅ مُصلَح |
| 10 | SEO | لا sitemap.xml/robots.txt | 🟡 متوسطة | جذر المشروع | ✅ مُصلَح |
| 11 | SEO | لا Open Graph/canonical | 🟡 متوسطة | `products/[id]/page.tsx` | ✅ مُصلَح |
| 12 | Performance | DOM ضخم بلوحة الأدمن (8,661 عنصر، بدون pagination) | 🟡 متوسطة | `dashboard/page.tsx` (جدول المنتجات) | 📄 مؤجَّل لجلسة معمارية منفصلة |
| 13 | Accessibility | 4 مفاتيح تبديل (toggle) بدون اسم accessible — اكتُشف لاحقًا أثناء QA حي لفرع `fix/web-quality-quick-wins`، ليس بالتدقيق الأصلي | 🟡 متوسطة | `dashboard/page.tsx:832-837, 1165-1170, 1220-1225, 1321-1326` | ✅ مُصلَح (فرع `fix/toggle-accessible-names`) |

**11 من 13 بند (12 الأصلية + البند 13 المكتشَف لاحقًا) انحلّت بكود فعلي** — تفاصيل كل بند وأرقام Lighthouse قبل/بعد بقسم "⚠️ حالة المعالجة" أعلى الملف. البنود 2-11 دُمجت بـ`main` عبر فرع `fix/web-quality-quick-wins` (مدموج ومرفوع فعليًا). البند 13 على فرع منفصل `fix/toggle-accessible-names` (من `main`) — بانتظار مراجعة المستخدم، **لا merge ولا push بعد**. البندان 1 و12 لسا مؤجَّلان لجلسة معمارية منفصلة.
