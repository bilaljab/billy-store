# CLAUDE.md

هذا الملف يوثّق بنية مشروع Billy Store لمساعدة أي جلسة Claude Code مستقبلية على فهم المشروع بسرعة دون الحاجة لإعادة الاستكشاف.

## نظرة عامة

متجر إلكتروني (storefront) لألعاب PS4/PS5 واشتراكات PS Plus، موجّه للسوق السعودي. واجهة عربية RTL بالكامل، مع لوحة تحكم إدارية (admin dashboard) لإدارة المنتجات والخصومات والإعلانات.

## Tech Stack

| الطبقة | التقنية |
|---|---|
| Framework | Next.js 15 (App Router) + React 19 |
| اللغة | TypeScript (strict mode) |
| قاعدة البيانات | SQLite عبر Turso (`@libsql/client`) — محلياً ملف `data/billy.db`، وفي الإنتاج عبر Turso cloud (`TURSO_URL` / `TURSO_AUTH_TOKEN`) |
| Auth | JWT مخصص (`jose`) في cookie httpOnly باسم `admin_token` + `bcryptjs` لتشفير كلمة مرور الأدمن. لا يوجد نظام مستخدمين عاديين — أدمن واحد فقط (seed تلقائي من `ADMIN_PASSWORD`) |
| Styling | TailwindCSS 3 (`tailwind.config.js`) + CSS مخصص في `globals.css` (خط Cairo، ألوان PlayStation، أنيميشنز) |
| رفع الصور | Cloudinary (unsigned upload preset) — وليس تخزين محلي |
| State management | لا يوجد state management library — كله `useState`/`useEffect` محلي داخل الصفحات (`'use client'`) |
| Linting | ESLint (`eslint.config.mjs`، flat config) + `eslint-config-next` |
| Deployment | Vercel (موصى به في DEPLOYMENT.md)، مع Turso كبديل serverless-friendly عن SQLite المحلي |

## بنية المجلدات

```
src/
  app/                    # Next.js App Router
    page.tsx              # الصفحة الرئيسية
    products/              # صفحة المنتجات + [id] لصفحة تفاصيل المنتج
    about/, faq/            # صفحات ثابتة
    admin/
      login/               # صفحة تسجيل دخول الأدمن
      dashboard/           # لوحة التحكم (ملف ضخم ~1270 سطر، كل الإدارة فيه)
    api/                   # Route handlers (route.ts)
      products/, products/[id]/
      discounts/            # GET عام: {global, targeted} معاً
      admin/
        products/, announcement/, export/, import/, stats/
        discounts/
          global/           # CRUD للخصم العام الوحيد (كان اسمها /api/admin/discount)
          targeted/          # CRUD لقواعد الخصومات المستهدفة (لكل منتج/نطاق سعر)
      auth/login/, auth/logout/
      upload/               # رفع صور عبر Cloudinary
      views/, visit/         # تتبع المشاهدات والزيارات
    globals.css
  middleware.ts             # حماية /admin/dashboard + جزء من أمان الهيدرز (CSP) — الجزء الثاني (HSTS) بـ next.config.js، انظر Gotcha #16
  components/
    layout/                 # Navbar, Footer
    ui/                     # مكونات عامة (ProductCard, FaqAccordion, ScrollReveal, ConfirmDialog...)
    admin/                  # مكونات خاصة بلوحة التحكم
  lib/
    db.ts                   # اتصال قاعدة البيانات + initDb() + serialization
    auth.ts                 # JWT sign/verify + auth helpers
    xlsxParser.ts            # بارسر لملفات Excel/CSV للاستيراد الجماعي
data/billy.db               # قاعدة بيانات SQLite المحلية (gitignored)
scripts/init-db.js          # سكربت تهيئة قاعدة بيانات مستقل
```

### Conventions ملحوظة
- كل API route هو `route.ts` بأسلوب Next.js App Router (`export async function GET/POST/...`).
- التحقق من صلاحية الأدمن في الـ API routes يتم عبر `isAuthenticated(req)` من `lib/auth.ts`، وعلى مستوى الصفحات عبر `middleware.ts`.
- كل استعلامات SQL تستخدم parameterized queries (`db.execute({ sql, args })`) — لا تكتب string interpolation مباشر في SQL.
- الألوان والخطوط معرّفة مركزياً في `tailwind.config.js` (لون أساسي `#0070CC`، خلفية داكنة `#050A14`، خط Cairo، animations).
- الصفحات تستخدم `'use client'` بكثرة مع state محلي — لا يوجد server components pattern منظم أو data-fetching library.
- تسمية مسارات الخصومات: `/api/admin/discounts/global` (الخصم العام، واحد بس) مقابل `/api/admin/discounts/targeted` (قواعد متعددة لكل منتج/نطاق سعر). المسار العام `/api/discounts` بيرجّع الاثنين مع بعض بـ response واحد.
- التحقق من صحة قواعد الخصم المستهدف (clamp النسبة 1-90، `type`، ترتيب `minPrice`/`maxPrice`) مركزي بدالة `validateRuleInput()` داخل `targeted/route.ts`، تُستخدم من `POST` و`PUT` معًا — أي حقل أو قاعدة تحقق جديدة تُضاف هناك مرة وحدة، ما تتكرر بكل handler على حدة.
- حذف عناصر حسّاسة (منتج، خصم عام، قاعدة مستهدفة) يمر عبر `ConfirmDialog` مشترك (`src/components/ui/ConfirmDialog.tsx`) — لا تكتب مودال تأكيد حذف جديد مضمّن، استهلك هذا المكوّن.

## أوامر التشغيل

```bash
npm install       # تثبيت الحزم
npm run dev       # تشغيل بيئة التطوير (localhost:3000)
npm run build     # بناء الإنتاج
npm run start     # تشغيل نسخة الإنتاج بعد البناء
npm run lint      # next lint (ESLint)
npx tsc --noEmit  # فحص الأنواع فقط — لا يوجد سكربت npm مخصص لهذا بـ package.json
```

**لا يوجد test suite** — لا سكربت `test` في package.json ولا ملفات `*.test.*` / `*.spec.*` في المشروع. أي تحقق من صحة الكود حالياً يعتمد على `lint` والاختبار اليدوي فقط. قرار مقصود (تم تأجيله) — إضافة إطار اختبار (Jest/Vitest/Playwright) تستاهل نقاش منفصل، مش جزء من تنظيف سريع.

## متغيرات البيئة

موجودة في `.env` و`.env.local` محلياً (مستثناة من git عبر `.gitignore`). **`.env.example`** بجذر المشروع بيوثّق كل المتغيرات المطلوبة بدون قيم حقيقية — انسخه لـ `.env.local` وعبّي القيم عند إعداد بيئة جديدة.

| المتغير | مطلوب | الوصف |
|---|---|---|
| `JWT_SECRET` | ✅ دائماً | لازم 32+ حرف، وإلا `lib/auth.ts` يرمي خطأ عند الإقلاع |
| `ADMIN_PASSWORD` | ✅ دائماً | يُستخدم لعمل seed لحساب الأدمن الأول فقط عند عدم وجوده بقاعدة البيانات |
| `TURSO_URL` / `TURSO_AUTH_TOKEN` | اختياري محلياً، مطلوب بالإنتاج (Vercel) | لو فاضية بيستخدم SQLite محلي في `data/billy.db` تلقائياً |
| `CLOUDINARY_CLOUD_NAME` / `CLOUDINARY_UPLOAD_PRESET` | ✅ لميزة رفع الصور | بدونها `/api/upload` يرجع 500 |

## ملاحظات وأشياء تحتاج انتباه (Gotchas)

1. **أسرار حقيقية داخل `.env`/`.env.local` المحليين** (JWT secret فعلي، كلمة سر أدمن فعلية، Turso auth token فعلي). مش مشكلة أمنية بحد ذاتها لأنهم مستثنون من git، بس انتبه ما تطبعهم أو تشاركهم بأي output خارجي.
2. **`data/billy.db`** هي قاعدة بيانات SQLite حقيقية فيها بيانات — موجودة محلياً وليست فارغة. لا تحذفها أو تعيد تهيئتها بدون التأكد إذا فيها بيانات مهمة (SECURITY.md بيذكر حذفها فقط عند تغيير `ADMIN_PASSWORD` بالإنتاج عشان تعيد الـ seed).
3. **`eslint.config.mjs`**: قاعدة `@typescript-eslint/no-require-imports` مخفّضة لـ `warn` (بدل `error` الافتراضي)، بسبب workaround مقصود بـ `src/lib/db.ts` (`require('fs')`/`require('path')` ديناميكي لمنع الـ bundler من تضمينهم بالإنتاج) — لسا قائم، تأجيل مقصود. (قاعدة `@typescript-eslint/no-explicit-any` كانت مخفّضة لنفس السبب لحد `2026-07-24`: انحلّت بالكامل — 12 مخالفة صريحة عبر 4 ملفات صارت صفر، على فرع `chore/type-safety-and-audit` — عبر تعريف `interface TargetedRule` بـ `dashboard/page.tsx` وحذف 7 casts زائدة كان النوع الصحيح واضح فيها أصلاً من `serializeProduct()`/الـ interfaces الموجودة. القاعدة نفسها بـ `eslint.config.mjs` لسا مسجّلة `warn` — ما انرجّعت لـ `error` تلقائياً، قرار منفصل بسيط لو حدا حاب يعمله.)
4. **`no-img-element`** (تحذير ESLint) موجود بـ 3 أماكن (`dashboard/page.tsx`, `Footer.tsx`, `Navbar.tsx`) — استخدام `<img>` عادي بدل `next/image`. مش مصلّح، خارج نطاق التنظيف الحالي.
5. **`npm audit`** (فرع `chore/type-safety-and-audit`): كان فيه 5 ثغرات high-severity (`next`, `postcss`, `sharp`, `picomatch`, `ws`). **3 منهم انحلّوا** عبر `npm audit fix` العادي (بدون `--force`) بدون أي breaking changes: `ws` (8.19.0→8.21.1)، `picomatch` (→2.3.2)، وnسخة `postcss` الجذرية (8.5.8→8.5.22) — والتحديث الجانبي رفع `next` نفسها لـ `15.5.21` (لسا داخل `^15.2.4` المعرّف بـ `package.json`). **ثغرتين لسا موجودتين وما ينحلّوا بدون قرار منفصل**: `sharp` (next@15.5.21 — آخر إصدار متاح بخط 15.x بالكامل — بيثبّت `sharp: "^0.34.3"` بداخله، تحت نسخة الإصلاح 0.35.0) و`postcss` (نسخة **مدمجة داخلياً** جوه `node_modules/next/node_modules/postcss@8.4.31`، منفصلة عن النسخة الجذرية المصلَّحة). كلا الثغرتين متجذّرتين بـ Next نفسه على آخر نسخة بخط 15.x — حلّهم يتطلب ترقية major لخط 16.x، قرار منفصل يحتاج تقييم breaking changes (خصوصاً على تكامل App Router/React 19)، لسا ما اتخذ. اقتراح `npm audit fix --force` (تنزيل Next لـ 9.3.3) مو خيار حقيقي إطلاقاً — بيكسر التطبيق بالكامل.
6. لوحظ إنه Next.js المثبت فعلياً كان `15.5.12` (وصار `15.5.21` بعد إصلاح الأودت أعلاه) رغم إنه `package.json` بيحدد `"next": "^15.2.4"` — طبيعي (semver range)، بس خليها ببالك إذا حبيت تثبّت نسخة محددة.
7. **باگ كان موجود أصلاً بلوحة الأدمن (اتصلّح على فرع `design/ui-ux-audit-fixes`)**: شريط أدوات "إدارة المنتجات" ([dashboard/page.tsx](src/app/admin/dashboard/page.tsx)، قسم جدول المنتجات) كان صف أزرار (تصدير CSV/الإحصائيات/تعديل الأسعار/استيراد Excel-CSV/**+ إضافة منتج**) بـ`flex` عادي بدون `flex-wrap` ولا `overflow-x-auto`، وبما إنه `body { overflow-x: hidden }` معرّف عالمياً بـ`globals.css`، كانت الأزرار الزايدة عن عرض الشاشة تُقطع تماماً خارج حدود الرؤية على الموبايل (~<556px) بدون أي طريقة توصلها — بما فيها زر "+ إضافة منتج" (الإجراء الرئيسي بالصفحة). الباگ هذا **مش من مخالفات تدقيق UI/UX** (`design-audit.md`) ولا نتج عن أي تعديل بهاي الجلسة — اكتُشف بالصدفة أثناء فحص بصري فعلي (Chrome DevTools MCP) لبند 5.5 من التدقيق (أعمدة الجدول بعرض الموبايل)، وتم إصلاحه فوراً لأنه يمنع وصول كامل لإجراء أساسي. الحل: الهيدر صار `flex-col sm:flex-row` (العنوان فوق، الأزرار تحت على الموبايل) وصف الأزرار صار `flex-wrap`.
8. **مدة كاش `/api/discounts`** ([src/app/api/discounts/route.ts](src/app/api/discounts/route.ts)): `Cache-Control: public, s-maxage=30, stale-while-revalidate=120` — يعني تغييرات الأدمن على الخصم العام أو القواعد المستهدفة ممكن تتأخر بالظهور للزوار حتى ~150 ثانية (30 + 120). **قرار مقصود** (راجعه المستخدم صراحة بجلسة `fix/discount-system-issues` وأبقاه كما هو) لتخفيف الحمل على Turso (كل صفحة منتج/قائمة بتقرأ هالمسار) مقابل تأخير مقبول لحجم المتجر الحالي (عدد منتجات صغير، تغييرات خصم غير متكررة كل ثانية). لو احتجت مستقبلاً استجابة أسرع (مثلاً عروض ومضة قصيرة المدة تحتاج تظهر فوراً)، هذا قرار يُعاد تقييمه وقتها بناءً على حاجة فعلية — مش تغيير استباقي.
9. **تدقيق وإصلاح شامل لأنظمة الخصومات (2026-07-25، فرع `fix/discount-system-issues`، مدموج بـ`main`)**: تقرير كامل بـ[discount-audit.md](discount-audit.md) بجذر المشروع يوثّق 16 مشكلة كانت موجودة فعليًا (بعضها بتأثير مباشر على السعر المعروض للزبون، بعضها ثغرة أمان بتسريب بيانات خصم عبر `GET` غير محمي، وبعضها تجربة إدارية فقط) — **14 منها انحلّت بالكامل** (clamp/رفض صريح لنسب الخصم بكلا النظامين، مصادقة على كل `GET`، معالجة أخطاء منظمة، توحيد تحقق قواعد الخصم المستهدف بدالة مشتركة، مكوّن `ConfirmDialog` مشترك). **بندان لسا مؤجّلان بقرار صريح ولازم أي جلسة مستقبلية تعرفهم قبل ما تلمس كود الخصومات**:
   - لا يوجد مفهوم تاريخ بداية/نهاية (expiry/scheduling) لأي خصم — ميزة جديدة، مش عيب، لسا ما اتقرر تنفيذها.
   - منطق أولوية الخصم العام مقابل المستهدف (اختيار أعلى نسبة مستهدفة تنطبق، والخصم العام fallback بس لو ما فيه قاعدة مستهدفة منطبقة) **مكرَّر يدويًا حرفيًا بثلاث ملفات منفصلة بدون دالة مشتركة**: [src/components/ui/ProductCard.tsx](src/components/ui/ProductCard.tsx)، [src/app/products/page.tsx](src/app/products/page.tsx)، و[src/app/products/[id]/page.tsx](src/app/products/[id]/page.tsx). **أي تعديل مستقبلي على قاعدة الأولوية (مثلاً السماح بالتراكم بدل "الأعلى يفوز") لازم يصير بالثلاث ملفات معًا** — تعديل ملف واحد بس بينتج فورًا سعر مختلف لنفس المنتج حسب الصفحة (الرئيسية مقابل القائمة مقابل التفاصيل). **تحديث (2026-07-26، فرع `feature/products-isr`)**: آلية `products/page.tsx` تحديدًا تغيّرت من HTTP fetch لـ`/api/discounts` (client-side) إلى استعلام DB مباشر داخل Server Component (`getProductsWithDiscounts()`، بنفس نمط `getPageData()` بـ`products/[id]/page.tsx`) — العدد الإجمالي للملفات المكرِّرة لسا 3 بدون تغيير، فقط آلية هذا الملف الواحد تبدّلت.
10. **`PUT /api/admin/products/[id]` بدون أي تحقق من صحة المدخلات** ([src/app/api/admin/products/[id]/route.ts](src/app/api/admin/products/[id]/route.ts))، بعكس `POST` بنفس المسار (`src/app/api/admin/products/route.ts`) اللي بيتحقق بصرامة من الاسم والسعر. تعديل منتج موجود ممكن يكتب `NaN`/سعر سالب أو صفر/اسم فارغ بصمت بقاعدة البيانات. لو طُلب "أضف تحقق للمنتجات"، لازم PUT يتغطى، مش بس POST.
11. **Rate limiting بتسجيل الدخول (`src/app/api/auth/login/route.ts`) بذاكرة (`Map`) داخل العملية، مش مشتركة بين instances** — بترجع لصفر عند كل redeploy أو cold start، وعلى بيئة serverless متعددة instances (Vercel) الحد الفعلي "5 محاولات/15 دقيقة" أضعف بكثير مما يبدو. لا يوجد rate limiting بأي route ثاني بالمشروع. **حسم صريح (تدقيق أمني 2026-07-25، `security-audit.md` §2.2)**: مفتاح الحد يعتمد على `x-forwarded-for` (`login/route.ts:25`) بدون أي تحقق من مصدره بالكود نفسه — لكن توثيق Vercel الرسمي (`vercel.com/docs/headers/request-headers`) يؤكد صراحة إن المنصة **تعيد كتابة** هالهيدر وترفض أي قيمة يرسلها العميل مباشرة ("to prevent IP spoofing"). يعني التصنيف الصحيح هو **🟡 ضعف تحصين (multi-instance/cold-start يصفّر الحد)، وليس 🔴 تجاوز كامل عبر تزوير الهيدر** — طالما التطبيق يُستقبل حصرًا خلف Vercel edge كما هو مخطَّط بالإنتاج (Gotcha #17). تحقّق عملي محلي (dev server بدون طبقة Vercel) أثبت إن الكود المجرّد فعلاً يثق بالهيدر بالكامل لو وصلته الطلبات مباشرة — **هذا الحسم يُعاد تقييمه فورًا لو تغيّرت بنية الاستضافة مستقبلًا** (self-hosted، أو proxy آخر لا يعيد كتابة الهيدر).
12. **"تعديل الأسعار بالجملة" (`bulk-price`) بدون حد أقصى منطقي على نسبة الزيادة/النقصان** (`src/app/api/admin/products/bulk-price/route.ts`) — أي نسبة من 0 لـ99999 مقبولة لكل من percentage وfixed، والنتيجة تُقرَّب لأقرب عدد صحيح بحد أدنى 1 بصمت. أي أسعار عشرية موجودة تنمسح لما يُستخدم هالأداة.
13. **عدّاد "المشاهدات" (`/api/views`) ما بيستثني تصفح لوحة الأدمن، بعكس عدّاد "الزيارات" (`/api/visit`)** اللي بيستثني صراحة أي referer فيه `/admin` وبيعمل dedup لكل IP كل 30 دقيقة. تصفح الأدمن لصفحات منتج بينفخ رقم المشاهدات بالإحصائيات بدون ما يأثر على رقم الزيارات — الرقمين مو مقارَنين بنفس المعيار.
14. **`src/lib/xlsxParser.ts` بارسر ZIP/XML مكتوب يدويًا بالكامل** (بدون مكتبة `xlsx`/sheetjs)، يقرأ أول sheet بس، وبيحدّ أي entry داخل الملف بـ2 ميجابايت بصمت. ملف تالف أو أكبر من الحد بيرجع "0 استيراد" بدون رسالة خطأ توضيحية — قبل اقتراح "رجّع مكتبة xlsx حقيقية"، اسأل ليش كانت مكتوبة يدوي أول مرة (على الأغلب لتفادي حجم bundle أو dependency ثقيلة).
15. **`scripts/init-db.js` كود قديم متروك (orphaned)، مش طريقة تهيئة بديلة صالحة** — يحتاج حزمة `better-sqlite3` غير موجودة إطلاقًا بـ`package.json` (تشغيله برمي `Cannot find module`)، وبيعمل seed لكلمة سر أدمن مكتوبة صراحة بالكود (`'Bilal2026*'`) تختلف كليًا عن مسار الـseed الفعلي المستخدم حاليًا (متغيّر `ADMIN_PASSWORD` عبر `lib/db.ts`). لا تشغّله.
16. **أمان الهيدرز مُعرَّف بملفين بدون مصدر واحد، بتكرار جزئي**: خمس هيدرز (`X-Content-Type-Options`, `X-Frame-Options`, `X-XSS-Protection`, `Referrer-Policy`, `Permissions-Policy`) مكرَّرة حرفيًا بكل من `next.config.js` و`middleware.ts` — لكن `Strict-Transport-Security` (HSTS) موجود بـ`next.config.js` بس، و`Content-Security-Policy` موجود بـ`middleware.ts` بس. أي تعديل سياسة هيدرز لازم يلمس الملفين وإلا بيصير تناقض. **كمان**: كود إعادة توجيه favicon جوا `middleware.ts` (السطرين 8-9) ميت فعليًا — تأكّدت بقراءة الـ`matcher` مباشرة: بيستثني كل مسار بامتداد `.ico` (وصور تانية) من استدعاء دالة الـmiddleware أصلًا، فالكود موجود لكن ما بينفّذ أبدًا لطلب `/favicon.ico` حقيقي.
    **تحديثات تدقيق أمني 2026-07-25 (`security-audit.md`، فرع `fix/security-audit-issues`)**:
    - **`unsafe-eval` — انحلّت.** كان مفعّل بدون شرط بـ`script-src` رغم تعليق الكود "tighten in prod". فحص عملي (بناء إنتاجي فعلي + `grep` على `.next/static/chunks/*.js`) أكّد صفر استدعاءات `eval()` بحزمة الإنتاج، فصار `script-src` يبني `'unsafe-eval'` شرطيًا حسب `NODE_ENV` (موجود بـdev فقط). تحقّق: `curl -I` بوضع `npm start` الإنتاجي يرجّع CSP بدون `'unsafe-eval'`، وجولة حية كاملة بالمتصفح (الصفحات العامة + تسجيل دخول أدمن + فتح/حفظ/حذف خصم عبر `ConfirmDialog`) بدون أي رسالة `Refused to evaluate` أو CSP violation بالـconsole.
    - **`unsafe-inline`** (script-src وstyle-src): **باقٍ — قرار مقبول موثّق، مش نسيان.** مطلوب وظيفيًا حاليًا: لا يوجد nonce بالـCSP، وسكربت bootstrap الخاص بـNext.js نفسه (مو كود التطبيق) يحتاج `unsafe-inline` بـscript-src ليشتغل؛ React `style` prop (مستخدم بعدة ملفات: `page.tsx`, `Navbar.tsx`, `ScrollReveal.tsx`, `dashboard/page.tsx`, `products/[id]/page.tsx`) يحتاج `unsafe-inline` بـstyle-src. لا مصدر خطر مؤكد حاليًا (بحث شامل: صفر `dangerouslySetInnerHTML`/`eval`/`new Function` بكل `src/`). **يُعاد تقييمه فورًا لو ظهرت مستقبلًا أي نقطة حقن HTML** (مثلًا حقل نصي من أدمن يُعرض بدون تنقية لأي زائر) — عندها الـCSP الحالي لن يوفر أي حماية إضافية ضدها.
    - **تكرار الـ5 هيدرز المشتركة بين الملفين: توحيد مؤجَّل بقرار صريح.** لا خلل عملي حاليًا (تحقّق فعلي بـcurl: كل الثمانية توجيهات/هيدرز تصل صحيحة بنفس الطلب، Next.js يدمج هيدرز الملفين تلقائيًا). خطر تراجع/كسر أي محاولة توحيد (مثلًا استخراج ملف مصدر واحد يُستورد بكليهما) أكبر من الفائدة الحالية بحجم المشروع — قرار مقصود، مش تقصير.
17. **`DEPLOYMENT.md` غير محدَّث ويناقض الستاك الفعلي المستخدم** — بيقول SQLite ما بيشتغل على Vercel ويقترح Neon/PlanetScale/Supabase، بدون أي ذكر لـTurso (المستخدم فعليًا بالإنتاج حسب هذا الملف)، وبيسرد `JWT_SECRET` بس كمتغير بيئة مطلوب (ناقص `ADMIN_PASSWORD` وTurso وCloudinary). **لا تتبعه حرفيًا** — جدول "متغيرات البيئة" أعلى بهذا الملف هو المرجع الصحيح والحالي.
18. **تناقض ISR/تحديث السعر بين صفحات شبه متطابقة** — **جزئيًا محلول (2026-07-26، فرع `feature/products-isr`)**: كان الوصف الأصلي إن الرئيسية `revalidate=60`، تفاصيل المنتج `revalidate=30`، لكن `/products` صفحة `'use client'` بتجيب البيانات فريش بكل mount بدون ISR إطلاقًا. **الآن**: `/products` صارت Server Component بـ`revalidate=30` (تطابق تفاصيل المنتج) — التناقض الأصلي (صفحة بدون ISR إطلاقًا مقابل صفحتين فيهم) انحل. يبقى فرق طفيف مقصود بين 30 (تفاصيل/قائمة) و60 (الرئيسية) — غير موحَّد عمدًا، تفاصيله بخطة `feature/products-isr` (نقطة 5 من التحليل). **ما زال قائمًا بدون حل**: `ProductCard.tsx` عنده كاش خصومات على مستوى الموديول ([src/components/ui/ProductCard.tsx](src/components/ui/ProductCard.tsx)) بينحمّل مرة وحدة بس لكل جلسة متصفح وما بينعاد تحميله إلا بـfull reload — بعد ما الأدمن يغيّر خصم، بطاقات منتجات معروضة بنفس جلسة SPA ممكن تفضل تعرض السعر/الشارة القديمة. هذا الجزء **خارج نطاق** تحويل `/products` لـISR (يخص `ProductCard` نفسه، المستخدم بثلاث أماكن ولم يُلمَس).
19. **روابط تواصل (واتساب/انستقرام/تيليجرام) مكتوبة مباشرة (hardcoded) بأكتر من 8 ملفات منفصلة بدون مصدر واحد مشترك** (`page.tsx`, `about/page.tsx`, `faq/page.tsx`, `Footer.tsx`, `Navbar.tsx`, `ProductActions.tsx`, `WhatsAppFloat.tsx`) — تغيير رقم الواتساب أو أي حساب يحتاج بحث واستبدال يدوي بكل هالملفات، ما فيه constant مشترك يُعدَّل بمكان واحد.
20. **تدقيق أمني شامل (2026-07-25، فرع `audit/security` → إصلاحات على `fix/security-audit-issues`)**: تقرير كامل بـ[security-audit.md](security-audit.md) بجذر المشروع يغطي هيدرز/CSP (Gotcha #16)، rate limiting (Gotcha #11)، مسح شامل لكل API routes بحثًا عن أنماط مشابهة لثغرة الخصومات (`discount-audit.md`)، وCSRF/تسريب env/SQL injection/نسخة Next.js الفعلية. **بندان انحلّا بكود فعلي**: `GET /api/admin/announcement` كان بدون `isAuthenticated` (بعكس كل شقيق آخر بـ`admin/**`) — أُضيف التحقق؛ و`unsafe-eval` بالـCSP صار شرطي بـ`NODE_ENV` (تفاصيل بـGotcha #16 أعلاه). باقي الملاحظات (rate limiting، `unsafe-inline`، تكرار الهيدرز) موثّقة كقرارات مقبولة ضمن Gotcha #11 وGotcha #16 أعلاه، مش عيوب تحتاج إصلاح فوري. **ثغرة `npm audit` إضافية مكتشفة (جديدة، غير موثّقة بـGotcha #5)**: 9 حزم بسلسلة devDependencies (`eslint`, `eslint-config-next`, `eslint-plugin-import`, `eslint-plugin-jsx-a11y`, `eslint-plugin-react`, `@eslint/config-array`, `@eslint/eslintrc`, `minimatch`, `brace-expansion`) — جذرها ثغرة `brace-expansion` واحدة (`GHSA-mh99-v99m-4gvg`، DoS). صفر أثر إنتاجي (devDependencies فقط، لا تُشحن). الإصلاح يتطلب ترقية `eslint` لنسخة major (`10.8.0`) — **قرار منفصل مؤجَّل، بنفس نمط قرار ترقية Next 16.x الموثّق بـGotcha #5** (اللي لسا المرجع الصحيح لثغرتي `sharp`/`postcss` المتجذّرتين بـ`next` نفسه — هالجلسة ما لمستهم). **CSRF (بند 6 بـ`security-audit.md`)**: تحليل إضافة فحص `Origin`/`Referer` كدفاع بعمق ثانٍ لطلبات `admin/**` المتغيّرة للحالة عُرض على المستخدم وقرر **عدم التنفيذ حاليًا** — `sameSite: 'strict'` بكوكي `admin_token` كافية لحجم الاستخدام الفعلي (أدمن واحد)، ومخاطرة false positives (preview URLs بنطاقات مختلفة، متصفحات تحذف Referer) تفوق الفائدة الحالية. **يُعاد تقييمه فقط لو ظهر سبب فعلي** (مثلًا: عدة حسابات أدمن، أو دليل استغلال CSRF حقيقي) — قرار مقصود، مش إهمال.
21. **✅ Pagination لجدول إدارة المنتجات بلوحة الأدمن (2026-07-26، فرع `feature/admin-table-pagination` من `main`) — مُصلَح**: بند 12 بـ[web-quality-audit.md](web-quality-audit.md) كان موثّقًا كـ"مؤجَّل لجلسة معمارية منفصلة" (DOM ضخم: 8,661 عنصر إجمالي، 396 ابن مباشر بـ`<tbody>` جدول المنتجات، أحداث layout بتكلفة 358ms/346ms). الحل المُنفَّذ: **client-side pagination + بحث/فلترة محليان بالكامل** ([dashboard/page.tsx](src/app/admin/dashboard/page.tsx)) — بدون أي تعديل backend/`db.ts`/مكتبة جديدة. الجلب يبقى fetch واحد غير محدود لكل المنتجات (كما كان)، لكن الرندر بس يعمل `.slice()` بحجم صفحة 20 (`PRODUCTS_PAGE_SIZE`) فوق مصفوفة مفلترة بالبحث/الفئة بالذاكرة. **تحقّق فعلي حي (Chrome DevTools MCP، بيانات Turso حقيقية 396 منتج، بناء إنتاجي فعلي)**: إجمالي DOM انخفض من 8,661 لـ**793** (‑90.8%)، أبناء `<tbody>` من 396 لـ**20** بالضبط، أكبر حدث layout انخفض من 358ms إلى **110ms** (يؤثر على 161 عقدة بدل حتى 9,561). "تحديد الكل" صار يعني "كل النتائج المطابقة للفلتر الحالي عبر كل الصفحات" (وليس فقط الصفحة الظاهرة) — قرار صريح من المستخدم، مع رسالة توضيحية بشريط الإجراءات الجماعية ("محدد عبر N صفحات") وتصفير تلقائي للتحديد عند تغيير البحث/الفلتر (لمنع حذف عناصر غير ظاهرة بالغلط). حذف جماعي عبر عدة صفحات يُرجع صراحة لصفحة 1 (`setPage(1)` صريح بـ`handleDeleteSelected`، وليس اعتمادًا فقط على تثبيت حدود الصفحة العام — تحقّق مباشر أثبت الفرق: حذف عنصرين فقط من أصل 45 نتيجة مفلترة، حيث بقي عدد الصفحات كما هو (3) فما كان تثبيت الحدود ليتدخّل أصلًا، ومع ذلك رجعت الصفحة لـ1 بفضل الاستدعاء الصريح). بطاقات الإحصائيات ومنتقي المنتجات بمودال الخصم المستهدف ([:1130-1154](src/app/admin/dashboard/page.tsx#L1130)) **بدون أي تعديل** — يبقيان يقرآن مصفوفة `products` الكاملة كما هي (صحيح للأول لأنه لازم يعكس الكتالوج كامل؛ الثاني مودال مغلق افتراضيًا فلا يساهم بمشكلة الـDOM المقاسة أصلًا). اختبار وظيفي شامل عبر منتجات تجريبية مؤقتة (استُوردت عبر `/api/admin/import` ثم حُذفت بالكامل عبر نفس ميزة الحذف الجماعي المُختبرة، صفر أثر على الكتالوج الحقيقي 396 منتج).
22. **تدقيق CRO لتدفق الطلب عبر ثلاث قنوات + إصلاحات جزئية (2026-07-27، فرع `fix/cro-instagram-priority` من `main`)**: تقرير كامل بـ[cro-audit.md](cro-audit.md) بجذر المشروع يقيّم تدفق الطلب عبر واتساب/تلغرام/إنستقرام، بعد تصحيح صريح من المستخدم إن **إنستقرام (وليس واتساب) هي القناة التسويقية الأساسية** رغم إن واتساب كان الأسهل تقنيًا (`wa.me/...?text=`). التدقيق وثّق 21 ملاحظة؛ **6 انحلّت بكود فعلي على هذا الفرع** وبند واحد أُعيد تصنيفه من "فجوة" إلى "قرار مقصود" — التفاصيل الكاملة بقسم "⚠️ حالة الإصلاح" أعلى `cro-audit.md`. أهم نقطتين لأي جلسة مستقبلية تلمس كود التواصل/الطلب:
    - **الزر العائم (`WhatsAppFloat.tsx`) يبقى واتساب فقط — قرار نهائي مقصود، غير مرتبط بأولوية إنستقرام التسويقية**. بالمثل، ترتيب DOM ووزن زر واتساب البصري بـ[ProductActions.tsx](src/components/ui/ProductActions.tsx) (أول عنصر، `flex-1`، أخضر صلب، أكبر حجم) **بدون أي تغيير بقرار صريح من المستخدم** — واتساب يبقى القناة الأساسية بنيويًا/بصريًا حتى مع كون إنستقرام الأولوية التسويقية. التخفيف الفعلي تم عبر تحسين نص وتقنية القناتين الأخريين بدل تغيير موضع/تصميم واتساب: تلغرام/إنستقرام صار لهما فعل أمر صريح بدل اسم القناة المجرد ("تواصل عبر تيليجرام" / "راسلنا عبر إنستقرام")، ورابط إنستقرام تحوّل من بروفايل عادي (`instagram.com/username`) لصيغة `ig.me/m/username` (خيار Meta الرسمي لفتح DM). **تحقّق فعلي مباشر (Chrome DevTools MCP، بدون جلسة إنستقرام مسجَّلة)**: `ig.me/m/` يعيد التوجيه لصفحة تسجيل دخول إنستقرام بمعامل `next=https://www.instagram.com/m/username` — زائر مسجّل دخوله فعلاً بالمتصفح يصل مباشرة لواجهة إرسال رسالة، وزائر غير مسجّل يمر بخطوة تسجيل دخول إضافية أولاً. ليس حلاً كاملاً (القيد التقني الأساسي — غياب نص مُعبّأ مسبقًا كـwa.me — يبقى قائمًا بحدود منصة إنستقرام نفسها)، لكنه أفضل حل رسمي متاح، وتحسين حقيقي عن الرابط القديم.
    - **دالة مشتركة جديدة `buildInquiryMessage(productName, price)` بـ[src/lib/messages.ts](src/lib/messages.ts)** توحّد نص رسالة الاستفسار (المُستخدَم برابط واتساب وبزر "نسخ رسالة")، تستخدمها [src/app/products/[id]/page.tsx](src/app/products/[id]/page.tsx) و[ProductActions.tsx](src/components/ui/ProductActions.tsx) معًا بدل قالبين مكتوبين يدويًا بشكل منفصل. **توحيد جزئي فقط**: هذا يوحّد نص الرسالة حصرًا — أرقام/معرّفات القنوات نفسها (`966508949041`, `@BillyStore1`, `Billy_Store3`) تبقى مكتوبة حرفياً (hardcoded) بكل الملفات الثمانية كما هي موثّقة أصلاً بـGotcha #19، لم تُلمَس هالجلسة. أثر جانبي مقصود وموثَّق: نص زر "نسخ رسالة" بـ`ProductActions.tsx` اكتسب "مرحباً،" الافتتاحية ليطابق نص واتساب تمامًا (القيمة الرقمية للسعر لم تتأثر).
23. **قسم تصنيفات مصوَّر بالصفحة الرئيسية (2026-07-30، فرع `feature/home-visual-layer` من `main`)** — مع **تراجع صريح عن خلفية الـhero المصوّرة**:
    - **[CategoryTiles.tsx](src/components/ui/CategoryTiles.tsx)** (جديد) — بطاقتا «ألعاب PS4/PS5» و«اشتراكات PS Plus» بصورة من الكتالوج لكل تصنيف + عدّاد منتجات حقيقي، تُخفى تلقائيًا أي بطاقة تصنيفها فارغ (حتى لا يُرسَل الزائر لصفحة بلا نتائج). fallback عند غياب الصورة بنفس مفردات `ProductCard`. القسم مُدرَج بين الـhero و«ABOUT PREVIEW» بـ[page.tsx](src/app/page.tsx). بلا أي حزمة جديدة (CSS + `ScrollReveal` الموجود)، وبلا لمس `next.config.js` أو `middleware.ts` (الـCSP `img-src ... https:` و`remotePatterns: hostname:'**'` يغطّيان Cloudinary أصلًا).
    - **[src/lib/siteImages.ts](src/lib/siteImages.ts)** (جديد) — مصدر واحد لنصوص/روابط البطاقتين (`CATEGORY_TILES` + `CategoryKey`). القيم `games`/`subscription` تطابق الـDB.
    - **`getHomeData()` بـ[page.tsx](src/app/page.tsx)** — استبدلت `getFeaturedProducts()`: استعلامات متوازية بـ`Promise.all` (المميزة + عدّ التصنيفات + استعلام صورة **منفصل لكل تصنيف**). الاستعلام المنفصل مقصود: استعلام عام واحد بـ`LIMIT` قد تبتلعه أحدث صور تصنيف واحد فيطلع الآخر بلا صورة رغم توفّرها. تعمل مرة كل 60 ثانية بفضل `revalidate` لا لكل زائر، وتُرجع قيَمًا فارغة آمنة عند أي استثناء.
    - **`?category=` صار مدعومًا بـ`/products`** عبر `useSearchParams()` بـ[ProductsBrowser.tsx](src/components/ui/ProductsBrowser.tsx) مع allow-list (`all`/`games`/`subscription`، وأي قيمة أخرى — بما فيها محاولة حقن — تسقط لـ`all`؛ مُختبَر)، و`<Suspense>` بـ[products/page.tsx](src/app/products/page.tsx). **لا تقرأ `searchParams` بالـServer Component هنا** — يحوّل الصفحة لـdynamic ويُلغي `revalidate = 30` (Gotcha #18).
    - **🔴 خلفية الـhero المصوّرة: نُفِّذت ثم أُزيلت بالكامل بقرار صريح من المالك.** الـhero **يبقى بتصميمه الأصلي** (كرات ضوء blur + رموز `△ ○ ✕ □` العائمة، بلا أي صورة). جُرِّبت عدة بوسترات (بوستر GTA VI مفرد، ثم كولاجات ألعاب، ثم art direction بملفين عرضي/طولي عبر `getImageProps` + `<picture>`) وكلها شغّالة تقنيًا ومُقاسة — ثم قرر المالك الرجوع للتصميم القديم. حُذف `HeroBackdrop.tsx` ومجلد `public/hero/` وكل أنيميشنز/CSS الخلفية، و`globals.css` رجع **مطابقًا للأصل بايت ببايت**. **لا تُعد إضافة صورة خلفية للـhero بدون طلب صريح.**
    - **دروس تقنية من تلك المحاولة تستحق البقاء** (تنطبق على أي عمل صور مستقبلي بهذا المشروع):
      - **`scale` على حاوية الصورة لا يغيّر كم يظهر منها إطلاقًا** — يكبّر الحاوية والصورة معًا. الجزء المرئي محكوم بـ`نسبة الحاوية ÷ نسبة الصورة` وحدها. الأداة الفعّالة `object-position`؛ و`scale` مفيد فقط كهامش overflow لإزاحة `translate`.
      - **art direction بملفين تتطلب `getImageProps` + `<picture>`/`<source media>`** (مُصدَّرة ومستقرة بـnext 15.5.21، بلا `unstable_`). مكوّن `<Image>` يقبل مصدرًا واحدًا، ونمط «مكوّنَين مع hidden/block» يحمّل الملفين معًا. عند استخدام `<img>` مع spread، مرّر `alt` **صريحًا بعده** وإلا اشتكت `jsx-a11y/alt-text`.
      - **⚠️ منهجية قياس الشبكة**: تبديل الـviewport/DPR داخل **نفس التبويب** يلوّث `performance.getEntriesByType('resource')` — ظهر تحميل مزدوج **وهمي** لصورة الـhero دفع لتشخيص خاطئ وإزالة أنيميشن بلا سبب ثم إعادته. القياس الصحيح يتطلب `new_page` + `isolatedContext` **لكل مقاس**.
      - **⚠️ على Windows: أوقف السيرفر قبل `npm run build`.** البناء مع سيرفر `npm start` حيّ يُفسد `.next` بقفل ملفات، والأثر يظهر كـ`Cannot find module './vendor-chunks/*.js'` وصفحة فارغة/500 وقت التشغيل — **لا كخطأ بناء**. حدث مرتين؛ العلاج: إيقاف السيرفر ثم `Remove-Item -Recurse -Force .next` ثم بناء نظيف.
    - **CLS على الصفحة الرئيسية = ارتجاف خط Cairo، لا الصور — عيب سابق قائم لم يُصلَح.** قياس بارد أظهر CLS 0.02–0.08 بالرئيسية (صفر عند كاش خط دافئ، وصفر على `/about` و`/faq`). `CLSCulprits` سمّى ملفَّي `fonts.gstatic.com/.../cairo/...woff2` جذرًا، و**كل مصادر الإزاحة عقد `#text` — صفر عنصر صورة**. السبب البنيوي: Cairo يُحمَّل بـ`@import` داخل [globals.css](src/app/globals.css#L1) بدل `next/font/google`، فلا `font-display`/preload ولا حجز مقاسات. الرئيسية تتأثر أكثر لأن عنوانها `text-7xl`. **الحل (ترحيل Cairo لـ`next/font/google`) يلمس كل الصفحات ويستحق تغييرًا منفصلًا — لم يُنفَّذ.**
    - **تحقّق فعلي (بناء إنتاجي + Chrome DevTools MCP)**: `/` و`/products` بقيتا static/ISR (1m و30s)، صفر أخطاء console/CSP، صفر صور مكسورة، ولا تمرير أفقي على 390px، وبطاقات التصنيف تفتح الفلتر الصحيح مسبقًا (3 اشتراكات / 5 ألعاب مطابقة للـDB المحلية). اختبار المسار المصوَّر جرى بصور مؤقتة على `data/billy.db` ثم **استُرجعت الحالة الأصلية حرفيًا** (`typeof`/`quote` مطابقان، صفر `''`، `updated_at` بلا تغيير) — انظر الذاكرة `test-data-restore-discipline`.
24. **مساعد إداري بالذكاء الاصطناعي (2026-07-31/08-01، فرع منفصل عن `main`) — صفحة `/admin/assistant`**: الأدمن يكتب أمراً بالعربي، والذكاء الاصطناعي يفسّره عبر function-calling وينفّذه باستدعاء نفس الـAPI routes الإدارية الموجودة أصلاً (self-fetch داخلي مع تمرير كوكي `admin_token`، صفر تكرار لمنطق التحقق).
    - **الملفات الجديدة**: `src/lib/ai-provider.ts` (طبقة اتصال بمزوّد الذكاء الاصطناعي، انظر قرار الموديل أدناه)، `src/lib/assistant-tools.ts` (سجل 20 أداة + تصنيف مخاطر safe/single-confirm/double-confirm)، `src/lib/assistant-executor.ts` (تنفيذ self-fetch + سجل تدقيق)، `src/lib/self-fetch.ts`، `src/lib/poster-search.ts` (بحث بوستر PlayStation، انظر أدناه)، `src/lib/rate-limit.ts` (مشترك مع `login/route.ts` بعد استخلاصه من هناك)، `src/app/api/admin/assistant/{chat,confirm}/route.ts`، `src/app/admin/assistant/page.tsx`، `src/components/admin/{AssistantConfirmCard,AssistantFinalConfirmModal,BulkPriceUndoDialog}.tsx`.
    - **🔴 بحث تلقائي عن بوستر اللعبة — مبني بالكامل لكن مُعطَّل عمداً (2026-08-01)**: أداة `findGamePoster` (safe tier) موجودة كاملة بالكود (`src/lib/poster-search.ts` + `findWebUrl` بـ`ai-provider.ts`) لكن **مُستبعَدة صراحة من `toAIDeclarations()`** بـ`assistant-tools.ts` (عبر `DISABLED_TOOLS` set) — Gemini لا يراها ولا يقدر يستدعيها إطلاقاً، والتوجيه المرتبط بها أُزيل من `SYSTEM_INSTRUCTION` ومن وصف `createProduct`. **السبب**: الأداة تعتمد على ميزة Google Search grounding بـ`google_search` tool المدمج بـGemini، واللي تبيّن بالاختبار الحي إنه يرجع `429 RESOURCE_EXHAUSTED` **بشكل متطابق عبر موديلين مختلفين تماماً** (`gemini-3.1-flash-lite` و`gemini-3.6-flash`) بنفس اللحظة اللي فيها نداءات `generateContent` العادية وfunction-calling العادي شغالة 100% بدون أي مشكلة على نفس المفتاح — دليل قوي إن الـgrounding يحتاج حساب فوترة (billing) مرتبط بمشروع Google Cloud حتى يفعّل حصته المجانية الاسمية (نمط موثّق لهالميزة تحديداً، مش خلل بالكود). **المستخدم رفض تفعيل أي فوترة بشكل نهائي وصريح** — قرار مستخدم واضح، مو تقييم جودة كود أو حصة يومية عادية. الكود يبقى موجوداً كاملاً (غير محذوف) بانتظار قرار مستقبلي مختلف، موثّق بتعليقات صريحة بكل من `ai-provider.ts` و`assistant-tools.ts` تشرح بالضبط ليش هو معطَّل.
      - **الإرفاق/الاستبدال اليدوي هو الطريقة الوحيدة والدائمة لإضافة صور — شغّال ومُتحقَّق منه بالكامل**: `AssistantConfirmCard.tsx` يعرض معاينة مصغّرة للصورة (إن وُجدت) + زر "إرفاق/استبدال صورة" لأي اقتراح `createProduct`/`updateProduct` — يرفع عبر `/api/upload` (Cloudinary) **نفس المسار المستخدم أصلاً بالداشبورد اليدوي**، ويحدّث `pendingConfirm.args.image` بالصفحة قبل التأكيد النهائي. مُختبَر طرفاً لطرف عبر استدعاء مباشر لـ`/api/admin/assistant/confirm` بحقل `image` حقيقي — وصل بنجاح لقاعدة البيانات.
      - **آلية الاستخراج المبنية (معطَّلة حالياً، لكن موثَّقة لو انفكت مستقبلاً)**: صفحات `store.playstation.com` تطبيق React/Next.js مُصيَّر بالكامل بجافاسكربت — **الـHTML الخام صفر وسوم `og:*` أو `<meta>` مفيدة** (تحقّق مباشر: صفحة 1.4MB، صفر `og:` تاغز)، فلا تحاول أبداً استخراج `og:image` مستقبلاً. المصدر الصحيح المؤكَّد: وسم `<script id="mfe-jsonld-tags" type="application/ld+json">` يحوي بيانات `schema.org/Product` (تتضمن حقل `image` رسمي وحيد)، موجودة بالـHTML الخام رغم إن باقي الصفحة JS-rendered لأنها تُدرَج مباشرة من السيرفر لأغراض الـSEO. منطق الاستخراج نفسه (JSON.parse + تحقق نطاق `.playstation.com`) اختُبر بنجاح على HTML حقيقي محفوظ من صفحة فعلية — الجزء المعطَّل حصراً هو خطوة تحديد رابط الصفحة الصحيحة عبر `google_search`، وليس منطق الاستخراج نفسه.
      - **توسيع بنية `ToolDef`**: حقل اختياري جديد `execute?: (args, req) => Promise<{ok,status,data}>` بـ`assistant-tools.ts` — منفذ (escape hatch) للأدوات اللي ما تقابل route داخلي بالمشروع، بدل الاعتماد الإجباري على self-fetch. `assistant-executor.ts`'s `executeTool` يتحقق من وجود `tool.execute` أولاً قبل اللجوء لـ`buildRequest`/self-fetch القديم — كل الأدوات الأصلية غير متأثرة (ما عندها `execute`، تستمر بنفس مسار self-fetch). هذا التوسيع يبقى مفيداً بغض النظر عن تعطيل `findGamePoster`، لأي أداة مستقبلية تحتاج نفس النمط.
      - لاحظ المستخدم أثناء تجربته الفعلية للميزة (قبل اكتشاف مشكلة الفوترة) إنه أضاف منتجاً حقيقياً ("GTA VI"، `id=23`) بدون صورة — تُرك بدون لمس عمداً (بيانات حقيقية، ليست بيانات اختبار)، يصلح لتجربة حية لاحقة لإرفاق بوستر له يدوياً عبر الزر أعلاه.
    - **آلية التأكيد**: أدوات القراءة (`listProducts`, `getStats`, ...) تُنفَّذ فوراً بدون تأكيد. أدوات الكتابة العادية (`createProduct`, `setGlobalDiscount`, ...) تحتاج تأكيد واحد. الثلاث الأخطر (`bulkDeleteProducts`, `bulkUpdatePrices`, `importProducts`) تتطلب **خطوتين فعليتين منفصلتين حقاً**: بطاقة ملخص أولى، ثم نافذة ثانية منفصلة تتطلب **كتابة الرقم المتأثر صراحة** (يُعاد حسابه واستيثاقه من السيرفر وقت التنفيذ، لا يُؤخذ من الواجهة). دعم "التسلسل المتعدد" (multi-hop، حتى 5 قفزات بـ`chat/route.ts`) ضروري: أمر بالاسم لا بالـid (مثل "احذف اللعبتين X وY") يستدعي الذكاء الاصطناعي أولاً `listProducts` لحل الاسم لـid، ثم الأداة الفعلية بالقفزة الثانية — تجاهل هذا التسلسل (تصميم أول كان يدعم قفزة وحدة فقط) كان بَاگ فعلي اكتُشف بالاختبار الحي: أمر حذف بالاسم كان يرجع "تم التنفيذ" بدون أي حذف فعلي.
    - **حذف ناعم + تراجع**: عمود `products.deleted_at` (فراغ = غير محذوف) + جدول `audit_log` (موسّع بـ`undo_data`/`undone_at`) بـ`db.ts`. `bulk-delete` صار `UPDATE ... SET deleted_at` بدل `DELETE` حقيقي (يشمل الاستخدام اليدوي القديم بالداشبورد أيضاً، مو بس المساعد). قسم "المحذوفات" بالداشبورد يعرض المحذوف لمدة 7 أيام قابلة للاسترجاع. `bulk-price`/`import` يكتبان `snapshot`/`newIds` بـ`audit_log.undo_data` تلقائياً (حتى عند الاستخدام اليدوي) فيتيحان زر "تراجع" بالداشبورد — تراجع `bulk-price` تحديداً يعرض حواراً يقارن السعر الحالي **الحي** بالقديم (`BulkPriceUndoDialog`) قبل التنفيذ، بقرار صريح من المستخدم، لأنه لا حد أقصى على السعر الناتج من `bulk-price` (قيد موجود مسبقاً، موثّق أدناه بلا إصلاح).
    - **استثناء `deleted_at` إجباري بـ13 موقع** عبر كل استعلامات `products` بالمشروع (زائر + إداري) — راجع أي تعديل جديد يقرأ الجدول ويتأكد من الفلتر، خصوصاً صفحات ISR (`page.tsx`, `products/page.tsx`, `products/[id]/page.tsx`) و`sitemap.ts`.
    - **⚠️ قرار موديل الذكاء الاصطناعي — حاسم ومُحدَّث، اقرأه قبل لمس `ai-provider.ts`**: الموديل النهائي **`gemini-3.1-flash-lite`** (مثبَّت صراحة بالكود، **وليس** alias زي `-latest`). أرقام حصة **حقيقية مؤكدة بالاختبار المباشر** (استخراج رسالة 429 الفعلية، مو من توثيق/مدونات): **≥140 طلب/يوم** (اختُبر فعلياً 140 نداء متتالي بدون أي 429 يومي)، **15 طلب/دقيقة**. جودة فهم التسلسل المتعدد (multi-hop) مطابقة لأقوى موديل جُرِّب (`gemini-3.6-flash`) بنفس سيناريو الاختبار بالضبط.
      - **لماذا استُبعد كل بديل آخر**: جيل **Gemini 2.5** بالكامل (`gemini-2.5-flash` و`gemini-2.5-flash-lite`) **محظور كلياً** لهالمفتاح — خطأ `404 NOT_FOUND` صريح "no longer available to new users"، قيد إتاحة وليس حصة (لا يتأثر بمرور الوقت أو إعادة تعيين يومية). جيل **Gemini 2.0** (`flash` و`flash-lite`) حصته **صفر** فعلياً — `429` فوري بـ`limit: 0`. **Gemini 3.5-flash-lite** و**3.6-flash** (خلف alias `gemini-flash-latest`) يعملان لكن بحصة أضعف: 3.5-flash-lite أثبت 15/دقيقة بس (يومي لم يُستكمل فحصه لعدم الحاجة)، و3.6-flash أثبت **20/يوم فقط** رغم كونه "الأحدث" خلف alias — دليل مباشر إن alias الأحدث مو بالضرورة الأفضل حصةً. **Groq** (`llama-3.3-70b-versatile` عبر API متوافق OpenAI) **نُفِّذ بالكامل بملف `ai-provider.ts` بديل ثم تراجُع عنه بالكامل قبل أي اختبار فعلي بمفتاح حقيقي** — أُلغي نهائياً، صفر أثر متبقٍ بالكود (تحقّق: `grep -ri groq src/` يرجع صفر نتائج كود، فقط تعليق تاريخي بـ`ai-provider.ts`).
      - **قيد تقني مكتشف بالاختبار الحي، غير موثّق بأي مصدر رسمي وقت الكتابة**: موديلات Gemini 3.x "المفكِّرة" (بما فيها `gemini-3.1-flash-lite` المُستخدَم) تتطلب حقل `thoughtSignature` يُعاد إرساله حرفياً مع جزء `functionCall` بالدورة التالية، وإلا يرفض الـAPI الطلب. كمان رسائل نتيجة الأداة (function response) يجب أن تُرسَل بـ`role: 'user'`، **وليس** `role: 'function'` كما كانت توثّق نسخ APIs أقدم — استخدام `'function'` يرجع خطأ صريح "Role 'function' is not supported". كود `ai-provider.ts` الحالي يطبّق هذا العقد بالضبط.
      - **قرار "تثبيت صريح لا alias" مقصود ومبني على حادثة فعلية بهالجلسة**: أول اختيار كان `gemini-2.5-flash` (موديل مثبَّت) — انحظر فجأة أثناء الجلسة. الاختيار الثاني كان alias `gemini-flash-latest` (بحجة "يتبع أحدث توصية من Google تلقائياً") — تبيّن إنه يشير فعلياً لـ`gemini-3.6-flash` بحصة يومية ضعيفة جداً (20). النتيجة: alias "الأحدث" لا يضمن أفضل حصة، والتثبيت الصريح لموديل مُختبَر ومؤكَّد (`gemini-3.1-flash-lite`) أضمن هنا من اتباع "الأحدث" آلياً — **لا تُبدَّل لـalias مستقبلاً بدون تكرار نفس اختبار الحصة الحقيقية أولاً**.
25. **تدقيق أمني/جودة على ميزة المساعد الإداري بالذكاء الاصطناعي + إصلاحات (2026-08-01، فرع `main` مباشرة)**: أول جولة تدقيق منظمة لكل ملفات Gotcha #24 (`ai-provider.ts`, `assistant-tools.ts`, `assistant-executor.ts`, `self-fetch.ts`, `poster-search.ts`, `rate-limit.ts`, راوترات `/api/admin/assistant/*` و`/api/admin/audit-log/undo`). **خمس إصلاحات طُبّقت**:
    - **`PUT /api/admin/products/[id]` صار يتحقق من صحة المدخلات** (يعكس تماماً تحقق `POST` الموجود أصلاً بنفس الملف الشقيق: الاسم والسعر مطلوبان، السعر رقم بين 0-99999، الاسم ≤200 حرف) — كان الفجوة الموثّقة سابقاً بـ**Gotcha #10**، لكن الأولوية ارتفعت لأن أداة `updateProduct` بالمساعد (Gotcha #24) توصل لنفس المسار بمدخلات مولّدة من الذكاء الاصطناعي وليست فقط من نموذج بشري. **تحقّق فعلي مباشر (PowerShell + كوكي جلسة أدمن حقيقي، ضد منتج حقيقي بالـDB)**: سعر سالب (`-50`) واسم فارغ رُفضا كلاهما بـ400 مع رسالة عربية واضحة، والمنتج الحقيقي المُستهدف طابق لقطته الأصلية حرفياً (JSON مطابق تماماً) بعد المحاولتين — تعديل صالح بنفس المسار استمر ينجح بـ200.
    - **`POST /api/admin/assistant/confirm` صار كامل الـhandler ملفوف بـtry/catch** (كان بدون أي معالجة أخطاء إطلاقاً، بعكس الشقيق `chat/route.ts`) — JSON تالف أو خطأ داخلي أثناء التنفيذ يرجع الآن `{success:false,error:'...'}` نظيف بدل استثناء غير معالَج. **تحقّق فعلي**: جسم JSON تالف أعاد `500` بصيغة JSON نظيفة بدل استثناء خام.
    - **الأدوات المعطّلة (`DISABLED_TOOLS`، حالياً `findGamePoster` فقط) صارت مرفوضة داخل `getTool()` نفسها**، مو بس مستبعدة من `toAIDeclarations()` — قبل هذا، لو Gemini "هلوس" باسم أداة معطّلة (نادر لكن موثّق كسلوك نماذج)، كانت `chat/route.ts` بتنفّذها تلقائياً (`riskTier: 'safe'`) رغم عدم إخبار النموذج بوجودها أصلاً. **تحقّق فعلي**: استدعاء `/confirm` بـ`toolName: 'findGamePoster'` صراحة يرجع الآن "أداة غير صالحة للتأكيد" (400) لأن `getTool()` ترجع `undefined`.
    - **`chat/route.ts` صار يتحقق من شكل عناصر `history`** (كل عنصر لازم `role` ∈ `{'user','model'}` و`text` نص) قبل تمريرها لـGemini، بدل الاكتفاء بالتحقق إن `history` نفسها مصفوفة. عنصر مشوّه كان يوصل لطلب Gemini ويرجع 502 عام؛ صار يُرفض فوراً بـ400 "سجل محادثة غير صالح". **تحقّق فعلي**: history صحيح استمر يعمل (200)، history بعنصر مشوّه (`role:'attacker', text:123`) رُفض بـ400 كما هو متوقع.
    - **`ai-provider.ts`: نداء `fetch()` لـGemini صار ملفوفاً بـtry/catch** يحوّل فشل الشبكة الخام (DNS/timeout) لـ`AIApiError` برسالة عربية واضحة بدل `TypeError` عام — تجميلي فقط (الاستثناء العام بـ`chat/route.ts` كان يمسكه أصلاً بدون تسريب)، لا تغيير سلوكي ملموس للمستخدم غير الرسالة.
    - **كود ميت مؤكَّد حُذف**: فرع `bulkDeleteProducts` بـ`POST /api/admin/audit-log/undo` (كان يعيد `deleted_at = NULL` بناءً على `undo_data.ids`) — تحقّق مباشر إن الـGET بنفس الملف يرفض صراحة `tool=bulkDeleteProducts` (400)، وإن الداشبورد (`admin/dashboard/page.tsx`) ما يطلب أبداً معاينة تراجع لهالأداة، يعني الفرع ما كان قابل للوصول من أي واجهة من اليوم الأول — استرجاع الحذف الجماعي يمر بالكامل عبر `products/trash` + `trash/restore` بدل هذا المسار. الحذف تحقّق منه بعد الإزالة: `GET ?tool=bulkUpdatePrices/importProducts` و`GET ?tool=bulkDeleteProducts` (لسا 400 كما كان) يشتغلون تماماً كالسابق.
    - **تبعيات**: bump غير-major لـ`next` (15.5.21→15.5.22)، `react`/`react-dom` (→19.2.8)، `@types/react`/`@types/react-dom`/`@types/node`، `autoprefixer`، `eslint-config-next` (→15.5.22)، `lucide-react` (→1.28.0)، `postcss` الجذري، و`@libsql/client` (0.14.0→0.17.4 — الحزمة pre-1.0 الوحيدة بالقائمة، تحقّق إضافي إنها ما كسرت شي عبر بناء إنتاجي ناجح يولّد صفحات `products/[id]` الثابتة عبر استعلامات DB حقيقية وقت البناء + smoke test حي بعد إعادة تشغيل السيرفر). **أثر جانبي غير متوقّع مفيد**: bump `eslint-config-next` وحده (بدون أي ترقية major لـ`eslint` نفسه) قلّل ثغرات `npm audit` من 12 لـ4 عالية الخطورة فوراً، وبعدها `npm audit fix` غير-الكاسر (بدون `--force`) أغلق ثغرة `brace-expansion` كاملة (كانت موثّقة كجزء من سلسلة **Gotcha #20** التي كانت تتطلب ظاهرياً ترقية `eslint` major لحلها) — **Gotcha #20 صار الآن أضيق نطاقاً**: الثغرتين المتبقيتين حصراً (`sharp`/`postcss` المدمجتين داخل `next` نفسه، تحتاج Next 16 major) تبقيان بلا حل كما كانتا، موثقتين أصلاً بـ**Gotcha #5**. لم تُطبّق أي ترقية major (`next` 16، `eslint` 10، `jose` 6، `bcryptjs` 3، `tailwindcss` 4، `typescript` 7) — بقيت مؤجّلة كقرارات منفصلة كما هي موثّقة بـGotcha #5/#20.
    - **تحقّق نهائي**: `npx tsc --noEmit` وبناء إنتاجي (`npm run build`) نظيفين تماماً بعد كل مجموعة تعديلات (الأمان أولاً، ثم التبعيات، ثم حذف الكود الميت) — تحذيرات ESLint المتبقية (require بـ`db.ts`، تعبير غير مستخدم بـ`dashboard/page.tsx`) كلها سابقة الوجود وموثّقة، لم يُضَف أي تحذير جديد.
    - **🔴 ملاحظة مؤجَّلة بقرار صريح، غير مُصلَحة الآن**: حذف منتج فردي عبر `DELETE /api/admin/products/[id]` (يستخدمه كل من زر الحذف اليدوي بالداشبورد، أداة `deleteProduct` بالمساعد، وزر "حذف نهائي" بقسم المحذوفات) هو **حذف حقيقي نهائي** (`DELETE FROM products`) — بعكس الحذف الجماعي (`bulk-delete`) اللي هو *soft-delete* عبر `deleted_at` مع استرجاع 7 أيام (Gotcha #24). يعني أمر بصيغة "احذف هذا المنتج" (فردي) يعطي نتيجة أقل أماناً وقابلية-للاسترجاع من "احذف هذه المنتجات" (جماعي) بنفس الأداة تقريباً، بدون ما يكون هذا الفرق واضح من اسم الأداة وحده — رغم إن نص تأكيد المساعد نفسه يوضّح صراحة "لا يمكن التراجع عنه" فما فيه تضليل للمستخدم. **توحيد السلوك (تحويل الحذف الفردي لـsoft-delete أيضاً) قرار سلوكي يحتاج موافقة صريحة بجلسة منفصلة — لم يُنفَّذ هالجلسة بقرار مقصود من المستخدم** (طُلب توثيقه بدل إصلاحه، بنفس مبدأ توثيق القرارات المؤجلة بباقي هذا الملف).
26. **أداة `findGameInfo` عبر RAWG API (2026-08-01، فرع `main` مباشرة) — بديل فعلي لـ`findGamePoster` المعطّلة (Gotcha #24)**: `findGamePoster` بقيت كما هي (معطّلة، PlayStation Store عبر Google Search grounding). الجديد: `src/lib/game-info.ts` + أداة `findGameInfo` (`safe` tier، `execute` escape hatch) بـ`assistant-tools.ts` تجلب اسم/وصف/صورة/تاريخ إصدار/أنواع لعبة من **RAWG API** (REST مباشر بمفتاح مجاني، **صفر بحث Google أو فوترة**) لاستخدامها من `createProduct`.
    - **مفتاح جديد `RAWG_API_KEY`** بـ`.env.example` (نفس نمط `GEMINI_API_KEY`) — اختياري، بدونه الأداة ترجع خطأ عربي واضح (`findGameInfo` لا ترمي استثناء أبداً، نفس عقد `poster-search.ts`).
    - **إعادة صياغة الوصف العربي تتم عبر استدعاء Gemini منفصل ومخصص** (`rewriteGameDescriptionAr` بـ`ai-provider.ts`، نص عادي بدون tools/history/thoughtSignature) — **قرار صريح من المستخدم**: الجودة يجب أن تكون مضمونة بغض النظر عن سياق محادثة المساعد أو صياغة طلب الأدمن، بدل الاعتماد على `SYSTEM_INSTRUCTION` العام. **هذا الاستدعاء يستهلك من نفس حصة `GEMINI_API_KEY` المشتركة مع محادثة المساعد (~140 طلب/يوم، Gotcha #24) — ليست حصة منفصلة.** عملية "إضافة منتج" واحدة عبر `findGameInfo` ~ 3-4 طلبات Gemini إجمالاً (هوبّي `callAI` + استدعاء إعادة الصياغة)، ضمن حدود الحصة المؤكَّدة بارتياح لحجم استخدام متجر صغير.
    - **تحقّق حي مباشر (مفتاحا RAWG/Gemini حقيقيان، `curl` + خادم dev فعلي، بدون بيانات اختبار متبقية بالـDB)** كشف تفصيلاً غير متوقَّع: نتيجة بحث RAWG الأولى (`search`) **لا تحوي حقل `description_raw` إطلاقاً بأي حال** — استدعاء `GET /api/games/{id}` منفصل **إلزامي دائماً**، لا شرطي كما افتُرض أول التخطيط. كمان، أعلى نتيجة بالصلة لبحث "God of War Ragnarok" كانت نسخة PC مكررة (id مختلف، منصة PC فقط) بينما النسخة الرسمية متعددة المنصات (PS4/PS5/PC) جات **ثانية** — لذا `findGameInfo` تفضّل أي نتيجة من أعلى 5 نتائج تحوي منصة اسمها يحوي "PlayStation" (`isPlayStationTitle`) بدل أخذ أول نتيجة دائماً؛ تحقّق فعلي بعد الإصلاح: طلب "أضف God of War Ragnarök" رجّع بطاقة تأكيد بالاسم/الصورة/تاريخ الإصدار الصحيحين للنسخة الرسمية، **مو** نسخة PC المكررة.
    - **سلوك فشل موجود مسبقاً بـ`chat/route.ts` (غير مرتبط بهالإضافة، لكن يهم أي أداة `safe` جديدة)**: لو أداة `safe` رجّعت `ok:false`، الرد يرجع فوراً `{type:'error', error:<رسالة الأداة>}` للأدمن — Gemini **ما بياخذ فرصة** يعيد المحاولة أو يسأل توضيحياً (لا hop إضافي). تحقّق فعلي: اسم لعبة عشوائي غير موجود رجّع مباشرة `"لم يُعثر على لعبة مطابقة بقاعدة بيانات RAWG"` بدون استثناء أو 500.
    - **لا رفع تلقائي لـCloudinary** — رابط الصورة المسترجَع (`media.rawg.io`) يُستخدم مباشرة بحقل `image`، بنفس فلسفة `findGamePoster` الأصلية. زر "إرفاق/استبدال صورة" الموجود أصلاً بـ`AssistantConfirmCard.tsx` (Gotcha #24) يبقى الخيار لو احتاج الأدمن نسخة مستضافة على Cloudinary.
    - **حصة RAWG المجانية**: "up to 20,000 requests per month" — نص مقتبس حرفياً من `rawg.io/apidocs` الرسمية (fetch حي بهالجلسة، مو مدونة طرف ثالث). طلب بدون مفتاح لـ`api.rawg.io/api/games` رجّع `401` حقيقي (تحقّق حي إضافي إن `key` إلزامي حتى للبحث). منفصلة تماماً عن حصة Gemini، صفر علاقة بينهما.
27. **مصدر `findGameInfo` تغيّر من RAWG لـPSN Store مباشرة (2026-08-02/03، فرع `main` مباشرة) — RAWG صار fallback معطّل، مو محذوف**: قصة كاملة بدأت بحادثة إنتاج حقيقية وانتهت بإعادة تصميم `findGameInfo` بالكامل.
    - **الحادثة الأصلية**: أول اختبار حي لصفحة `/admin/assistant` على `billy-store.vercel.app` رجّع 502 عام ("تعذّر الاتصال بمساعد الذكاء الاصطناعي"). تشخيص عبر `vercel env ls`/`vercel logs` (read-only، Vercel CLI مسجّل دخول مسبقاً) أثبت السبب بثلاث طرق مستقلة: `GEMINI_API_KEY` (و`RAWG_API_KEY`) **ما كانوا مضافين إطلاقاً** لمتغيرات بيئة Vercel production — الميزة كاملة (Gotcha #24-26) اتبنت واختُبرت محلياً بس عبر `.env.local`، ولا حد أضاف المفاتيح للإنتاج قبل هالجلسة. الإصلاح: `vercel env add` للمفتاحين + `vercel --prod` نشر جديد (كان يشمل RAWG أصلاً كود Gotcha #26 اللي ما وصل الإنتاج قبل هيك أصلاً — أول push فعلي لـ`main` ما بيطلق نشر تلقائي، لأنه لا يوجد Git integration مفعّل على المشروع، كل النشرات يدوية عبر `vercel --prod`).
    - **RAWG اتضح إنه واقع بشكل شبه دائم أثناء كامل هالجلسة** (فحص مباشر متكرر لـ`api.rawg.io` رجّع `HTTP 000`/`522` timeout بدون استجابة، مو 429 حصة) — بقرار صريح من المستخدم، `findGameInfo` بـ`game-info.ts` صارت تستدعي `findPsnGameInfo` **حصراً** بدون أي fallback لـRAWG. `findRawgGameInfo` بقيت بالكود كاملة (**exported**، مو محذوفة) بدالة `findGameInfo` نفسها معلَّقة كتعليق جاهز لإعادة التفعيل بسطر واحد لو رجع RAWG يشتغل مستقبلاً.
    - **Giant Bomb اتفحصت كبديل واستُبعدت نهائياً**: `giantbomb.com/api` نفسها تصرّح رسمياً إن API الألعاب (وكل شي ثاني) **"Not currently available"** — الشركة بمنتصف إعادة بناء بنيتها التحتية بالكامل بعد الانفصال عن Fandom (مؤكَّد بزيارة الصفحة الرسمية مباشرة، مو تخمين أو حجب Cloudflare عابر).
    - **PSN القديمة (`store.playstation.com/store/api/chihiro/...`, reverse-engineered عبر [olegshulyakov/psn-swagger](https://github.com/olegshulyakov/psn-swagger)) اشتُغل بيها فعلياً وانحلّت بالكامل** (بحث بدون مفتاح، اشتغلت من Vercel، فحص تطابق اسم `nameMatchesQuery` أُضيف بعد اكتشاف حي إن ترتيب بحثها ممكن يرجّع نتيجة غلط بثقة) — **لكن تبيّن إنها API قديمة/متروكة نسبياً**: تحقّق حي مباشر أثبت إنها **لا تعرف عن "EA Sports FC 26" إطلاقاً** (صفر نتائج بأي صياغة، حتى مع فرز `sort=release_date&direction=desc` الموثّق بمواصفتها لكن بدون أي تأثير فعلي — دليل إضافي إنها staleغير مُحدَّثة). القرار: **استُبدلت بالكامل** بدل الإبقاء عليها كطبقة أولى.
    - **المصدر الحالي (`src/lib/psn-store.ts`): يقرأ صفحات `store.playstation.com` الحيّة نفسها مباشرة** (نفس الموقع اللي يتصفحه أي زائر حقيقي، `ar-sa` locale) — بدون أي مفتاح أو مصادقة. آلية الاستخراج:
      - صفحة `/{locale}/search/{query}` وصفحة `/{locale}/product/{id}` كلاهما SSR (Next.js) وفيهم `<script id="__NEXT_DATA__">` يحوي كامل Apollo cache (`props.apolloState`) — نتائج البحث (اسم، منصات، `storeDisplayClassification`: `FULL_GAME`/`GAME_BUNDLE`/`PREMIUM_EDITION`/`VIRTUAL_CURRENCY`/`ITEM`، صور `media` بأدوار مسمّاة زي `MASTER`) تُقرأ مباشرة من هنا.
      - **الوصف الكامل مو بنفس المستوى** — مدفون طبقة أعمق جوه `pageProps.batarangs.info.text` (نص HTML لِـ"batarang"، مصطلح سوني الداخلي لـwidget مستقل)، وجواه `<script id="env:...">` تاني بـJSON خاص فيه نسخة أغنى من كائن المنتج (`descriptions[]` مقسّمة بحقل `type`: `SHORT`/`LONG`/`LEGAL`/`COMPATIBILITY_NOTICE` — `LONG` بالضبط الفقرة السردية النظيفة المطلوبة، منفصلة عن النص القانوني من الأساس، بعكس `long_desc` القديمة اللي كانت تخلط كل شي بفقرة وحدة).
      - اكتُشفت هالبنية عبر Chrome DevTools MCP حي (تصفّح فعلي + فحص شبكة) بعد ما تبيّن إن نتيجة البحث SSR بالكامل (صفر طلب GraphQL من طرف العميل يُلتقط بـNetwork tab للبحث تحديداً — البحث يصير على سيرفر سوني نفسه). محاولة اتباع مقترح المستخدم الأصلي (استدعاء مباشر لـ`web.np.playstation.com/api/graphql/v1/` بطريقة Apollo persisted queries، بمرجعية مكتبتين مفتوحتين: [mrt1m/playstation-store-api](https://github.com/mrt1m/playstation-store-api) و[FriendsOfGalaxy/galaxy-integration-psn](https://github.com/FriendsOfGalaxy/galaxy-integration-psn)) **كُشفت غير كافية**: كلا المرجعين يدعمان بس "جيب منتج/concept بمعرّف معروف مسبقاً" أو عمليات مصادَق-عليها لمكتبة المستخدم، **صفر عملية بحث نصي حر** — لا يوجد hash محفوظ علناً لعملية البحث، والبديل العملي الوحيد المتاح كان قراءة HTML الجاهز نفسه بدل محاولة استخراج hash عملية GraphQL غير موثّقة لمحرك بحث SSR بالكامل من طرف العميل.
      - **تحقّق حي مقارَن مباشر (نفس الاستعلامات بالضبط، القديمة مقابل الجديدة)**: "EA Sports FC 26" — القديمة صفر نتائج، الجديدة لقتها فوراً (`EA SPORTS FC™ 26 Standard Edition`). "Resident Evil 4" — القديمة رتّبت DLC غير متعلّق (`Dead by Daylight`) بالمركز الأول ومو حتى موجودة اللعبة الحقيقية بأول 10 نتائج، الجديدة رتّبت "Resident Evil 4 PS4 & PS5" (`FULL_GAME`) بالمركز الأول بالضبط.
      - **تحقّق حي كامل عبر `/admin/assistant`** (خادم dev محلي، صفر بيانات اختبار متبقية): "God of War Ragnarök"، "EA Sports FC 26"، و"Resident Evil 4" الثلاثة رجّعوا بطاقات تأكيد صحيحة (اسم/صورة CDN سوني الحقيقي/وصف عربي رسمي دقيق يذكر تفاصيل فعلية زي فأس الليفياثان أو ليون كينيدي)، واسم غير موجود رجّع خطأ عربي واضح نظيف بدون استثناء.
    - **`nameMatchesQuery` بقيت كما هي بالضبط** (نُقلت حرفياً من التطبيق القديم لـ`psn-store.ts` الجديد) رغم إن ترتيب البحث بالمصدر الجديد أفضل بكثير — إبقاؤها كشبكة أمان إضافية مقصود، مش افتراض إنها لسا ضرورية بنفس درجة الأهمية.
    - **`cleanPsnDescriptionAr` (بـ`ai-provider.ts`) لسا مستخدَمة كما هي** رغم إن مصدر البيانات تغيّر بالكامل — دورها تغيّر شوي: مع API القديمة كانت "تستخرج فقرة سردية من نص مبعثر بقانوني/DLC"، ومع المصدر الجديد (نص `LONG` نظيف أصلاً بدون خلط) صار دورها الفعلي أخف ("تلخيص/تكثيف لطول أسلوب الكتالوج") — نفس الدالة، نفس الـprompt، بدون حاجة لتعديل، بس المدخل الآن أنظف بكثير من قبل.
    - **صورة الغلاف تجي من `image.api.playstation.com` مباشرة** (نطاق سوني الرسمي، نفس الصور المعروضة لأي زائر حقيقي بالموقع) — `pickImage()` تفضّل `media` بدور `role === 'MASTER'` (نوع "الصورة الرئيسية" بمصطلحات سوني)، وإلا أول صورة `IMAGE` متوفرة.
    - **منطقة رمادية بشروط الاستخدام لسا قائمة كما كانت مع API القديمة** — هذا استخدام غير رسمي لصفحات الموقع (scraping HTML بدل استهلاك API موثّق)، والنص القانوني المُستخرَج نفسه (نوع `LEGAL`) بيذكر صراحة PlayStation Terms of Service. قرار مقبول بنفس منطق `findGamePoster` الأصلية (Gotcha #24) — يُعاد تقييمه فقط لو صار حجب فعلي أو تغيّر جوهري بالبنية.
28. **تبديل نظام الخطوط بالكامل من Thmanyah/Sahel المزدوج لخط STC وحيد (2026-08-07، فرع `design/spatial-ui-light`، ما زال بدون commit)** — هذا الفرع يحوي إعادة تصميم شاملة لكامل الواجهة العامة (`/`, `/products`, `/products/[id]`, `/about`, `/faq`) من الثيم الداكن القديم لثيم "Spatial UI" فاتح (تدرّج أزرق/بنفسجي فاتح، بطاقات بيضاء عائمة، ألوان `--brand`/`--ink`/`--surface`/... الجديدة بـ`globals.css`/`tailwind.config.js`) — تفاصيل التصميم الكاملة غير موثَّقة هنا بعد (الفرع لم يُدمَج بـ`main` بعد وقت كتابة هالسطر). بعد اكتمال ذاك التصميم بالكامل (كل الصفحات + المكوّنات المشتركة `Button`/`Badge`/`Card`/`PsGlyphField`/إلخ)، طلب المستخدم كخطوة أخيرة قبل أي commit استبدال نظام الخطوط المزدوج (Thmanyah للعناوين، Sahel لكل شي ثاني) بخط واحد يغطي الموقع كامل، مع تمايز المستويات عبر وزن الخط (font-weight) بدل تبديل العائلة.
    - **⚠️ خط STC ملكية خاصة بشركة الاتصالات السعودية (stc)، بدون ترخيص رسمي متاح — قرار مستخدم صريح بالمتابعة رغم ذلك.** بحث مباشر أكّد: الخط صُمم حصريًا لـstc بإعادة تسميتها التجارية 2011 عبر استوديو **Tarek Atrissi Design** (موثّق بصفحتي دراسة حالة رسميتين على `atrissi.com`)، وما إله أي قناة ترخيص أو شراء رسمية — كل نسخة متداولة أونلاين مصدرها مواقع توزيع خطوط عربية غير رسمية (`arbfonts.com`, `alfont.com`, إلخ). عُرض الموضوع على المستخدم صراحة (بما فيه بُعد التماهي مع علامة تجارية حقيقية غير متعلقة بالمتجر) عبر `AskUserQuestion`، وقرر المتابعة — نفس نمط قرار مخاطرة Thmanyah المقبول أصلاً (زيبب غير قابل لإعادة التوزيع)، موثّق هنا بنفس الروح.
    - **تحقّق فعلي من الأصالة قبل الاستخدام — اكتشف مشكلة حقيقية بالمصدر الأول.** أول رابط تحميل مباشر تم تجربته (`fontstatic.com/fonts/stc/stc.ttf`) رجّع ملف TTF حقيقي، لكن فحص جدول `name` بداخله عبر `fonttools ttx` كشف إنه فعليًا خط **"GE SS Text Light" لشركة Boutros International** معاد تسميته لـ`stc.ttf` فقط — مو STC إطلاقًا. رُفض هذا الملف فورًا. المصدر الصحيح المؤكَّد: `arbfonts.com` و`alfont.com` (رابطي تحميل مباشرين لكل منهما، مو نفس الرابط) رجّعا ملفين **متطابقين بالـMD5 checksum** لبعض، وجدول الـ`name` بيهم يقول صراحة `STC Regular`/`STC Bold`، بحقوق نشر `Copyright (c) 2011 by Tarek Atrissi Design` — يطابق تمامًا قصة إعادة التسمية التجارية الموثّقة أعلاه. تغطية الحروف العربية فُحصت أيضًا (جدول `cmap`) وشملت كل الحروف الأساسية + أشكال العرض (presentation forms) + الروابط (ligatures) — خط عربي احترافي كامل، مو placeholder.
    - **وزنان حقيقيان فقط (Regular 400 + Bold 700) — تأكَّد بالفحص المباشر، مو افتراضًا.** مواصفة التصميم الأصلية (2011) بتذكر "4 أوزان" صُمّمت، لكن التوزيع العلني الفعلي يقتصر على وزنين. جُرِّب فعليًا طلب أسماء أوزان أخرى (`STC-Light.ttf`, `STC-Medium.ttf`, `STC-SemiBold.ttf`, `STC-ExtraBold.ttf`, `STC-Black.ttf`, ...) على نفس مصدر `arbfonts.com` المؤكَّد — كلها رجعت صفحات 404 حقيقية (HTML، مو خطوط)، يعني ما فيه وزن ثالث حقيقي متاح للتحميل حاليًا. لو ظهر مستقبلاً مصدر موثوق لوزن إضافي حقيقي (مو faux-bold مُصنَّع من موقع توزيع)، لازم يُعاد بناء جدول الأوزان أدناه — القرار الحالي مبني صراحة على "وزنان بس".
    - **جدول الأدوار-للأوزان (weight-role table) — نظام ثنائي (Bold/Regular) بدل تدرّج 4-5 مستويات كان متاح بالنظام القديم.** بما إنه ما فيه غير وزنين، مافيه طريقة حقيقية لتمثيل 7 مستويات نصية مختلفة عبر الوزن وحده — التمايز الإضافي بيصير عبر الحجم/اللون فقط (كانت أصلاً تسوي جزء كبير من هالشغلة قبل التبديل). **كل شي تقريبًا صار Bold (700)**: H1، H2 (يتساوى بالوزن مع H1، والتمايز الوحيد صار حجم الخط فقط — بلا وزن أخف متاح)، العناوين الفرعية/التاجلاين (kicker labels + سطر الهيرو الثاني)، الشعار النصي (wordmark)، نص الأزرار وروابط الـnav (قرار صريح لصالح Bold مو الوزن المتوسط المفترض أصلًا — نص أبيض معكوس على خلفية ملوّنة صلبة يحتاج وزن ثقيل للتباين)، وlabels/badges الصغيرة (تصنيف، breadcrumb). **الاستثناء الوحيد لـRegular (400)**: عناوين الكروت فقط (`ProductCard` اسم المنتج، `FeatureCard.title`، `CategoryTiles` h3) — تخفيف وزن حقيقي ومقصود عن النظام القديم (كانت `font-black`/`font-bold`)، لتبقى أخف من العناوين الكبيرة. **استثناء المستخدم الصريح على "الأسعار"**: خطة التصميم الأصلية اقترحت تخفيف وزن الأسعار لـRegular (بحجة "الحجم الكبير كافي للبروز")، لكن المستخدم رفض هالجزء تحديدًا وطلب إبقاء الأسعار Bold — "أهم عنصر تحويل بالصفحة، والوزن الخفيف بيقلل بروزها البصري حتى لو الحجم الكبير محفوظ". **لا تُعِد تخفيف وزن الأسعار لـRegular بدون طلب صريح جديد.**
    - **الملفات المتأثرة**: `src/lib/fonts.ts` (تعريف `stc` وحيد عبر `next/font/local`، وزنين فقط)، `tailwind.config.js` (`fontFamily.sans` و`fontFamily.display` كلاهما يشيران لنفس `var(--font-stc)` — الفصل بينهم صار بلا معنى فعليًا، أُبقي `font-display` بالماركب كما هو بدل تعديل 6 ملفات لمجرد إعادة تسمية class بقيمة متطابقة)، `src/app/layout.tsx`، و17 ملف storefront (كل الصفحات الخمس + `Navbar`/`Footer`/`Button`/`Badge`/`Card`/`CategoryTiles`/`ProductCard`/`ProductActions`/`ProductsBrowser`/`RelatedProducts`/`not-found`) — تطبيق الجدول أعلاه عبر تطبيع كل `font-black`/`font-semibold`/`font-medium`/`font-extrabold` لإما `font-bold` أو `font-normal` صراحة (تحقّق نهائي: grep لهالأنماط الأربعة يرجّع صفر خارج `admin/**`). **`admin/**` بالكامل غير ملموس** (خارج نطاق التصميم الجديد أصلًا). `public/fonts/thmanyah/` و`public/fonts/sahel/` **حُذفوا بالكامل** بعد تأكيد نجاح بناء إنتاجي كامل بالخط الجديد (لم يُحذفوا قبل التأكد، كشبكة أمان رجوع).
    - **لا تستغرب لو لقيت `font-bold` بمكان كان `font-black`/`font-semibold` بالكود القديم — مو خطأ ولا فقدان بالغلط، هيك القرار المتعمَّد أعلاه.** أي محاولة "استرجاع" الوزن الأصلي بدون فهم إنه ما فيه غير Bold/Regular فعليًا بالخط الحالي هتكسر الاتساق البصري المقصود.
    - **تحقّق فعلي شامل (بناء إنتاجي + Chrome DevTools MCP، `npm start` على منفذ منفصل)**: `getComputedStyle` أكّد الخط يحل فعليًا لـ`stc, "stc Fallback", Tahoma, sans-serif` بكل عنصر مفحوص (H1/H2/tagline/wordmark) بوزن `700`، وعناوين الكروت بوزن `400` بالضبط كما مخطَّط. لقطات شاشة حية للصفحات الخمس (بعد التأكد إن `take_screenshot` بخاصية `fullPage:true` بيعطي نتيجة فاضية مضلِّلة لصفحات طويلة بهالبيئة تحديدًا — تحقّق فعلي عبر مقارنة مباشرة مع screenshot عادي بدون `fullPage` بعد `scrollIntoView`، اللي أظهر المحتوى الحقيقي كامل وسليم؛ **استخدم screenshots بدون `fullPage` + `scrollIntoView` يدوي لصفحات طويلة بهالمشروع**، مو `fullPage:true`). صفر أخطاء console بكل الصفحات الخمس (باستثناء تحذير a11y سابق التوثيق وغير متعلق: حقل بحث `/products` بدون `name`/`id`). ملاحظتان جانبيتان غير متعلقتين بالخط، لوحظتا أثناء الفحص ولا تحتاجان فعل: (1) فرق `window.innerWidth` (406) عن `visualViewport.width`/`document.documentElement.clientWidth` (390 كلاهما) بمقدار 16px بالضبط تحت محاكاة الموبايل بهالبيئة تحديدًا (Windows + مقياس عرض 125%) — نفس عائلة الخلل الموثّق أصلًا مع أداة `resize_page`، لا يظهر بصريًا (`overflow-x:hidden` على `body` يمنع أي تمرير أفقي فعلي)، وتأكَّد بمقارنة screenshot مباشر لا يوجد أي قص/كسر حقيقي. (2) نص وصف منتجين تجريبيين ("Spider-Man 2" وغيره) يحوي حروف مشوَّهة (mojibake) — بيانات قاعدة بيانات تالفة الترميز أصلًا (ظهرت بنفس الشكل بكل من بطاقة المنتج وصفحة التفاصيل، يعني المشكلة بالبيانات نفسها لا بعرض الخط) — خارج نطاق هالجلسة، لم تُلمَس.
    - **تصحيح لاحق (Gotcha #29)**: الملاحظة أعلاه عن حقل بحث `/products` بدون `name`/`id` **صارت غير دقيقة** — تحقّق مباشر لاحق (فحص a11y حي) أكّد الحقل فعليًا `id="product-search"`, `name="search"`, `aria-label` كامل. غير معروف بالضبط أي commit سكّر هالفجوة (على الأرجح أثناء اندماج فرع الأدمن/QA لاحقًا)، بس الحالة الحالية سليمة.
29. **تدقيق شامل قبل إطلاق تصميم "Spatial UI Light" لايف + دمج الفرع بـ`main` (2026-08-11/12، فرع `design/spatial-ui-light`)**: الفرع (Gotcha #28) وصل لحالة "جاهز لايف" عبر ثلاث جلسات فحص متتالية، ثم اندمج فعليًا بـ`main` ونُشر — أول مرة يندمج هالفرع الضخم بعد أسابيع من العمل المتراكم بدون أي commit.
    - **البداية: تقرير مستخدم "صفحة الأدمن تخبصت"، والسبب الجذري كان توكِنز Tailwind ميتة، مش خطأ تصميم**. بعد اكتمال التصميم العام (Gotcha #28)، `admin/**` بالكامل (login/dashboard/assistant + 3 مكوّنات مساعد) بقيت تستخدم توكِنز الثيم الداكن القديم (`bg-dark`, `text-primary`, `border-dark-border`, `text-slate-400`...) اللي اتشالت من `tailwind.config.js` — Tailwind يولّد **صفر CSS** لأي كلاس غير معرَّف بدل ما يرمي خطأ بناء، فالصفحة تظهر بألوان مكسورة بصمت. الإصلاح: هجرة ميكانيكية بـ`sed` مرتّبة حسب طول/تخصيص النمط (الأطول أولاً لتفادي تصادم substring، مثلاً `hover:bg-primary-dark` قبل `bg-primary`) عبر الملفات الستة، زائد إصلاحات يدوية لتصادمات نتجت عن الاستبدال الآلي (بادجات فئة المنتج تصير متطابقة بالغلط، أيقونات إحصائيات تفقد تمايزها).
    - **مشكلة تباين حدود البطاقات (63 موقع/13 ملف) — نفس فئة الخطأ (توكِنز ميتة/ضعيفة) لكن أعمق: توكن *معرَّف* لكن بتباين شبه معدوم.** `border-chip` (`--chip-bg: #DCEAFB`) يعطي تباين **~1.1:1** فقط ضد `--surface`/`--canvas` (الحد الأدنى WCAG لحدود UI هو 3:1) — بطاقات "شبه مختفية" رغم إنها كلاس صحيح تقنيًا. الحل: إعادة استخدام نمط `Card.tsx` الموجود أصلًا (`border-2 border-black/15`، بوردر أسود شفاف حقيقي) بدل اختراع توكن جديد أو تغميق `--chip-bg` عالميًا (اللي مستخدم لأغراض تانية بـ23 ملف — بادجات، فواصل جداول). **لا تلمس `--chip-bg` نفسه لحل مشاكل تباين حدود مستقبلية — أضف/استخدم بوردر مخصص بدل ما تغيّر توكن مشترك متعدد الاستخدامات.**
    - **الفراغ فوق الهيدر + شريط الإعلانات الأبيض كانا تشخيص خاطئ من المستخدم — الكود كان سليم فعليًا، والسبب سيرفر تطوير قديم/متوقف.** المستخدم افترض رجوع باگ قديم (commit `1c8c7b1`) بناءً على صور فوتوغرافية لشاشته. التحقّق الفعلي (`git diff`/قراءة مباشرة) أثبت الكود مطابق تمامًا للنسخة الصحيحة بكلا الملفين — والسبب الحقيقي: محاولة الوصول لـ`localhost:3000` وقتها رجّعت `ERR_CONNECTION_REFUSED` (سيرفر متوقف/متعطّل من جلسة سابقة). **الدرس**: لو مستخدم وصف "رجوع" مشكلة كانت اتصلحت، تحقّق من الكود **قبل** افتراض حاجة لتعديل — إعادة تشغيل نظيفة للسيرفر ممكن تكون الحل الكامل.
    - **`ConfirmDialog.tsx` (مودال تأكيد الحذف المشترك، يُستخدم لكل حذف بالموقع) اكتُشف لاحقًا إنه ناجٍ بالكامل من هجرة الأدمن الأولى — 100% توكِنز ميتة، مودال شفاف تقريبًا عند أي محاولة حذف.** لم يُكتشف إلا بجولة فحص شاملة لاحقة (grep عريض `bg-dark|text-primary|slate-\d{3}|border-dark`) — **درس منهجي: بعد أي هجرة تصميم كبيرة، اعمل grep نهائي شامل على كامل `src/` للتوكِنز القديمة قبل اعتبار الشغل مكتمل، مش بس الملفات اللي كنت تتوقعها.** نفس الجولة كشفت `text-dark` (توكن ميت تاني) على زر أمبر بلوحة الأدمن — اتصلح لـ`text-brand-ink`.
    - **فحص a11y حي (وكيل خلفي، Lighthouse + مقارنة تباين يدوية دقيقة) كشف مشكلة أعمق من مجرد "توكِنز ميتة": 38 عنصر بلوحة الأدمن (زر حذف بكل صف منتج، أزرار شريط الأدوات، بادجات) كانت تستخدم `text-{لون}-400` — وزن لون فاتح صُمم أصلًا للخلفية الداكنة القديمة، وتحت الخلفية البيضا الجديدة صار التباين 1.3:1-2.6:1 (تحت حد WCAG 4.5:1 بكتير).** هذي كلاسات *صحيحة تقنيًا وموجودة فعليًا* بـTailwind (بعكس التوكِنز الميتة) — يعني `grep` عن كلاسات غير معرَّفة ما كان راح يلقطها؛ لازم فحص تباين فعلي (Lighthouse/حساب WCAG مباشر) بعد أي انتقال ثيم داكن→فاتح، مش بس فحص "هل الكلاس موجود". الحل: تحويل موحّد لـ`-600` (وزن Tailwind المقروء على الأبيض) لكل عائلات الألوان الخمس المستخدمة.
    - **`--muted` (نص ثانوي) كان حدّي (4.42-4.50:1) تحديدًا لما يقع مباشرة فوق تدرّج الخلفية (خارج أي بطاقة بيضا)** — نفس الكلاس يعطي 5.03:1+ فوق `bg-surface`. غُمِّق لـ`#4A6084` (من `#5B7091`) ليعطي 5.5-6.4:1 مضمونة بكل السياقات. **قرار مقصود: عدل التوكن نفسه (نقطة مركزية وحيدة) بدل إضافة استثناءات per-usage.**
    - **إصلاحات a11y صغيرة إضافية**: شارة "1" الحمراء على زر واتساب العائم كانت بدون `aria-hidden`، فاسمها الـaccessible ما كان يطابق الظاهر بصريًا (WCAG 2.5.3) — أُضيف `aria-hidden="true"` (الشارة زخرفية بحتة، رقم ثابت مو عدّاد حقيقي). تحذير Next.js dev بخصوص `scroll-behavior: smooth` بدون `data-scroll-behavior` على `<html>` — أُضيف الـattribute.
    - **⚠️ خطأ تشخيصي وقع أثناء الفحص النهائي الحي، يستاهل توثيق كدرس تشغيلي**: صفحة `/products/8` رجّعت 500 حقيقي مرة وحدة برسالة "Jest worker encountered 2 child process exceptions" (اسم مضلِّل — مالوش علاقة بـJest الاختباري، دي آلية الـworkers الداخلية لـwebpack/SWC بـNext.js). التشخيص الأول المحتمل كان "باگ حقيقي بالصفحة" — لكن الفحص الفعلي (`Get-CimInstance`) كشف **16 عملية node.exe و34 عملية chrome.exe شغّالين بالتوازي** (تراكم من عدة إعادة تشغيل لسيرفر التطوير + عدة وكلاء خلفيين يفتحون متصفح بنفس الوقت خلال جلسة طويلة)، منها **شجرة `npm run dev` كاملة يتيمة** (سيرفر قديم منسي شغّال بدون ما يخدم أي طلب). بعد تنظيف العمليات اليتيمة + إعادة تشغيل نظيفة، نفس الصفحة اشتغلت فورًا بصفر أخطاء. **الدرس**: أي عطل غريب/غير متكرر وقت جلسة طويلة فيها إعادة تشغيلات متعددة لسيرفر Next.js + وكلاء خلفيين كتار يفتحوا متصفح، تحقّق من عدد عمليات `node`/`chrome` الشغّالة **قبل** افتراض باگ بالكود — تراكم عمليات يتيمة على Windows شائع لو ما وقفت كل سيرفر تطوير صراحة بـPID محدد قبل إطلاق واحد جديد.
    - **مراجعة أمنية (`security-review` المدمج، لا سكيل خاص محلي — استُدعي عبر `Skill` مباشرة) لكامل diff الفرع: صفر ثغرات.** الفرع بالكامل CSS/className/JSX بصري + مكوّنات UI جديدة عرضية بالكامل بدون أي منطق ديناميكي — صفر تدفقات بيانات جديدة، صفر منطق مصادقة جديد، صفر sinks غير آمنة (`dangerouslySetInnerHTML` الوحيد بالمشروع، بصفحة تفاصيل المنتج لـJSON-LD، لم يُلمَس). `middleware.ts` (أكثر ملف حساس بالـdiff) تغييره الوحيد كان **تضييق** الـCSP (حذف سماح Google Fonts الخارجي بعد التحول لخط مستضاف محليًا) — تحسين، مش ثغرة.
    - **بنية الـcommits عند دمج فرع ضخم متراكم بدون تاريخ حقيقي (شهور من العمل، صفر commits وسيطة)**: تعذّر فصل دقيق على مستوى الأسطر بين "التصميم الأصلي" و"إصلاحات هالجلسة" للملفات اللي لمستها الجلستان معًا (`globals.css`, `layout.tsx`) بدون مخاطرة `git add -p` يدوي عالي الخطورة لملفات كبيرة — بدل هيك، اعتُمد **تجميع على مستوى الملف** (كل ملف بالكامل بأنسب commit حسب الاهتمام الغالب فيه)، مع ذكر التغييرات الثانوية الصغيرة (زي `data-scroll-behavior` أو تغميق `--muted`) بنص رسالة الـcommit صراحة. النتيجة: 5 commits منطقية (tooling deps → خط STC → design tokens/primitives → صفحات المتجر → لوحة الأدمن) + commit توثيق أخير، بدل commit ضخم واحد أو تفكيك زائف يوهم بتاريخ دقيق غير موجود فعليًا. **درس لأي دمج مستقبلي لفرع مشابه**: التجميع حسب الملف/المنطقة كافٍ وصادق، ما يستاهل مخاطرة تعديل تاريخي مصطنع.
    - **الحالة النهائية**: `design/spatial-ui-light` اندمج بـ`main` (٥ commits + commit توثيق) ورُفع لـ`origin/main`، والفرع المحلي انحذف بعد التأكد من `git fetch` إن `main`/`origin/main` على نفس الـcommit بالضبط.
