# تدقيق UI/UX — Billy Store

تدقيق تصميم/تجربة مستخدم عبر سكيل `ui-ux-pro-max`، لخمس صفحات: الرئيسية، المنتجات،
تفاصيل المنتج، لوحة تحكم الأدمن، وتسجيل دخول الأدمن. **استكشاف وتقييم فقط — صفر تعديل
كود.** كل رقم تباين (contrast ratio) بهذا التقرير محسوب فعلياً بصيغة WCAG relative
luminance من قيم الألوان الفعلية بـ `tailwind.config.js`، مش تقدير بصري.

---

## معايير السكيل المُستخدَمة فعلياً

القيم المرجعية التالية من سكيل `ui-ux-pro-max` (Quick Reference، فئات Accessibility
و Touch & Interaction و Style Selection) هي الأساس اللي بُني عليه كل تصنيف بهذا التقرير:

| المعيار | القيمة المرجعية | المصدر بالسكيل |
|---|---|---|
| تباين النص العادي (WCAG AA) | **4.5:1** كحد أدنى | `color-contrast`, `contrast-readability` |
| تباين النص الكبير (18px+ أو 14px bold+) | **3:1** كحد أدنى | `color-contrast` |
| الحد الأدنى لحجم زر اللمس | **44×44px** (Apple) / 48×48dp (Material) | `touch-target-size` |
| أقل مسافة بين أزرار لمس متجاورة | **8px** | `touch-spacing` |
| أيقونات هيكلية (نقل، أزرار نظام) | SVG وليس إيموجي | `no-emoji-icons` (فئة Style Selection، تأثير HIGH) |
| رسائل الخطأ | يجب إعلانها لقارئ الشاشة عبر `aria-live` أو `role="alert"`، مش بصري فقط | `error-messages` (Accessibility، severity High) |
| ربط الـ label بالـ input | عبر `for`/`id` أو تغليف الـ input، مش تقارب بصري فقط | `form-labels` (Accessibility، severity High) |
| الحركة/الأنيميشن | احترام `prefers-reduced-motion` | `reduced-motion` |
| تسلسل العناوين | `h1→h6` متسلسل بدون تخطي مستوى | `heading-hierarchy` |

---

## 1) الصفحة الرئيسية — [src/app/page.tsx](src/app/page.tsx)

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 1.1 | [page.tsx:96-103](src/app/page.tsx#L96) و[صفحات أخرى](src/components/layout/Footer.tsx#L70) | نص إحصائيات ("7+ سنوات خبرة") وسطر رموز الفوتر (`△ ○ ✕ □`) يستخدمون `text-slate-500`/`text-slate-600` — تباين محسوب فعلياً: `slate-500` على خلفية `dark` (#050A14) = **4.16:1**، و`slate-600` = **2.61:1**. كلاهما دون حد WCAG AA (4.5:1) للنص العادي؛ `slate-600` فشل حتى حد النص الكبير (3:1). | **حرجة** (نمط متكرر بكل الصفحات، مو مكان واحد) |
| 1.2 | [page.tsx:127-140](src/app/page.tsx#L127) | **✅ مُعالَج جزئياً** — 🏆⚡ (بطاقة "من نحن") و💸🔒📱💬 (بطاقات "لماذا نحن") استُبدلت بأيقونات `lucide-react` (`Trophy`, `Zap`, `HandCoins`, `Lock`, `Smartphone`, `MessageCircle`) بحجم مطابق للإيموجي الأصلي (`size={30}`/`size={48}` حسب حجم الكارت — استثناء موثّق عن قاعدة 16/20 لأن هذي أيقونات كبيرة مستقلة، مش مجاورة لنص). 💰🎮 بنفس المصفوفة **لسا كإيموجي** — 💰 خارج نطاق أي دفعة تنفيذ حالياً (فجوة غير مقصودة بالتخطيط)، و🎮 مؤجل عمداً لتفادي ازدواجية مع تنفيذه بصفحات المنتجات (بند 3.2). | **✅ مُنفّذ (جزئياً) — الباقي: فجوة 💰 وتأجيل 🎮** |
| 1.3 | [page.tsx:45-52](src/app/page.tsx#L45)، [globals.css:63-64](src/app/globals.css#L63) | رموز عائمة زخرفية (`animate-float`) تلف لا نهائياً (`infinite`) على الصفحة الرئيسية بدون أي `@media (prefers-reduced-motion: reduce)` بملف `globals.css` — يخالف `reduced-motion`. نفس الأنيميشن `animate-glow`/`animate-float` مستخدم بصفحة تفاصيل المنتج أيضاً. | **متوسطة** |
| 1.4 | [page.tsx:161](src/app/page.tsx#L161) و[products/page.tsx:172](src/app/products/page.tsx#L172) | كلاس `grid-cols-2 sm:grid-cols-2 lg:grid-cols-3` — قيمة `sm:grid-cols-2` مكررة بلا تأثير فعلي (نفس قيمة الأساس). لا يكسر شي، بس dead code بالتناسق البصري المقصود بين breakpoints. | **تحسين اختياري** |

---

## 2) صفحة المنتجات — [src/app/products/page.tsx](src/app/products/page.tsx)

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 2.1 | [products/page.tsx:140-144](src/app/products/page.tsx#L140) | أزرار الترتيب (الافتراضي/الأرخص/الأغلى/أكبر خصم) بحجم `text-xs px-3 py-1.5` — ارتفاع فعلي تقريبي ~28px، دون حد اللمس الأدنى 44px. هاي أزرار أساسية بصفحة يتصفحها الزوار على الموبايل بشكل رئيسي (سوق سعودي، تصفح جوال غالباً). | **متوسطة** |
| 2.2 | [products/page.tsx:157](src/app/products/page.tsx#L157) | أيقونة البحث `absolute left-4` — بحث نصي RTL، الأيقونة بالطرف "النهاية" منطقياً (يسار بـRTL)، هذا سليم ومتسق مع باقي الموقع (نفس النمط مستخدم لواحقات الأسعار بلوحة الأدمن) — **ملاحظة إيجابية**، مش عيب. |  — |
| 2.3 | [products/page.tsx:172, 192](src/app/products/page.tsx#L172) | نفس ملاحظة `sm:grid-cols-2` الزائدة من القسم أعلاه. | **تحسين اختياري** |
| 2.4 | [ProductCard.tsx:157-159](src/components/ui/ProductCard.tsx#L157) | زر "تفاصيل ←" مخفي على الموبايل (`hidden sm:block`) — على الشاشات الصغيرة ما فيه أي CTA نصي داخل البطاقة، الاعتماد الكامل على إنه كل الكرت رابط (`<Link>` يغلف الكرت بأكمله بسطر 97) — هذا سليم فعلياً (الكرت كامل قابل للنقر)، بس التسمية النصية للـ CTA غايبة بصرياً بالموبايل وهو الاستخدام الأساسي. | **تحسين اختياري** |

---

## 3) صفحة تفاصيل المنتج — [src/app/products/[id]/page.tsx](src/app/products/[id]/page.tsx)

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 3.1 | [products/[id]/page.tsx:102, 156, 193, 198](src/app/products/[id]/page.tsx#L102) | نفس مشكلة تباين `text-slate-500` (breadcrumb، عداد المشاهدات، "تسليم رقمي فوري"، رابط "العودة للمنتجات") — **4.16:1**، دون حد AA. رابط "العودة للمنتجات" بالتحديد نص تفاعلي (رابط) بنفس اللون الضعيف التباين. | **حرجة** |
| 3.2 | [products/[id]/page.tsx:129, 153](src/app/products/[id]/page.tsx#L129) | **✅ مُعالَج بالكامل** — 🎮 (fallback صورة المنتج) استُبدل بـ`Gamepad2` بحجم `size={72}` (مطابق لـ`text-7xl` الأصلي، استثناء موثّق)، و⭐ (شارة "منتج مميز") استُبدل بـ`Star size={16}` (سياق نص عادي). نفس المعالجة طُبّقت أيضاً على `products/page.tsx` (فئات التصفح، شارة 🔥 التخفيض، حالة "لا نتائج") وعلى `ProductCard.tsx` (fallback الصورة وشارة "مميز") لأنها نفس الرمزين بالضبط. PS+ (نص، مش إيموجي) بقي كما هو، خارج نطاق `no-emoji-icons`. | **✅ مُنفّذ بالكامل** |
| 3.3 | [products/[id]/page.tsx:110](src/app/products/[id]/page.tsx#L110) | تخطيط `grid-cols-1 lg:grid-cols-2` — قفزة مباشرة من عمود واحد لعمودين عند `lg` (1024px) بدون نقطة توسط بمقاس تابلت (`md`، 768px) — ع الشاشات المتوسطة (768-1023px) الصورة والتفاصيل تبقى عمود واحد طويل بدل استغلال العرض المتاح. | **تحسين اختياري** |
| 3.4 | [products/[id]/page.tsx:163](src/app/products/[id]/page.tsx#L163) | `<h1>` لاسم المنتج موجود وصحيح — تسلسل عناوين الصفحة سليم (h1 ثم h3 للوصف كعناوين فرعية داخل بطاقات). **ملاحظة إيجابية**. |  — |

---

## 4) تسجيل دخول الأدمن — [src/app/admin/login/page.tsx](src/app/admin/login/page.tsx)

*(حسب طلبك: تركيز خاص على ربط label↔input، إعلان رسائل الخطأ، وحجم زر الدخول)*

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 4.1 | [admin/login/page.tsx:50-54](src/app/admin/login/page.tsx#L50) | رسالة الخطأ (`{error && <div>...</div>}`) نص بصري فقط — بدون `role="alert"` ولا `aria-live`. مستخدم قارئ شاشة لن يُعلَم بفشل تسجيل الدخول إطلاقاً إلا لو انتقل يدوياً لموضع الرسالة. هذا يخالف مباشرة معيار السكيل `error-messages` (severity **High** بقاعدة بيانات السكيل). | **حرجة** |
| 4.2 | [admin/login/page.tsx:58, 69](src/app/admin/login/page.tsx#L58) | كل من `<label>اسم المستخدم</label>` و`<label>كلمة المرور</label>` نص مرئي فوق الحقل مباشرة (تحقيق جزئي لـ `input-labels`) — لكن بدون `htmlFor` يربطها بـ `id` الحقل المقابل (ولا الحقل ملفوف داخل الـ label). قارئ الشاشة عند التركيز على الحقل ما رح يعلن اسم الـ label تلقائياً — يخالف `form-labels`. | **حرجة** |
| 4.3 | [admin/login/page.tsx:79-82](src/app/admin/login/page.tsx#L79) | زر "دخول": `w-full py-4 text-lg` — ارتفاع فعلي يتجاوز 44px بمريح (تقريباً 60px+). **يحقق معيار حجم اللمس بالكامل — ملاحظة إيجابية**، لا حاجة لتعديل. |  — |
| 4.4 | [admin/login/page.tsx:63, 74](src/app/admin/login/page.tsx#L63) | الحقول تعتمد على `placeholder` ("admin"، "••••••••") بالإضافة للـ label المرئي — هذا سليم (مو placeholder-only)، بس لاحظ إنه `type="password"` بدون أي زر إظهار/إخفاء كلمة المرور (`password-toggle` بالسكيل). | **تحسين اختياري** |

---

## 5) لوحة تحكم الأدمن — [src/app/admin/dashboard/page.tsx](src/app/admin/dashboard/page.tsx)

> **بخصوص حجم الملف (~1270 سطر):** حسب اتفاقنا، أي ملاحظة عن الطول/التعقيد مصنّفة
> "تحسين اختياري" حصراً وتقتصر على تصميم بصري بحت — بدون أي اقتراح بنيوي أو لمس منطق.

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 5.1 | [dashboard/page.tsx:390-408](src/app/admin/dashboard/page.tsx#L390) | الهيدر الكامل ("Billy Store"، "لوحة التحكم") مبني بـ `<span>`/`<p>` فقط — **لا يوجد أي `<h1>` بالصفحة كاملة**. أول عنوان حقيقي هو `<h2>إدارة المنتجات</h2>` بسطر 601، متبوع مباشرة بعدة `<h3>` داخل البطاقات والمودالات. تخطي مستوى h1 بالكامل يخالف `heading-hierarchy` ويكسر تنقل قارئ الشاشة بالعناوين لصفحة إدارية كاملة. | **حرجة** |
| 5.2 | [dashboard/page.tsx:404-799 وأماكن أخرى كثيرة](src/app/admin/dashboard/page.tsx#L404) | **✅ مُعالَج (24 موقع)** — كل الأزرار الصغيرة (تسجيل خروج، تعديل/حذف بجدول المنتجات، تبديل حالة الخصم، تبويبات الإحصائيات، أزرار المودالات...) كُبّرت لـ`min-h-11` (44px). مفاتيح التبديل الأربعة (`w-12 h-6` بأسطر 781، 1107، 1155، 1253) عولجت بطريقة مختلفة: منطقة النقر (hit area) وسّعت لـ44×44px عبر `<button>` حاوٍ (`min-w-11 min-h-11`) بدون تغيير الحجم المرئي للمفتاح نفسه — الشكل البصري ثابت تماماً كما كان. **استثناء موثّق وباقٍ عن قصد**: ثلاثي أزرار قاعدة الخصم المستهدف (✏️/⏸▶/✕ بسطر 511-533) أُبقي على حجمه الأصلي `text-xs px-2 py-1` بدون تكبير — قيد مساحة حقيقي: الأزرار الثلاثة تتنافس مباشرة مع نص وصف القاعدة القابل للاقتطاع (`min-w-0`) بنفس الصف، وتكبيرها لـ44px لكل واحد يحتاج ~140px+ عرض غير متوفر بثبات على البطاقات الأضيق. خُفّف الأثر جزئياً برفع الفجوة بين الأزرار الثلاثة من `gap-1` (4px) لـ`gap-2` (8px) لمطابقة الحد الأدنى لمعيار `touch-spacing` بالسكيل، رغم بقاء حجم كل زر دون 44px. أولوية هذا الاستثناء عملياً منخفضة لأنه (أ) قسم إداري يُستخدم غالباً من حاسوب مكتبي بالماوس، و(ب) لا توجد بديل بصري بسيط يحقق 44px بدون التأثير على وضوح نص القاعدة. | **متوسطة → تحسين اختياري** (بعد المعالجة الجزئية) |
| 5.3 | [dashboard/page.tsx:769, 814, 897, 1005, 1133](src/app/admin/dashboard/page.tsx#L769) وكل عناوين المودالات | **✅ مُعالَج جزئياً (24 موقع)** — ✏️💾🎯🏷️🗑️📢📈📉⚠️📊✅❌✕ (الوظيفي بسطر 533) استُبدلت بأيقونات `lucide-react`. 📦 (كروت الإحصائيات وحالة الفراغ) عولج بحجم أكبر (`size={30}`/`size={48}`) كاستثناء موثّق لأنها أيقونات كبيرة مستقلة. رسالة `priceResult` (✅/❌ مدمجة بنص حالة ديناميكي) عولجت بتعديل الـrender فقط (فصل الأيقونة عن النص) بدون لمس منطق `handleBulkPrice`. **لسا متبقٍّ كإيموجي بنفس الملف (فجوات غير مقصودة بالتخطيط الأصلي، مش قرار تأجيل):** 💰 (سطور 906، 1026 — ما كان بأي دفعة تنفيذ)، 🎯 بقائمة تصنيف صفحة المنتجات (استخدام مختلف عن 🎯 اللي بلوحة الأدمن نفسها المُعالَج)، 🏆 بكرت الإحصائيات (سطر 418)، ⏸/▶ (رموز إيقاف/تشغيل، لم تُكتشف بالجرد الأصلي — نطاق يونيكود مختلف). **مؤجل عمداً** لتفادي ازدواجية مع بند 3.2: 🎮⭐ بكل مواضعها بهذا الملف (415-418 بالمصفوفة، 495، 711، 732، 874). **استثناءات تقنية دائمة** (لا يمكن حلها بـSVG إطلاقاً): 🔥 بسطر 783 (نص داخل `placeholder` — سمة HTML نصية بحتة، ما تقبل عناصر React)، و🎮/⭐ بسطر 1243-1244 (داخل وسم `<option>` — المتصفح ما بيرندر عناصر HTML/React جوا قوائم `<select>` الأصلية، بس نص). | **✅ مُنفّذ جزئياً — الباقي: فجوات 💰/🎯/🏆/⏸▶ + استثناءات تقنية دائمة** |
| 5.4 | [dashboard/page.tsx:57-77 (login) و1022-1151 (مودالات الخصم)](src/app/admin/dashboard/page.tsx#L1022) | نفس مشكلة `<label>` بدون `htmlFor`/`id` من قسم تسجيل الدخول، متكررة بكل حقول مودالات الخصم/السعر/إضافة المنتج (اسم العرض، نسبة الخصم، السعر من/إلى...). | **متوسطة** |
| 5.5 | [dashboard/page.tsx:709-710](src/app/admin/dashboard/page.tsx#L709) | جدول المنتجات (`<table>` بسطر 672) ملفوف بـ`overflow-x-auto` (سليم كحل احتياطي)، لكن التخطيط الافتراضي لهاي الشاشة يبقى جدول 6 أعمدة بدون نسخة بديلة أخف بصرياً للشاشات الضيقة (تابلت/جوال) — النتيجة تمرير أفقي إجباري بدل إعادة تدفق العناصر. | **تحسين اختياري** *(نطاق: تجميع بصري/spacing فقط، مش إعادة هيكلة)* |
| 5.6 | [dashboard/page.tsx:676-695, 1076-1084](src/app/admin/dashboard/page.tsx#L676) | صناديق الاختيار (checkboxes) بحجم `w-4 h-4` (16×16px) — دون حد اللمس الأدنى بكثير، خصوصاً بقائمة اختيار منتجات الخصم المستهدف (سطر 1076) اللي فيها checkbox لكل منتج بقائمة قابلة للسكرول. | **تحسين اختياري** *(سياق لوحة إدارية يُستخدم غالباً بالماوس)* |
| 5.7 | [dashboard/page.tsx:736-752](src/app/admin/dashboard/page.tsx#L736) | تأكيد الحذف مبني كتبديل نصي مكاني (الزر يتحول من "حذف" لـ"تأكيد"/"إلغاء" بنفس المكان) بدل نافذة تأكيد منفصلة — يحقق مبدأ `confirmation-dialogs` (فيه تأكيد فعلي قبل حذف نهائي) لكن بصرياً قليل الوضوح لإنه نفس موقع الزر يتغير معناه فجأة. | **تحسين اختياري** |
| 5.8 | كامل الصفحة (كثافة الأقسام) | الصفحة تحوي 8+ أقسام إدارة مختلفة (إحصائيات، خصم عام، خصم مستهدف، إعلان، جدول منتجات، واستيراد/تصدير) بمسافات `mb-6`/`mb-8` متقاربة بين بعضها بدون فواصل بصرية أقوى (خط فاصل أوضح أو تجميع بعنوان قسم موحّد) — يصعّب المسح البصري السريع بين الأقسام. **ملاحظة تصميم بصري بحت، بدون أي اقتراح بنيوي.** | **تحسين اختياري** |

---

## 6) ملاحظات مشتركة عبر الموقع (Cross-cutting)

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 6.1 | [Navbar.tsx:101-105](src/components/layout/Navbar.tsx#L101) | زر قائمة الموبايل (hamburger) — `<button onClick={...} className="md:hidden text-white p-2">` بدون `aria-label` ولا `aria-expanded`. المحتوى الداخلي 3 خطوط `<div>` بدون نص بديل — قارئ الشاشة يعلن "button" بلا أي وصف لوظيفته أو حالته (مفتوح/مغلق). يخالف `aria-labels` (severity High بالسكيل). | **حرجة** |
| 6.2 | [Navbar.tsx:87-98](src/components/layout/Navbar.tsx#L87) | أزرار التواصل الاجتماعي (واتساب/إنستقرام/تيليجرام) بحجم `w-9 h-9` (36×36px) — دون حد اللمس 44px، وتعتمد فقط على `title="واتساب"` للوصف (غير مضمون الإعلان لكل قارئات الشاشة) بدل `aria-label`. | **متوسطة** |
| 6.3 | جميع الصفحات الخمس | لا يوجد أي `htmlFor`/`id` لربط `<label>` بـ`<input>` بكامل المشروع (تحقق: `aria-label`/`role=`/`htmlFor` يظهر مرة واحدة فقط بكل `src/`، بملف غير متعلق). النمط سليم بصرياً (label فوق الحقل دائماً) لكنه غير مرتبط برمجياً بأي مكان. | **حرجة** (نمط شامل، مش استثناء) |
| 6.4 | [globals.css](src/app/globals.css) كامل الملف | صفر أي `@media (prefers-reduced-motion: reduce)` بكامل ملف الأنيميشن — كل الحركات (`float`، `glow`، `slideUp`، `fadeIn`) تعمل دائماً بغض النظر عن تفضيل المستخدم بنظام التشغيل. | **متوسطة** |
| 6.5 | [Navbar.tsx:76](src/components/layout/Navbar.tsx#L76)، [Footer.tsx:11](src/components/layout/Footer.tsx#L11) | شعار الموقع (`<img src="/logo.jpg">`) بدل `next/image` — موثّقة أصلاً بـ`CLAUDE.md` (gotcha #4) كتحذير ESLint معروف وخارج نطاق التنظيف الحالي؛ أذكرها هون فقط لإنها تؤثر أيضاً على أداء تحميل الصورة (لا layout shift محسوب، لا lazy loading تلقائي) لا على الوصولية (الـ`alt` موجود وصحيح بالحالتين). | **تحسين اختياري** *(معروفة مسبقاً، مش اكتشاف جديد)* |

---

## ملخص الأولويات

| الأولوية | العدد | أبرز الأمثلة |
|---|---|---|
| **حرجة** | 6 | تباين `slate-500/600` المتكرر، رسالة خطأ تسجيل الدخول غير معلَنة لقارئ الشاشة، labels بدون ربط برمجي بكل الموقع، غياب `<h1>` بلوحة الأدمن، زر قائمة الموبايل بدون `aria-label` |
| **متوسطة** | 8 | إيموجي كأيقونات أساسية (كل الصفحات، وبكثافة أعلى بلوحة الأدمن)، أزرار صغيرة دون حد اللمس (منتجات + لوحة أدمن)، غياب `prefers-reduced-motion`، أزرار تواصل اجتماعي 36px |
| **تحسين اختياري** | 8 | كثافة أقسام لوحة الأدمن (بصري بحت)، جدول منتجات بدون بديل للشاشات الضيقة، checkboxes صغيرة، تأكيد حذف نصي مكاني، تكرار `sm:grid-cols-2` بلا تأثير، غياب زر إظهار كلمة المرور |

**ملاحظتان إيجابيتان** تستاهل الذكر: زر "دخول" بصفحة تسجيل الدخول يحقق حجم اللمس بالكامل، وصفحة تفاصيل المنتج فيها تسلسل عناوين (`h1`→`h3`) سليم — عكس لوحة الأدمن.

---

## استثناء موثّق: الرموز الزخرفية العائمة (△○✕□)

الرموز العائمة الأربعة بخلفية الهيرو ([page.tsx:45-52](src/app/page.tsx#L45))، خلفية صورة تفاصيل المنتج ([products/[id]/page.tsx:140](src/app/products/[id]/page.tsx#L140))، وسطر الفوتر ([Footer.tsx:70](src/components/layout/Footer.tsx#L70)) **استُثنيت عمداً من تنفيذ `no-emoji-icons`** ولن تتحول لـSVG. هذي رموز زخرفية بحتة (نقش بصري متكرر بالخلفية) مش أيقونات وظيفية تنقل معنى أو تُستخدم كعنصر تحكم — قرار هوية بصرية (brand pattern)، خارج نطاق معيار `no-emoji-icons` اللي يستهدف الأيقونات الهيكلية (تنقل، أزرار، حالات) حصراً.

---

## الخطوة الجاية
بانتظار قرارك: أي الملاحظات (لو أي) ننفذ، وبأي ترتيب. لا وجود لأي اقتراح إعادة هيكلة
كود أو تقسيم ملفات بهذا التقرير — فقط تصميم/UX/وصولية حسب المتفق عليه.
