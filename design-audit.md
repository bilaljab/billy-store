# تدقيق UI/UX — Billy Store

تدقيق تصميم/تجربة مستخدم عبر سكيل `ui-ux-pro-max`، لخمس صفحات: الرئيسية، المنتجات،
تفاصيل المنتج، لوحة تحكم الأدمن، وتسجيل دخول الأدمن. **استكشاف وتقييم فقط — صفر تعديل
كود.** كل رقم تباين (contrast ratio) بهذا التقرير محسوب فعلياً بصيغة WCAG relative
luminance من قيم الألوان الفعلية بـ `tailwind.config.js`، مش تقدير بصري.

---

## معايير السكيل المُستخدَمة فعلياً

القيم المرجعية التالية من سكيل `ui-ux-pro-max` (Quick Reference، فئات Accessibility
و Touch & Interaction و Style Selection) هي الأساس اللي بُني عليه كل تصنيف بهذا التقرير:

| المعيار | القيمة المرجعية | المصدر بالسكيل |
|---|---|---|
| تباين النص العادي (WCAG AA) | **4.5:1** كحد أدنى | `color-contrast`, `contrast-readability` |
| تباين النص الكبير (18px+ أو 14px bold+) | **3:1** كحد أدنى | `color-contrast` |
| الحد الأدنى لحجم زر اللمس | **44×44px** (Apple) / 48×48dp (Material) | `touch-target-size` |
| أقل مسافة بين أزرار لمس متجاورة | **8px** | `touch-spacing` |
| أيقونات هيكلية (نقل، أزرار نظام) | SVG وليس إيموجي | `no-emoji-icons` (فئة Style Selection، تأثير HIGH) |
| رسائل الخطأ | يجب إعلانها لقارئ الشاشة عبر `aria-live` أو `role="alert"`، مش بصري فقط | `error-messages` (Accessibility، severity High) |
| ربط الـ label بالـ input | عبر `for`/`id` أو تغليف الـ input، مش تقارب بصري فقط | `form-labels` (Accessibility، severity High) |
| الحركة/الأنيميشن | احترام `prefers-reduced-motion` | `reduced-motion` |
| تسلسل العناوين | `h1→h6` متسلسل بدون تخطي مستوى | `heading-hierarchy` |

---

## 1) الصفحة الرئيسية — [src/app/page.tsx](src/app/page.tsx)

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 1.1 | [page.tsx:96-103](src/app/page.tsx#L96) و[صفحات أخرى](src/components/layout/Footer.tsx#L70) | **✅ مُنفّذ** — استُبدل `text-slate-500`/`text-slate-600` بتوكن جديد `text-muted` (#8492a8، مضاف بـ`tailwind.config.js`) عبر كل الصفحات الست اللي كان فيها النمط (45 موقع). تباين محسوب فعلياً بعد التعديل: **6.28:1** على `dark`، **5.75:1** على `dark-card`، **5.13:1** على `dark-border` — كلها فوق حد WCAG AA (4.5:1). | **✅ مُنفّذ** |
| 1.2 | [page.tsx:127-140](src/app/page.tsx#L127) | **✅ مُعالَج جزئياً** — 🏆⚡ (بطاقة "من نحن") و💸🔒📱💬 (بطاقات "لماذا نحن") استُبدلت بأيقونات `lucide-react` (`Trophy`, `Zap`, `HandCoins`, `Lock`, `Smartphone`, `MessageCircle`) بحجم مطابق للإيموجي الأصلي (`size={30}`/`size={48}` حسب حجم الكارت — استثناء موثّق عن قاعدة 16/20 لأن هذي أيقونات كبيرة مستقلة، مش مجاورة لنص). 💰🎮 بنفس المصفوفة **لسا كإيموجي** — 💰 خارج نطاق أي دفعة تنفيذ حالياً (فجوة غير مقصودة بالتخطيط)، و🎮 مؤجل عمداً لتفادي ازدواجية مع تنفيذه بصفحات المنتجات (بند 3.2). | **✅ مُنفّذ (جزئياً) — الباقي: فجوة 💰 وتأجيل 🎮** |
| 1.3 | [page.tsx:45-52](src/app/page.tsx#L45)، [globals.css:63-64](src/app/globals.css#L63) | **✅ مُنفّذ** — أُضيف `@media (prefers-reduced-motion: reduce)` بـ`globals.css` يلغي `animation`/`transition` لكل من `animate-float`، `animate-glow`، `animate-slide-up`، `animate-fade-in`، `.ps-btn`، `.card-hover`، ويحوّل `scroll-behavior` لـ`auto`. | **✅ مُنفّذ** |
| 1.4 | [page.tsx:161](src/app/page.tsx#L161) و[products/page.tsx:172](src/app/products/page.tsx#L172) | **✅ مُنفّذ** — حُذفت `sm:grid-cols-2` الزائدة من كلا الموقعين. | **✅ مُنفّذ** |

---

## 2) صفحة المنتجات — [src/app/products/page.tsx](src/app/products/page.tsx)

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 2.1 | [products/page.tsx:140-144](src/app/products/page.tsx#L140) | **✅ مُنفّذ** — الأزرار الأربعة كُبّرت لـ`min-h-11` (44px) عبر `inline-flex items-center justify-center`. | **✅ مُنفّذ** |
| 2.2 | [products/page.tsx:157](src/app/products/page.tsx#L157) | أيقونة البحث `absolute left-4` — بحث نصي RTL، الأيقونة بالطرف "النهاية" منطقياً (يسار بـRTL)، هذا سليم ومتسق مع باقي الموقع (نفس النمط مستخدم لواحقات الأسعار بلوحة الأدمن) — **ملاحظة إيجابية**، مش عيب. |  — |
| 2.3 | [products/page.tsx:172, 192](src/app/products/page.tsx#L172) | **✅ مُنفّذ** — حُذفت `sm:grid-cols-2` الزائدة من كلا الموقعين. | **✅ مُنفّذ** |
| 2.4 | [ProductCard.tsx:157-159](src/components/ui/ProductCard.tsx#L157) | **✅ مُنفّذ** — حُذف `hidden sm:block`، نص "تفاصيل ←" ظاهر الآن بكل المقاسات. | **✅ مُنفّذ** |

---

## 3) صفحة تفاصيل المنتج — [src/app/products/[id]/page.tsx](src/app/products/[id]/page.tsx)

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 3.1 | [products/[id]/page.tsx:102, 156, 193, 198](src/app/products/[id]/page.tsx#L102) | **✅ مُنفّذ** — نفس معالجة البند 1.1، استُبدل بـ`text-muted` (6.28:1+). | **✅ مُنفّذ** |
| 3.2 | [products/[id]/page.tsx:129, 153](src/app/products/[id]/page.tsx#L129) | **✅ مُعالَج بالكامل** — 🎮 (fallback صورة المنتج) استُبدل بـ`Gamepad2` بحجم `size={72}` (مطابق لـ`text-7xl` الأصلي، استثناء موثّق)، و⭐ (شارة "منتج مميز") استُبدل بـ`Star size={16}` (سياق نص عادي). نفس المعالجة طُبّقت أيضاً على `products/page.tsx` (فئات التصفح، شارة 🔥 التخفيض، حالة "لا نتائج") وعلى `ProductCard.tsx` (fallback الصورة وشارة "مميز") لأنها نفس الرمزين بالضبط. PS+ (نص، مش إيموجي) بقي كما هو، خارج نطاق `no-emoji-icons`. | **✅ مُنفّذ بالكامل** |
| 3.3 | [products/[id]/page.tsx:110](src/app/products/[id]/page.tsx#L110) | **✅ مُنفّذ** — التخطيط صار `grid-cols-1 md:grid-cols-2` (768px بدل 1024px). | **✅ مُنفّذ** |
| 3.4 | [products/[id]/page.tsx:163](src/app/products/[id]/page.tsx#L163) | `<h1>` لاسم المنتج موجود وصحيح — تسلسل عناوين الصفحة سليم (h1 ثم h3 للوصف كعناوين فرعية داخل بطاقات). **ملاحظة إيجابية**. |  — |

---

## 4) تسجيل دخول الأدمن — [src/app/admin/login/page.tsx](src/app/admin/login/page.tsx)

*(حسب طلبك: تركيز خاص على ربط label↔input، إعلان رسائل الخطأ، وحجم زر الدخول)*

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 4.1 | [admin/login/page.tsx:50-54](src/app/admin/login/page.tsx#L50) | **✅ مُنفّذ** — أُضيف `role="alert"` لعنصر رسالة الخطأ. | **✅ مُنفّذ** |
| 4.2 | [admin/login/page.tsx:58, 69](src/app/admin/login/page.tsx#L58) | **✅ مُنفّذ** — أُضيف `htmlFor`/`id` (`login-username`، `login-password`) يربط كل `label` بحقله. | **✅ مُنفّذ** |
| 4.3 | [admin/login/page.tsx:79-82](src/app/admin/login/page.tsx#L79) | زر "دخول": `w-full py-4 text-lg` — ارتفاع فعلي يتجاوز 44px بمريح (تقريباً 60px+). **يحقق معيار حجم اللمس بالكامل — ملاحظة إيجابية**، لا حاجة لتعديل. |  — |
| 4.4 | [admin/login/page.tsx:63, 74](src/app/admin/login/page.tsx#L63) | **✅ مُنفّذ** — أُضيف زر إظهار/إخفاء كلمة المرور (أيقونتي `Eye`/`EyeOff` من `lucide-react`) بمنطقة نقر 44×44px. | **✅ مُنفّذ** |

---

## 5) لوحة تحكم الأدمن — [src/app/admin/dashboard/page.tsx](src/app/admin/dashboard/page.tsx)

> **بخصوص حجم الملف (~1270 سطر):** حسب اتفاقنا، أي ملاحظة عن الطول/التعقيد مصنّفة
> "تحسين اختياري" حصراً وتقتصر على تصميم بصري بحت — بدون أي اقتراح بنيوي أو لمس منطق.

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 5.1 | [dashboard/page.tsx:390-408](src/app/admin/dashboard/page.tsx#L390) | **✅ مُنفّذ** — "Billy Store" صار `<h1>` (نفس الشكل البصري، مع `aria-label="لوحة تحكم Billy Store"` لاسم أكثر وصفية لقارئ الشاشة). | **✅ مُنفّذ** |
| 5.2 | [dashboard/page.tsx:404-799 وأماكن أخرى كثيرة](src/app/admin/dashboard/page.tsx#L404) | **✅ مُعالَج (24 موقع)** — كل الأزرار الصغيرة (تسجيل خروج، تعديل/حذف بجدول المنتجات، تبديل حالة الخصم، تبويبات الإحصائيات، أزرار المودالات...) كُبّرت لـ`min-h-11` (44px). مفاتيح التبديل الأربعة (`w-12 h-6` بأسطر 781، 1107، 1155، 1253) عولجت بطريقة مختلفة: منطقة النقر (hit area) وسّعت لـ44×44px عبر `<button>` حاوٍ (`min-w-11 min-h-11`) بدون تغيير الحجم المرئي للمفتاح نفسه — الشكل البصري ثابت تماماً كما كان. **استثناء موثّق وباقٍ عن قصد**: ثلاثي أزرار قاعدة الخصم المستهدف (✏️/⏸▶/✕ بسطر 511-533) أُبقي على حجمه الأصلي `text-xs px-2 py-1` بدون تكبير — قيد مساحة حقيقي: الأزرار الثلاثة تتنافس مباشرة مع نص وصف القاعدة القابل للاقتطاع (`min-w-0`) بنفس الصف، وتكبيرها لـ44px لكل واحد يحتاج ~140px+ عرض غير متوفر بثبات على البطاقات الأضيق. خُفّف الأثر جزئياً برفع الفجوة بين الأزرار الثلاثة من `gap-1` (4px) لـ`gap-2` (8px) لمطابقة الحد الأدنى لمعيار `touch-spacing` بالسكيل، رغم بقاء حجم كل زر دون 44px. أولوية هذا الاستثناء عملياً منخفضة لأنه (أ) قسم إداري يُستخدم غالباً من حاسوب مكتبي بالماوس، و(ب) لا توجد بديل بصري بسيط يحقق 44px بدون التأثير على وضوح نص القاعدة. | **متوسطة → تحسين اختياري** (بعد المعالجة الجزئية) |
| 5.3 | [dashboard/page.tsx:769, 814, 897, 1005, 1133](src/app/admin/dashboard/page.tsx#L769) وكل عناوين المودالات | **✅ مُعالَج جزئياً (24 موقع)** — ✏️💾🎯🏷️🗑️📢📈📉⚠️📊✅❌✕ (الوظيفي بسطر 533) استُبدلت بأيقونات `lucide-react`. 📦 (كروت الإحصائيات وحالة الفراغ) عولج بحجم أكبر (`size={30}`/`size={48}`) كاستثناء موثّق لأنها أيقونات كبيرة مستقلة. رسالة `priceResult` (✅/❌ مدمجة بنص حالة ديناميكي) عولجت بتعديل الـrender فقط (فصل الأيقونة عن النص) بدون لمس منطق `handleBulkPrice`. **لسا متبقٍّ كإيموجي بنفس الملف (فجوات غير مقصودة بالتخطيط الأصلي، مش قرار تأجيل):** 💰 (سطور 906، 1026 — ما كان بأي دفعة تنفيذ)، 🎯 بقائمة تصنيف صفحة المنتجات (استخدام مختلف عن 🎯 اللي بلوحة الأدمن نفسها المُعالَج)، 🏆 بكرت الإحصائيات (سطر 418)، ⏸/▶ (رموز إيقاف/تشغيل، لم تُكتشف بالجرد الأصلي — نطاق يونيكود مختلف). **مؤجل عمداً** لتفادي ازدواجية مع بند 3.2: 🎮⭐ بكل مواضعها بهذا الملف (415-418 بالمصفوفة، 495، 711، 732، 874). **استثناءات تقنية دائمة** (لا يمكن حلها بـSVG إطلاقاً): 🔥 بسطر 783 (نص داخل `placeholder` — سمة HTML نصية بحتة، ما تقبل عناصر React)، و🎮/⭐ بسطر 1243-1244 (داخل وسم `<option>` — المتصفح ما بيرندر عناصر HTML/React جوا قوائم `<select>` الأصلية، بس نص). | **✅ مُنفّذ جزئياً — الباقي: فجوات 💰/🎯/🏆/⏸▶ + استثناءات تقنية دائمة** |
| 5.4 | [dashboard/page.tsx:57-77 (login) و1022-1151 (مودالات الخصم)](src/app/admin/dashboard/page.tsx#L1022) | **✅ مُنفّذ** — كل labels مودالات الإعلان/الخصم/السعر/إضافة المنتج ربطت بـ`htmlFor`/`id` (13 حقل). | **✅ مُنفّذ** |
| 5.5 | [dashboard/page.tsx:709-710](src/app/admin/dashboard/page.tsx#L709) | **✅ مُنفّذ (حل حقيقي، مش هروب من القيد)** — عمودي "الفئة" و"مميز" (الأقل أهمية للمسح السريع) صاروا `hidden sm:table-cell`، فيبقى بالموبايل بس 4 أعمدة أساسية (تحديد، منتج، سعر، إجراءات) بدل 6 — تقليل حقيقي لحاجة التمرير الأفقي، بدون أي تغيير ببنية الـ`<table>` أو المنطق. | **✅ مُنفّذ** |
| 5.6 | [dashboard/page.tsx:676-695, 1076-1084](src/app/admin/dashboard/page.tsx#L676) | **✅ مُنفّذ** — الثلاث checkboxes كُبّرت من `w-4 h-4` (16px) لـ`w-5 h-5` (20px). | **✅ مُنفّذ** |
| 5.7 | [dashboard/page.tsx:736-752](src/app/admin/dashboard/page.tsx#L736) | **✅ مُنفّذ** — استُبدل التبديل النصي المكاني بمودال حقيقي (overlay + بطاقة مركزية، نفس نمط باقي مودالات الصفحة) يعرض اسم المنتج ويطلب تأكيد صريح؛ يعيد استخدام نفس `deleteConfirm` state و`handleDelete` بدون أي تغيير منطقي. | **✅ مُنفّذ** |
| 5.8 | كامل الصفحة (كثافة الأقسام) | **✅ مُنفّذ** — رُفعت المسافة بين أقسام الإحصائيات/الخصم/الخصم المستهدف/الإعلان من `mb-6` (24px) لـ`mb-8` (32px) بشكل موحّد لفصل بصري أوضح بين الأقسام، بدون أي تغيير بنيوي. | **✅ مُنفّذ** |

---

## 6) ملاحظات مشتركة عبر الموقع (Cross-cutting)

| # | الموقع | المشكلة | الأولوية |
|---|---|---|---|
| 6.1 | [Navbar.tsx:101-105](src/components/layout/Navbar.tsx#L101) | **✅ مُنفّذ** — أُضيف `aria-label` ديناميكي ("فتح القائمة"/"إغلاق القائمة") و`aria-expanded`. | **✅ مُنفّذ** |
| 6.2 | [Navbar.tsx:87-98](src/components/layout/Navbar.tsx#L87) | **✅ مُنفّذ** — الأزرار الثلاثة كُبّرت لـ`w-11 h-11` (44px) واستُبدل `title` بـ`aria-label`. | **✅ مُنفّذ** |
| 6.3 | جميع الصفحات الخمس | **✅ مُنفّذ** — ربط `htmlFor`/`id` أُضيف لكل label بمعنى الكلمة (login + كل مودالات لوحة الأدمن، 15 حقل إجمالاً). Labels الجماعية (فوق مجموعات أزرار/checkboxes بدون input مفرد) استُثنيت عمداً — مش لها input واحد تربط فيه. | **✅ مُنفّذ** |
| 6.4 | [globals.css](src/app/globals.css) كامل الملف | **✅ مُنفّذ** — نفس معالجة البند 1.3 (`@media (prefers-reduced-motion: reduce)` بـ`globals.css`). | **✅ مُنفّذ** |
| 6.5 | [Navbar.tsx:76](src/components/layout/Navbar.tsx#L76)، [Footer.tsx:11](src/components/layout/Footer.tsx#L11) | شعار الموقع (`<img src="/logo.jpg">`) بدل `next/image` — موثّقة أصلاً بـ`CLAUDE.md` (gotcha #4) كتحذير ESLint معروف وخارج نطاق التنظيف الحالي؛ أذكرها هون فقط لإنها تؤثر أيضاً على أداء تحميل الصورة (لا layout shift محسوب، لا lazy loading تلقائي) لا على الوصولية (الـ`alt` موجود وصحيح بالحالتين). | **تحسين اختياري** *(معروفة مسبقاً، مش اكتشاف جديد)* |

---

## ملخص الأولويات (بعد التنفيذ)

| الأولوية | العدد الأصلي | الحالة النهائية |
|---|---|---|
| **حرجة** | 6 | **6/6 مُنفّذة بالكامل** |
| **متوسطة** | 8 | **6/8 مُنفّذة بالكامل**، 2 مُعالَجة جزئياً (1.2 إيموجي "من نحن"، 5.3 إيموجي لوحة الأدمن — فجوات 💰/🎯/🏆/⏸▶ متبقية، موثّقة أعلاه بكل بند) |
| **تحسين اختياري** | 8 | **8/8 مُنفّذة** (منها 5.2 و5.5 بحلول حقيقية ضمن قيد "بصري بس"، لا حلول جزئية أو مؤجلة) |

**ملاحظتان إيجابيتان** تستاهل الذكر: زر "دخول" بصفحة تسجيل الدخول يحقق حجم اللمس بالكامل، وصفحة تفاصيل المنتج فيها تسلسل عناوين (`h1`→`h3`) سليم — عكس لوحة الأدمن (تم حلها أيضاً).

---

## استثناء موثّق: الرموز الزخرفية العائمة (△○✕□)

الرموز العائمة الأربعة بخلفية الهيرو ([page.tsx:45-52](src/app/page.tsx#L45))، خلفية صورة تفاصيل المنتج ([products/[id]/page.tsx:140](src/app/products/[id]/page.tsx#L140))، وسطر الفوتر ([Footer.tsx:70](src/components/layout/Footer.tsx#L70)) **استُثنيت عمداً من تنفيذ `no-emoji-icons`** ولن تتحول لـSVG. هذي رموز زخرفية بحتة (نقش بصري متكرر بالخلفية) مش أيقونات وظيفية تنقل معنى أو تُستخدم كعنصر تحكم — قرار هوية بصرية (brand pattern)، خارج نطاق معيار `no-emoji-icons` اللي يستهدف الأيقونات الهيكلية (تنقل، أزرار، حالات) حصراً.

---

## حالة التنفيذ النهائية (فرع `design/ui-ux-audit-fixes`)

كل بنود التقرير الـ22 (حرجة + متوسطة + تحسين اختياري) اتنفذت أو عُولجت جزئياً بفروع
commits منفصلة، بعد تحقق `npx tsc --noEmit` + `npm run build` + `npm run lint` +
معاينة HTML فعلي عبر `curl` لكل واحد. التحقق النهائي الشامل: **صفر أخطاء TypeScript،
build ناجح، صفر تحذيرات lint جديدة** (نفس الأربع تحذيرات الأصلية الموثقة بـCLAUDE.md
فقط: `no-img-element`×3، `no-require-imports`×4 بـ`lib/db.ts`، `no-unused-expressions`×1).

### فجوات مكتشفة أثناء التنفيذ (مش قرارات تأجيل — سهو بتخطيط الدفعات الأصلي)
- 💰 (Banknote): موجودة بـ[dashboard/page.tsx:906, 1026](src/app/admin/dashboard/page.tsx#L906)
  — ما كانت بأي دفعة تنفيذ (1/2/3) رغم ورودها بالجرد الأصلي المقترح على المستخدم.
- 🎯 (Target): مستخدمة بقائمة تصنيف [products/page.tsx:85](src/app/products/page.tsx#L85)
  ("الكل") بمعنى مختلف عن 🎯 اللي بلوحة الأدمن (المُنفّذ فعلاً كـ`Target`) — لم تُعالَج.
- 🏆 (Trophy): مُنفّذة بـ`page.tsx` (بطاقة "من نحن")، لكن **نفس الرمز لسا إيموجي**
  بكرت إحصائيات لوحة الأدمن [dashboard/page.tsx:418](src/app/admin/dashboard/page.tsx#L418).
- ⏸/▶ (إيقاف/تشغيل): لم تُكتشف بالجرد الأصلي إطلاقاً (نطاق يونيكود مختلف عن باقي
  الإيموجي المفحوصة) — منتشرة بعدة مواضع بلوحة الأدمن (حالة الخصم/الإعلان/القاعدة).
- [RelatedProducts.tsx:27](src/components/ui/RelatedProducts.tsx#L27): نفس نمط
  `sm:grid-cols-2` الزائد (بند 1.4/2.3) موجود هون كمان — المكوّن مش من ضمن الصفحات
  الخمس المُدقَّقة أصلاً، فما انلمس.

### استثناءات تقنية دائمة (غير قابلة للحل بـSVG مهما كان)
- 🔥 بـ[dashboard/page.tsx:783](src/app/admin/dashboard/page.tsx#L783): نص داخل
  سمة `placeholder` — سمات HTML نصية بحتة، ما تقبل عناصر React.
- 🎮/⭐ بـ[dashboard/page.tsx:1243-1244](src/app/admin/dashboard/page.tsx#L1243):
  داخل وسم `<option>` — المتصفح يرندر قوائم `<select>` الأصلية كنص فقط.

### ملاحظة تحقق منهجية
عدة مودالات/أقسام بلوحة الأدمن (كل المودالات الخمسة، جدول المنتجات، RelatedProducts)
تُعرض بشكل شرطي بناءً على client state يبدأ "مغلق" افتراضياً (`useState(false)`) أو
بيانات تُجلب بعد الـhydration — يعني بعض التعديلات (خصوصاً جوا المودالات) ما قدرت
تتأكد من ظهورها بـHTML عبر `curl` مباشرة (بما إنه بيطلب تنفيذ JS/تفاعل مستخدم فعلي)،
واعتمدت بدلها على: نجاح `build`/`lint`/`tsc`، مراجعة الكود، وتأكيد نفس النمط مرئي
بمواضع أخرى غير مشروطة بنفس الملف.

لا merge ولا push — الفرع جاهز للمراجعة النهائية.
