# تقرير تدقيق أنظمة الخصومات — Billy Store

**تاريخ التدقيق:** 2026-07-25
**تاريخ الإصلاح:** 2026-07-25 (نفس اليوم)، فرع `fix/discount-system-issues`
**نطاق التدقيق الأصلي:** استكشاف وتوثيق فقط — لا يتضمن أي تعديل كود ولا اقتراح خطة إصلاح/دمج.
**منهجية التحقق:** قراءة كود كاملة لكل ملف ذي علاقة + تحقق عملي فعلي (dev server حقيقي، تسجيل دخول أدمن حقيقي، طلبات curl فعلية على كل API route بقيم حدّية، وتصفح فعلي عبر Chrome DevTools MCP لواجهة لوحة التحكم والمتجر). كل قيمة اختبارية حدّية أُعيدت لحالتها الأصلية فوراً بعد التحقق منها (تفصيل كامل في القسم الأخير).

---

## ⚠️ حالة الإصلاح (تحديث لاحق)

**كل بنود جدول المشاكل (قسم 6) انحلّت وتحقّقت فعلياً عدا بندين مؤجَّلين بقرار صريح:**
- **بند 13** (تاريخ بداية/نهاية للخصم) — **لم يُلمَس**. هذا طلب ميزة جديدة، مش إصلاح خلل، خارج نطاق جلسة الإصلاح بالكامل.
- **بند 15** (توحيد منطق الأولوية العام×المستهدف المكرَّر بثلاث ملفات: `ProductCard.tsx`, `products/page.tsx`, `products/[id]/page.tsx`) — **لم يُلمَس عمداً**. يحتاج جلسة منفصلة لأنه يمس منطق حساب السعر مباشرة. تم التحقق (قسم 6 أدناه) إنه لسا شغّال صح بعد كل إصلاحات هالجلسة (فحص رجوعي كامل عبر الصفحات الثلاث).

باقي الـ14 بند (1-12، 14، 16) **انحلّت بالكامل**، كل واحد بـcommit منفصل على فرع `fix/discount-system-issues`، مع إعادة تشغيل نفس اختبار التقرير الأصلي (curl/متصفح) للتأكد من التغيير الفعلي، وتنظيف فوري لأي بيانات اختبارية بعد كل خطوة. التفاصيل الكاملة بجدول قسم 6 (عمود "الحالة بعد الإصلاح" الجديد).

---

## 1. جرد الأنظمة الموجودة فعلياً بالكود

### 1.1 الخصم العام (Global Discount)

| الطبقة | الموقع |
|---|---|
| Frontend | [src/app/admin/dashboard/page.tsx](src/app/admin/dashboard/page.tsx) — state أسطر 43-46، fetch أسطر 83-90، handlers `saveDiscount`/`removeDiscount`/`toggleDiscount` أسطر 288-314، بانر أسطر 428-467، مودال أسطر 1154-1209 |
| API | [src/app/api/admin/discounts/global/route.ts](src/app/api/admin/discounts/global/route.ts) — `GET`/`POST`/`DELETE` (30 سطر بالكامل) |
| القراءة العامة | [src/app/api/discounts/route.ts](src/app/api/discounts/route.ts) (`GET` عام، يرجّع `{global, targeted}` معاً) |
| DB | لا يوجد جدول مخصص — JSON blob واحد بجدول `settings` تحت مفتاح `'discount'`، شكله `{percentage, label, active}` |

**ما يُفترض أن يعمل حسب الكود:** خصم بنسبة مئوية واحدة (`percentage`) واسم عرض (`label`) وحالة تفعيل (`active`)، يُطبَّق على *كل* المنتجات في المتجر بدون تعديل الأسعار المخزّنة فعلياً — مجرد عرض حسابي وقت العرض (`Math.round(price * (1 - percentage/100))`).

### 1.2 الخصومات المستهدفة (Targeted Rules)

| الطبقة | الموقع |
|---|---|
| Frontend | نفس الملف — state أسطر 47-59، fetch أسطر 101-107، handlers `saveTargetedRule`/`deleteTargetedRule`/`toggleTargetedRule` أسطر 109-159، قائمة أسطر 469-541، مودال أسطر 1022-1152 |
| API | [src/app/api/admin/discounts/targeted/route.ts](src/app/api/admin/discounts/targeted/route.ts) — `GET`/`POST`/`PUT`/`DELETE` (83 سطر بالكامل) |
| DB | JSON array بنفس جدول `settings` تحت مفتاح `'targeted_discounts'`، كل عنصر `{id, type, label, percentage, active, productIds, minPrice, maxPrice}`، و`id = Date.now()` (لا ضمان تفرّد حقيقي على مستوى DB) |

**ما يُفترض أن يعمل حسب الكود:** قائمة قواعد خصم مستقلة، كل قاعدة إما من نوع `'product'` (قائمة معرّفات منتجات محددة) أو `'range'` (نطاق سعري `minPrice`–`maxPrice`)، كل واحدة بنسبتها الخاصة وحالة تفعيلها.

### 1.3 لا يوجد نظام خصم ثالث

بحث شامل (`grep -ril "discount"` عبر `src/app`, `src/lib`, `src/components`) أكّد عدم وجود أي مسار API أو منطق خصم آخر. تحديدًا: `/api/products` و`/api/products/[id]` لا يتضمنان أي منطق خصم، ولا يوجد أي جدول DB مخصص للخصومات (كل شيء JSON blobs بجدول `settings` العام).

### 1.4 ميزتان مجاورتان بنفس الملف — **ليستا** نظام خصم (توضيح لتفادي الخلط)

- **الشريط الإعلاني** ("الإعلان") — نفس نمط UI (بانر + toggle + مودال) لكنه نص إعلاني عادي، لا علاقة له بالسعر.
- **تعديل الأسعار بالجملة** ("Bulk price editor") — يعدّل الأسعار **المخزّنة فعلياً وبشكل دائم** في قاعدة البيانات (تحذير صريح بالواجهة: "هذا الإجراء يعدّل الأسعار الفعلية في قاعدة البيانات بشكل دائم"). لا يقرأ ولا يكتب أي من مفاتيح `discount`/`targeted_discounts` — منفصل كليًا عن حالة الخصم رغم تشابهه البصري.

---

## 2. تصنيف كل ميزة/زر/حقل

الرمز المستخدم: ✅ شغالة بالكامل (تحققت فعلياً) — ⚠️ منطق ناقص/غير مكتمل — ❌ غير شغالة إطلاقًا — 🔀 غير متسقة بين النظامين

### 2.1 الخصم العام

| الميزة | التصنيف | كيف تحقّقت |
|---|---|---|
| بانر الحالة (نشط/متوقف/لا يوجد) | ✅ | قراءة كود مطابقة لمشاهدة فعلية بالمتصفح: baseline فارغ يظهر "لا يوجد خصم مفعّل حالياً" |
| زر "إيقاف مؤقت/تفعيل" | ✅ | `toggleDiscount` يستدعي `POST` فعلي، تحقق عبر قراءة كود |
| زر "+ إضافة خصم" (فتح المودال) | ✅ | نُقر فعليًا بالمتصفح، فتح المودال بالقيم الافتراضية (20%، "عرض خاص") |
| زر "حذف" | ⚠️ | يعمل (استدعاء `DELETE` حقيقي، تحقق عبر curl) لكن **بدون confirmation dialog** — بعكس حذف منتج اللي عنده confirm modal (أسطر 758-779 بنفس الملف) — تناقض UX داخل نفس الملف |
| حقل نسبة الخصم (`min="1" max="90"`) | ❌ (عمليًا غير فعّال) | **تحقّق حي بالمتصفح**: كتابة `500` بالحقل تُظهر `invalid=true` (HTML5 validity) لكن القيمة تُقبل بلا أي منع، ومعاينة السعر بالمودال أظهرت فعليًا "100 ر.س → **-400** ر.س" أمام عين الأدمن. لا `<form>` حقيقي يمنع الإرسال (الزر `onClick` عادي وليس `type="submit"`) |
| زر "حفظ الخصم" | 🔀 | يرسل `parseFloat()` بدون أي clamp لا بالفرونت ولا بالباك إند (بعكس النظام المستهدف اللي عنده clamp سيرفر-سايد) — **تأكيد عبر curl**: POST بـ`percentage:-50` و`percentage:500` كلاهما قُبل وخُزّن حرفيًا كما هو (اختباران منفصلان، انظر قسم 4) |
| حقل اسم العرض (label) | ⚠️ | لا حد أقصى للطول (بعكس النظام المستهدف اللي بيقص لـ100 حرف)، ولا يمنع القيمة الفارغة |
| معاينة الأثر ("منتج بسعر 100 → X") | ✅ | تُحدَّث حيًا مع تغيير الحقل — وهي نفسها اللي كشفت مشكلة القيمة السالبة أعلاه |
| زر "إلغاء" | ✅ | تحقّق حي بالمتصفح: إلغاء بعد كتابة 500% لم يُرسل أي طلب، `GET` تالٍ أكّد `null` |
| `credentials:'include'` على `saveDiscount`/`toggleDiscount` | ⚠️ | غائب (بعكس كل استدعاء آخر بالملف)، على الأغلب غير مؤثر عمليًا لأنه same-origin، لكنه تناقض بالكود |
| `GET` بدون مصادقة (`/api/admin/discounts/global`) | ❌ أمان | **تحقّق فعلي عبر curl بدون كوكي**: أرجع 200 وبيانات كاملة (نسبة الخصم الفعلية) — انظر قسم 3 |
| معالجة الأخطاء (JSON فاسد، بيانات تالفة) | ❌ | **تحقّق فعلي**: إرسال body غير JSON صالح لـ`POST` أرجع 500 غير معالَج (لا try/catch بالراوت إطلاقًا) |

### 2.2 الخصومات المستهدفة

| الميزة | التصنيف | كيف تحقّقت |
|---|---|---|
| قائمة القواعد (عرض) | ✅ | baseline فارغ يظهر "لا توجد قواعد خصم مستهدفة" — تطابق فعلي |
| زر "+ إضافة قاعدة" | ✅ | نُقر فعليًا، فتح مودال "خصم مستهدف جديد" بالقيم الافتراضية |
| اختيار النوع (نطاق سعري / منتجات محددة) | ✅ | نُقر فعليًا على "منتجات محددة" — ظهرت قائمة كاملة بكل الـ8 منتجات مع أسعارها وcheckbox لكل واحد |
| حقل نسبة الخصم (`min="1" max="90"`) | ⚠️ (نفس مشكلة الحقل، لكن الباك إند يحمي) | نفس القيد الشكلي غير الفعّال بالواجهة، لكن **الفرق الجوهري**: `POST` يعمل `Math.min(90, Math.max(1, ...))` فعليًا — **تحقّق عبر curl**: إرسال `percentage:200` أرجع القاعدة محفوظة بـ`percentage:90` (مقصوصة صح) |
| زر "تعديل" (pencil) لكل قاعدة | ✅ | يملأ الفورم الكامل من بيانات القاعدة، تحقق عبر قراءة كود (أسطر 512-527) |
| زر Pause/Play لكل قاعدة | ✅ | `toggleTargetedRule` — استدعاء `PUT` حقيقي |
| زر حذف (X) لكل قاعدة | ⚠️ | يعمل (تحقّق عبر curl: `DELETE` فعلي بحذف صحيح) لكن **بدون confirmation dialog** — نفس تناقض الخصم العام |
| حقل السعر من/إلى (لنوع range) | ❌ | **تحقّق عبر curl**: إرسال `minPrice:100, maxPrice:10` (معكوسان) قُبل وخُزّن حرفيًا بدون أي رفض أو تصحيح — القاعدة تُحفظ لكنها لن تطابق أي منتج أبدًا (فشل صامت، ليس خطأ ظاهر للأدمن) |
| حقل النوع (`type`) | ❌ | **تحقّق عبر curl**: إرسال `type:"bogus-type"` قُبل وخُزّن كما هو — القاعدة تُحفظ لكنها لن تنطبق على أي منتج أبدًا (منطق `getProductDiscount` بالفرونت يتحقق فقط من `'product'`/`'range'` صراحة) |
| زر "حفظ" (تعديل قاعدة موجودة عبر PUT) | ❌ أمان/سلامة بيانات | **تحقّق عبر curl — أخطر ثغرة بالتقرير**: `PUT` يعمل `{...existingRule, ...body}` بدون أي إعادة تحقق — أنشأنا قاعدة صحيحة (`percentage:50`)، ثم أرسلنا `PUT {id, percentage:500}` ونجح التخزين بـ`percentage:500` **رغم أنّ نفس القيمة عبر `POST` كانت سترفض/تُقصّ لـ90**. أي تعديل لقاعدة موجودة يتجاوز كل حماية الإنشاء |
| زر "إلغاء" بالمودال | ⚠️ | يُغلق المودال لكن **لا يصفّر `targetedForm`/`editingRule`** (بعكس النقر خارج المودال اللي بيصفّرهم) — بيانات قديمة ممكن تظهر بالمرة الجاية لو فُتح المودال عبر "+ إضافة قاعدة" |
| `DELETE` بمعرّف غير موجود | ⚠️ | **تحقّق عبر curl**: `DELETE {id:999999999}` أرجع `{"success":true}` بصمت (فلترة `!==id` ما بتطابق شي، فمافي تغيير فعلي ولا خطأ) — لا 404، لا رسالة توضّح إنه المعرّف غير موجود |
| `GET` بدون مصادقة | ❌ أمان | **تحقّق عبر curl بدون كوكي مع بيانات حقيقية**: أرجع القائمة الكاملة (قاعدة بنسبة 77% ومعرّفات منتجات) — انظر قسم 3 |
| `fetchTargetedRules` عند فشل الجلب | ⚠️ | `catch{}` فاضي بالفرونت — فشل الشبكة يظهر كـ"لا توجد قواعد" بلا تمييز عن الحالة الفعلية الفارغة |

### 2.3 مقارنة تناسق (🔀) بين النظامين

| الجانب | الخصم العام | المستهدف | الحكم |
|---|---|---|---|
| Clamp النسبة سيرفر-سايد | ❌ لا يوجد | ✅ موجود بـ`POST` (لكن غائب بـ`PUT`) | غير متسق |
| حد طول التسمية | ❌ لا يوجد | ✅ 100 حرف | غير متسق |
| Confirmation عند الحذف | ❌ لا يوجد | ❌ لا يوجد | متسق (لكن كلاهما ناقص مقارنة بحذف منتج) |
| `credentials:'include'` | ⚠️ ناقص بـ`POST` | ✅ موجود بكل مكان | غير متسق |
| تاريخ بداية/نهاية (expiry) | ❌ غير موجود | ❌ غير موجود | متسق (ميزة غائبة بالكامل من كلا النظامين) |

---

## 3. الأمان/تسريب البيانات — بُعد خطورة مستقل

**ثغرة مؤكدة عمليًا بكلا الراوتين:** `GET /api/admin/discounts/global` و`GET /api/admin/discounts/targeted` **لا يتحققان من `isAuthenticated`** إطلاقًا (بعكس `POST`/`PUT`/`DELETE` المحميين بشكل صحيح بكليهما).

**دليل التحقق الفعلي (curl بدون أي كوكي جلسة):**

```
# ضبطنا خصم عام حقيقي (10%) مؤقتًا لأغراض الاختبار، ثم:
curl http://localhost:3000/api/admin/discounts/global   → 200 {"percentage":500,"label":"over-test","active":true}
```
```
# ضبطنا قاعدة مستهدفة حقيقية (77%) مؤقتًا، ثم:
curl http://localhost:3000/api/admin/discounts/targeted  → 200 [{"id":...,"percentage":77,"label":"leak-test",...}]
```

هذا تسريب بيانات تجارية حقيقية (نسب خصم فعلية، تسميات عروض، معرّفات منتجات مستهدفة) لأي زائر غير مسجّل دخول — **مختلف تمامًا عن بُعدي "سعر الزبون" و"تجربة إدارية"** أعلاه، لأنه لا يغيّر أي سعر ولا يعطّل أي وظيفة، لكنه يكشف معلومات كان يُفترض أن تبقى خلف تسجيل الدخول (المسار العام المخصص لهذا الغرض أصلًا هو `/api/discounts`، وهو بلا مصادقة بتصميم مقصود ومُوثّق).

---

## 4. تعارض الخصم العام × المستهدف — الفحص الفعلي

### 4.1 المنطق كما يظهر بالكود (مكرَّر بثلاث ملفات منفصلة، بدون دالة مشتركة)

الملفات: `ProductCard.tsx` (`getProductDiscount`، أسطر 49-78)، `products/page.tsx` (`getDiscount`)، `products/[id]/page.tsx` (`getPageData`) — نفس المنطق حرفيًا بالثلاثة:

1. تُفحص كل القواعد المستهدفة **الفعّالة فقط** (`active`)، ويُختار الأعلى `percentage` من بين القواعد المنطبقة على المنتج (لا تراكم بين عدة قواعد منطبقة).
2. الخصم العام يُستخدم **فقط كـ fallback**: `if (!best && global?.active)` — أي لو انطبقت أي قاعدة مستهدفة، الخصم العام يُتجاهل كليًا (لا ضرب، لا جمع).

### 4.2 التحقق العملي الفعلي بالمتصفح

**الإعداد:** خصم عام 10% فعّال + قاعدة مستهدفة type=`product` بنسبة 30% على المنتج #1 (God of War Ragnarok، السعر الأصلي 149 ر.س) فعّالة.

| الصفحة | الملف المسؤول | النتيجة المشاهَدة فعليًا |
|---|---|---|
| الصفحة الرئيسية (بطاقة المنتج) | `ProductCard.tsx` | **-30%**، 149 ← **104** ر.س |
| صفحة قائمة المنتجات `/products` | `products/page.tsx` | **-30%**، 149 ← **104** ر.س (كل المنتجات الأخرى أظهرت -10% الخصم العام كما متوقّع) |
| صفحة تفاصيل المنتج `/products/1` | `products/[id]/page.tsx` | **-30%**، 149 ← **104** ر.س، والتسمية المعروضة "targeted-30-p1" (تسمية القاعدة المستهدفة، وليست تسمية الخصم العام) — حتى رابط طلب واتساب استخدم السعر المخفَّض 104 |

**الخلاصة:** السلوك الفعلي مطابق تمامًا لما يقوله الكود بالثلاث صفحات — القاعدة المستهدفة تطغى بالكامل على الخصم العام (بدون تراكم)، وهذا ثابت عبر كل نقاط العرض الثلاث. **لا يوجد باگ حسابي أو عدم اتساق بين الصفحات حاليًا** — لكن بما إنّ المنطق مكرَّر يدويًا بثلاث أماكن منفصلة بدون دالة/ملف مشترك، أي تعديل مستقبلي جزئي (تصحيح بملف واحد بس) سينتج فورًا تناقضًا فعليًا بالسعر المعروض لنفس المنتج حسب الصفحة.

---

## 5. تنظيف وتحقق نهائي من سلامة البيانات

كل اختبار حدّي كتب/عدّل بيانات حقيقية بقاعدة `data/billy.db` تم عكسه فورًا بعد تسجيل نتيجته، مع `GET` تأكيدي قبل الانتقال للاختبار التالي — لم تُترك أي قيمة فاسدة مفعّلة لأكثر من لحظة الاختبار الواحد.

| الاختبار | القيمة المؤقتة | التنظيف | GET تأكيدي بعد التنظيف |
|---|---|---|---|
| Global percentage=-50 | `{"percentage":-50,"label":"neg-test",...}` | `DELETE` فوري | `null` ✅ |
| Global percentage=500 (+ تأكيد تسريب) | `{"percentage":500,"label":"over-test",...}` | `DELETE` فوري | `null` ✅ |
| Global percentage مفقود (NaN→null) | `{"percentage":null,"label":"nan-test",...}` | `DELETE` فوري | `null` ✅ |
| Global body غير JSON صالح | (500 غير معالَج، لا تخزين) | لا حاجة (لم يُخزَّن شيء) | `null` ✅ |
| Targeted percentage=200 (اختبار clamp) | قاعدة محفوظة بـ`percentage:90` | `DELETE` بالمعرّف | `[]` ✅ |
| Targeted PUT bypass (percentage=500) | قاعدة معدَّلة لـ`percentage:500` | `DELETE` بالمعرّف | `[]` ✅ |
| Targeted type="bogus-type" | قاعدة بنوع غير صالح محفوظة | `DELETE` بالمعرّف | `[]` ✅ |
| Targeted minPrice>maxPrice (100>10) | قاعدة محفوظة كما هي | `DELETE` بالمعرّف | `[]` ✅ |
| Targeted DELETE بمعرّف غير موجود | لا تغيير فعلي | — | `[]` ✅ |
| Targeted leak-test (تأكيد تسريب) | قاعدة 77% | `DELETE` بالمعرّف | `[]` ✅ |
| اختبار التعارض (Global 10% + Targeted 30%/منتج#1) | كلا السجلين فعّالان معًا | `DELETE` لكليهما بعد التحقق بالمتصفح | `null` / `[]` ✅ |
| استكشاف واجهة لوحة التحكم (فتح مودالات، كتابة 500% بالحقل، فتح تبويب "منتجات محددة") | لا شيء — كل الإجراءات كانت "إلغاء" وليست "حفظ" | لا حاجة | `null` / `[]` ✅ (تأكيد نهائي مزدوج) |

**مقارنة baseline النهائية:**

| | قبل بدء التدقيق (baseline) | بعد انتهاء كل الاختبارات |
|---|---|---|
| `GET /api/admin/discounts/global` | `null` | `null` ✅ **مطابق** |
| `GET /api/admin/discounts/targeted` | `[]` | `[]` ✅ **مطابق** |

الحالة الكاملة رجعت مطابقة تمامًا لما كانت عليه قبل التدقيق — لا أثر متبقٍ لأي بيانات اختبارية بقاعدة البيانات الحقيقية.

---

## 6. جدول المشاكل — بثلاثة أبعاد خطورة

**الأبعاد:** 🔴 سعر حقيقي للزبون | 🟡 تجربة إدارية فقط | 🔵 أمان/تسريب بيانات

**عمود "الحالة بعد الإصلاح"**: يعكس فرع `fix/discount-system-issues` (2026-07-25) — ✅ يعني تم التحقق فعليًا (curl/متصفح) إنه السلوك القديم الموصوف بعمود "الوصف" لم يعد يحدث.

| # | القسم | الموقع بالكود | الوصف | التصنيف (قسم 2) | الخطورة | الحالة بعد الإصلاح |
|---|---|---|---|---|---|---|
| 1 | الخصم العام | `global/route.ts:18` | `percentage` بدون أي clamp (`Math.min/max`) — نسبة سالبة أو >100% تُخزَّن كما هي وتُطبَّق مباشرة على سعر حقيقي (`Math.round(price*(1-percentage/100))`) — **تحقّق حي بالمتصفح: معاينة أظهرت "100 ← -400 ر.س"** | ❌ غير شغالة (حماية) | 🔴 عالية | ✅ محلول — `POST` يرفض `NaN`/غير رقمي بـ400، ويقصّ 1-90. تحقّق: `percentage:-50`→1، `percentage:500`→90، حقل مفقود→400 مرفوض |
| 2 | الخصومات المستهدفة | `targeted/route.ts:58` (PUT) | `PUT` يعمل `{...r, ...body}` بدون أي إعادة تحقق — يتجاوز clamp الـ1-90 اللي `POST` بيفرضه. **تحقّق فعلي: قاعدة أُنشئت بـ50% ثم عُدِّلت عبر PUT لـ500% ونجحت** | ❌ غير شغالة (حماية) / 🔀 غير متسقة | 🔴 عالية | ✅ محلول — دالة `validateRuleInput` مشتركة بين POST وPUT؛ نفس سيناريو الاختبار أُعيد وأكّد القصّ لـ90 هالمرة |
| 3 | كلا النظامين | `global/route.ts:5`, `targeted/route.ts:8` | `GET` بدون `isAuthenticated` — تسريب نسب خصم وتسميات عروض ومعرّفات منتجات مستهدفة فعلية لأي زائر غير مسجّل. **تحقّق فعلي بـcurl بدون كوكي مع بيانات حقيقية** | ❌ غير شغالة (حماية) | 🔵 أمان | ✅ محلول — `isAuthenticated` مضافة لكلا GET؛ تحقّق: GET بدون كوكي يرجع 401 بدل بيانات، لوحة التحكم (اللي بترسل الكوكي دائمًا) غير متأثرة |
| 4 | كلا النظامين | `global/route.ts` (بالكامل), `targeted/route.ts` (بالكامل) | لا try/catch إطلاقًا — body غير JSON صالح أو بيانات تالفة بالـDB → 500 غير معالَج. **تحقّق فعلي: body غير JSON صالح أرجع 500** | ❌ غير شغالة | 🟡 إدارية (لا يوصل لزبون لأن `/api/discounts` العام عنده try/catch يحميه بحالة الفشل) | ✅ محلول — try/catch حول parsing وDB بكل من GET/POST/PUT/DELETE بكلا الملفين؛ تحقّق: body غير JSON صالح على 4 endpoints رجع 400 منظّم بدل 500 خام |
| 5 | الخصومات المستهدفة | `targeted/route.ts:32-33` | `minPrice`/`maxPrice` بدون تحقق `min<max` — قاعدة بقيم معكوسة تُحفظ بصمت لكن لن تطابق أي منتج أبدًا (فشل صامت، لا رسالة توضيحية للأدمن). **تحقّق فعلي عبر curl** | ⚠️ منطق ناقص | 🟡 إدارية (القاعدة معطوبة لكن لا تنتج سعر خاطئ — فقط لا تعمل) | ✅ محلول — رفض 400 صريح لو `minPrice > maxPrice` (POST وPUT)؛ تحقّق: `100/10` رُفض على الاثنين، وقاعدة موجودة بقيت بدون تغيير عند محاولة PUT معكوسة |
| 6 | الخصومات المستهدفة | `targeted/route.ts:25` | `type` غير مُتحقَّق منه — أي قيمة (أو غياب القيمة) تُقبل وتُخزَّن، والقاعدة تصمت (لا تنطبق على شيء) بدل رفض واضح. **تحقّق فعلي عبر curl** | ⚠️ منطق ناقص | 🟡 إدارية | ✅ محلول — رفض 400 لأي `type` غير `'product'`/`'range'` (POST وPUT)؛ تحقّق: `"bogus-type"` رُفض على الاثنين |
| 7 | كلا النظامين | `dashboard/page.tsx` (حقول percentage، أسطر ~1165 و~1056) | قيود `min="1" max="90"` بالـHTML شكلية بحتة (لا `<form>` حقيقي، الحفظ عبر `onClick` عادي) — **تحقّق حي**: كتابة 500 أظهرت `invalid=true` لكن لم تُمنع، والمعاينة أظهرت سعرًا سالبًا فعليًا للأدمن قبل الحفظ | ❌ غير شغالة (حماية) | 🔴 عالية (بما إنّ الطريق مفتوح فعليًا للوصول لسعر زبون خاطئ عبر الباك إند غير المحمي أيضًا — بند 1) | ✅ محلول — تحقق JS فعلي قبل الإرسال بـ`saveDiscount`؛ تحقّق حي بالمتصفح: كتابة 500 أظهرت رسالة خطأ حمراء ومنعت الإرسال (تأكّد بـGET إنه ما تخزّن شي) |
| 8 | الخصم العام + المستهدف | `dashboard/page.tsx:459-463` (global) و`532-535` (targeted) | حذف الخصم العام أو أي قاعدة مستهدفة بدون confirmation dialog، بعكس حذف منتج بنفس الملف (أسطر 758-779) اللي عنده confirm modal كامل | ⚠️ 🔀 غير متسقة | 🟡 إدارية | ✅ محلول — استُخرج `ConfirmDialog` مشترك (`src/components/ui/ConfirmDialog.tsx`)، يُستخدم الآن للثلاث حالات (منتج/خصم عام/قاعدة مستهدفة)؛ تحقّق بصري حي للثلاث: فتح صحيح، إلغاء بدون حذف، تأكيد بحذف فعلي |
| 9 | الخصم العام | `dashboard/page.tsx:290-294, 308-312` | `saveDiscount`/`toggleDiscount` بدون `credentials:'include'` بعكس كل استدعاء fetch آخر بالملف (على الأرجح غير مؤثر عمليًا لأنه same-origin) | 🔀 غير متسقة | 🟡 إدارية (منخفضة الأثر عمليًا) | ✅ محلول — أُضيفت `credentials:'include'` للاثنين؛ تحقّق حي: حفظ/تفعيل/إيقاف الخصم العام عبر المتصفح لسا شغالين |
| 10 | الخصومات المستهدفة | `dashboard/page.tsx:1145-1148` | زر "إلغاء" بمودال القاعدة المستهدفة لا يصفّر `targetedForm`/`editingRule` (بعكس النقر خارج المودال اللي بيصفّرهم) — بيانات قديمة ممكن تظهر بفتحة تالية للمودال عبر "+ إضافة قاعدة" | ⚠️ منطق ناقص | 🟡 إدارية | ✅ محلول — زر الإلغاء صار يصفّر نفس الحالة؛ تحقّق بصري حي: فتح تعديل قاعدة → إلغاء → "+ إضافة قاعدة" أظهر فورم فاضي فعليًا |
| 11 | الخصومات المستهدفة | `dashboard/page.tsx:106` | `fetchTargetedRules` عندها `catch{}` فاضي — فشل الجلب يظهر كقائمة فارغة بدون تمييز عن "لا توجد قواعد فعلًا" | ⚠️ منطق ناقص | 🟡 إدارية | ✅ محلول — state جديد `targetedFetchError` يميّز الحالتين برسالة تحذير مختلفة؛ تحقّق حي عبر محاكاة فشل الجلب فعليًا بالمتصفح (init script يعترض الطلب) |
| 12 | الخصومات المستهدفة | `targeted/route.ts:71-75` (DELETE) | حذف بمعرّف غير موجود يرجع `{"success":true}` بصمت (لا 404، لا تنبيه) — **تحقّق فعلي عبر curl** | ⚠️ منطق ناقص | 🟡 إدارية | ✅ محلول — يرجع 404 صريح الآن لو المعرّف غير موجود؛ تحقّق: حذف حقيقي لسا يرجع 200، حذف معرّف وهمي يرجع 404 |
| 13 | كلا النظامين | لا يوجد بأي من الراوتين | لا مفهوم تاريخ بداية/نهاية (expiry/scheduling) بأي من النظامين — الخصم يُفعَّل/يُوقَف يدويًا فقط | (ميزة غائبة بالكامل، ليست "معطوبة") | 🟡 إدارية | ⏸️ **مؤجَّل بقرار** — ميزة جديدة مش إصلاح خلل، لم يُلمَس إطلاقًا بهالجلسة |
| 14 | القراءة العامة | `discounts/route.ts:26` | Cache-Control `s-maxage=30, stale-while-revalidate=120` — تغييرات الأدمن ممكن تتأخر بالظهور للزوار حتى ~150 ثانية (مضاعَف مع `revalidate=30` بصفحة تفاصيل المنتج) | (سلوك مقصود جزئيًا، لكن غير موثّق كقرار) | 🟡 إدارية (تأخر وليس خطأ سعر) | ✅ محسوم — القيم أُبقيت كما هي بقرار صريح من المستخدم، وتوثيقها بـ`CLAUDE.md` (Gotchas #8) كقرار مقصود لتخفيف حمل Turso |
| 15 | البنية العامة | `ProductCard.tsx`, `products/page.tsx`, `products/[id]/page.tsx` | منطق أولوية العام×المستهدف مكرَّر يدويًا بثلاث ملفات منفصلة بدون دالة مشتركة — **مؤكَّد أنه متطابق حاليًا فعليًا (قسم 4)**، لكنه خطر صيانة: أي تصحيح جزئي مستقبلي (ملف واحد فقط) سينتج تناقضًا فعليًا بالسعر المعروض لنفس المنتج حسب الصفحة | (ليست علة حالية، بل خطر بنيوي) | 🟡 إدارية/صيانة (لن يتحول لـ🔴 إلا عند تعديل مستقبلي جزئي) | ⏸️ **مؤجَّل بقرار** — يمس منطق حساب السعر بثلاث ملفات، جلسة منفصلة قادمة. **تحقّق رجوعي فقط**: أُعيد سيناريو التعارض (عام 10% + مستهدف 30%/منتج) بعد كل إصلاحات هالجلسة، وتأكّد لسا يعمل صح ومتسق عبر الصفحات الثلاث — لم يتأثر بأي من الإصلاحات |
| 16 | الخصم العام | `global/route.ts:18` | لا حد أقصى لطول `label` (بعكس المستهدف اللي بيقص لـ100 حرف) | 🔀 غير متسقة | 🟡 إدارية | ✅ محلول — `label` يُقصّ لـ100 حرف الآن مطابق للمستهدف؛ تحقّق: إرسال 150 حرف رجع مخزَّن بـ100 بالضبط |

---

## ملخص سريع (التدقيق الأصلي)

- **نظاما خصم فقط**، لا ثالث. كلاهما مبنيان على نفس نمط JSON blob بجدول `settings`.
- **لا يوجد "زر ميت" واحد** بأي من النظامين — كل عنصر واجهة مربوط فعليًا بمنطق حقيقي (state أو fetch)، لكن جزء كبير من هذا المنطق **غير محمي بما يكفي** سواء بالفرونت (قيود HTML شكلية) أو الباك إند (غياب clamp/validation في نقاط محددة، وخصوصًا `PUT` على المستهدف).
- **أخطر مشكلتين مؤكدتين عمليًا** بتأثير مباشر على سعر حقيقي للزبون: غياب clamp الخصم العام (بند 1)، وتجاوز الـ`PUT` لكل حماية الإنشاء بالمستهدف (بند 2).
- **ثغرة أمان مستقلة مؤكدة عمليًا**: تسريب بيانات الخصم عبر `GET` غير محمي بكلا الراوتين (بند 3).
- **التعارض بين العام والمستهدف يعمل حاليًا بشكل صحيح ومتسق** عبر الصفحات الثلاث المسؤولة عن عرض السعر — لكنه هش بنيويًا بسبب التكرار اليدوي بثلاث ملفات (بند 15).
- كل بيانات الاختبار أُزيلت وتأكّدت مطابقة baseline الأصلي (`null` / `[]`) بنهاية التدقيق.

## ملخص سريع (بعد جلسة الإصلاح — 2026-07-25، فرع `fix/discount-system-issues`)

- **14 من 16 بند انحلّت بالكامل** (1-12، 14، 16)، كل واحد بـcommit منفصل، مع إعادة تشغيل نفس اختبار التقرير الأصلي للتأكد من التغيير الفعلي (مو قراءة كود فقط) وتنظيف فوري لأي بيانات اختبارية.
- **بند 13 وبند 15 مؤجَّلان بقرار صريح** — الأول ميزة جديدة (تاريخ بداية/نهاية)، والثاني يمس منطق حساب السعر بثلاث ملفات ويحتاج جلسة منفصلة. **تحقّق رجوعي كامل** أُجري لبند 15 (سيناريو التعارض عام×مستهدف عبر الصفحات الثلاث) للتأكد إنه لم يتأثر بأي إصلاح.
- **استُخرج مكوّن `ConfirmDialog` مشترك** (`src/components/ui/ConfirmDialog.tsx`) يُستخدم الآن لحذف المنتج والخصم العام والقاعدة المستهدفة — الثلاثة تحقّقت بصريًا (فتح/إلغاء/تأكيد) بالمتصفح فعليًا.
- **`npm run build`**، **`npm run lint`**، و**`npx tsc --noEmit`** الثلاثة نجحوا بدون أخطاء (تحذيرات ESLint المتبقية كلها سابقة الوجود وغير متعلقة بهالجلسة: `no-img-element`، `no-require-imports` بـ`db.ts`، وتحذير `no-unused-expressions` بدالة `toggleSelect` غير المتعلقة بالخصومات).
- بنهاية كل الإصلاحات، أُعيد سيناريو التعارض الكامل (خصم عام 10% + قاعدة مستهدفة 30% على منتج) وتأكّد لسا يعمل صح ومتطابق عبر الصفحة الرئيسية وصفحة القائمة وصفحة التفاصيل — ثم نُظّفت كل بيانات الاختبار وتأكّدت المطابقة النهائية مع baseline (`null` / `[]` / 8 منتجات).
